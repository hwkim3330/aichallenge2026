# 목표 — 두 트랙 (2026-07-31)

두 트랙을 번갈아 진행한다. 공통 자산이 하나 있다: **라이다로 다른 차를 보는 것**. MPC 트랙에서는
완주율 문제고, AI 트랙에서는 슬라이드 필수 항목("다른 차량에 대한 회피 및 정지 고려")이다.
한 번 풀면 둘 다 쓴다.

---

# 트랙 1 — SW+AI (MPC 레이싱)

## 채점이 무엇인지

Elo 배틀: **challenger + ranker + NPC("Computer")**, 6 랩 필요, **480 초** 제한, 순위로 레이팅 이동.
보드 배틀 40 건 중 **34 건이 랩 수로 갈린다.** 0 랩·4 랩 완주가 흔하고 랭킹 9·12 위 팀도 4 랩에서
멈춘다. **완주가 1 순위, 총시간은 랩 동수일 때만 작동하는 타이브레이크.**

우리: 일반부(코스 2), 레이팅 734, 코스 2 내 22/60. 넘어야 할 1 위 CRESCendo 810.
참고 페이스: KSK(학생부 1 위) 6 랩 282.155 초, 최고 랩 44.010.

## 목표

| | 현재 최선 | 목표 |
|---|---|---|
| 480 초 내 6 랩 완주율 | 9/12 (75%) | **12/12** |
| 완주시 총시간 | 347.5 평균 / 285 최고 | 평균 **320 이하** |
| 교착 사건 | 레이스당 다수 | **0** |

## 페이스는 이미 끝났다

교착이 한 번도 없는 실행은 랩당 46.7 초, 5 랩 233.5 초 — **6 랩 환산 280 초로 KSK 의 282.155 를
넘는다.** 남은 손실 전부가 교착이다. 교착 1 건이 약 40 초.

## 확립된 것

`stuck_recovery_controller.cpp:127` 이 `command.longitudinal.speed < 1.0` 이면 빠져나간다. QP 가
실행 불가능해지면 MPC 는 0 을 명령하므로 **복구 노드가 존재하는 이유인 상황을 복구 노드가 볼 수
없다.** 실측: 랩 3 을 t+164 초에 끝낸 실행이 391 초를 명령속도 0.0 / 정지감지 0 회로 서 있었다.

교착 차단(정지 상태에서 일정 시간 후 1.5 m/s 명령)으로 12 레이스 랩 합이 **32 -> 68** 이 됐다.
이전 제출본은 **그리드 슬롯 3 에서만 완주한다**(4/4, 나머지 2/8).

480 초 기준 완주율은 구제 발동 지연에 단조롭게 반응한다: `TICKS` 20 -> 7/12, 40 -> 9/12, 70 -> 9/12.
반대 방향(`fast-escape`, 0.2 초 3.0 m/s)은 구제 498 회로 최악. **급하게 빼내면 스래싱이 된다.**
`FUNNEL_RATE` 는 0.15 가 0.25 와 0.08 을 모두 이긴다 — 깔때기는 열되 닫는 힘이 필요하다.

## 남은 레버

1. 교착 사건 수를 0 으로. 지금은 탈출을 개선했을 뿐 발생을 막지 못했다.
2. NPC 대응. NPC 는 **v2x 에 절대 안 실린다**(실측: 2 대 + NPC 구성에서도 v2x 는 2 건만 보고).
   라이다만 본다. **`lidar_guard` 는 NPC 가 있는 조건에서 여전히 측정된 적이 없다** — 아래의
   2026-08-02 정정 참조.

### 2026-08-02 — NPC 1 대가 랩 진행을 70% 깎는다 (단, 원인 미분리)

**정정**: 이 절의 첫 판은 7/31 의 `npc-with` / `npc-without` 을 라이다 on/off A/B 로 읽었다.
틀렸다. `awsim.log` 의 실행 스크립트를 보면 두 arm 은 **세 가지가 동시에 다르다**:

| | `npc-with-*` = `npc1.sh` | `npc-without-*` = `solo6.sh` |
|---|---|---|
| NPC | **1** | **0** |
| lidar | **cpu** (가드 활성) | **off** (가드 passthrough) |
| 라이다 CPU 비용 | 있음 | 없음 |

그 외(1 대, 6 랩, 480 s, collisions on, handicap off, wall-recovery off, ranking on, count 시작)는
동일하다. 스캔 토픽 유무만 보고 라이다 A/B 라고 판단한 것이 잘못이었다.

점수가 남아 있지 않아 `tools/analyse_stall_from_bag.py` 로 bag 에서 복원한 값은 유효하다:

| arm | laps@480s | 정지% | 정지 이벤트 | 최장 정지 | 평균 속도 |
|---|---:|---:|---:|---:|---:|
| NPC 1 대 (+lidar on) | **2.15** (2.13-2.19) | 67.5-68.0 | 37-44 | 61-72 s | 1.17 |
| 솔로 (+lidar off) | **7.08** (4.19-10.12) | 15-51 | 1-3 | 83-223 s | 3.46-6.19 |

**읽을 수 있는 것**: NPC 가 하나 있는 조건에서 랩 진행이 2.15 랩/480 s 로 무너진다. 3 회가
2.13/2.13/2.19 로 거의 동일해서 우연이 아니다. 6 랩 완주가 필요한데 2 랩밖에 못 가고, **모든
채점 배틀에는 NPC 가 있다.** 이것만으로도 완주율 문제의 핵심 후보다.

**읽을 수 없는 것**: 원인. NPC 에 막히는 것인지, 가드가 NPC 를 보고 제동하는 것인지, 라이다
렌더링의 CPU 경합인지 이 데이터로는 분리되지 않는다.

**한 가지는 깨끗하게 분리된다**: 같은 날 밤 `solo6lidar.sh`(솔로 + **라이다 on** + 가드 기본값)로
돌린 두 번은 6/6 완주에 평균 46.45 / 46.56 s 였다. 즉 **가드는 솔로에서 무해하다** — 벽과 콘이
`front_limit=1.25 / side_limit=0.72` 안에 있어도 제동하지 않는다. 그러므로 가드가 문제라면
그것은 벽이 아니라 **다른 차량에 대해** 제동하는 것이고, 이는 이미 기록된
"회피를 켜면 추월이 아니라 감속 후 줄서기가 된다"와 같은 양상이다.

**분리 실험 실행함 (2026-08-02, n=1/arm)**: `npc1.sh` 고정, 가드 한계값만 변경.
`GUARD_FRONT_LIMIT` / `GUARD_SIDE_LIMIT` / `GUARD_SPEED` 를 compose → `run_autoware.bash` →
`aichallenge_system.launch.xml` → `aichallenge_submit.launch.xml` → `reference.launch.xml` 까지
배선했다(그전에는 외부에서 설정 불가였고, 인자를 선언·전달하지 않으면 조용히 무시된다).
라이브 `ros2 param get /lidar_guard front_limit` 로 0.0 이 실제로 들어갔음을 확인했다.

| arm | 가드 | 공식 완주 | laps@480s | 정지% | 정지 이벤트 | 최장 정지 | 평균 속도 |
|---|---|---:|---:|---:|---:|---:|---:|
| A | 기본값 1.25 / 0.72 | **3/6** | **3.46** | 60.1 | 23 | **42.8 s** | 2.39 |
| B | **0 / 0 (무력)** | 2/6 | 2.79 | 60.0 | 32 | **144.2 s** | 1.85 |

**가설과 반대다.** 가드를 끄면 더 나빠진다 — 특히 최장 정지가 42.8 → 144.2 초로 늘어난다.
가드는 NPC 를 만났을 때 **종국적 갇힘을 줄이는 쪽**으로 작동하고 있었다. 기본값 유지가 맞다.

**단서를 붙인다: arm 당 n=1 이다.** 이 레포의 규칙대로 다차량 결과는 랩 수 급 차이만 신뢰할 수
있고 3 vs 2 랩은 그 급이긴 하지만 단일 샘플로는 트래픽 운과 구분되지 않는다. 확정하려면
arm 당 2-3 회가 더 필요하다. 다만 결론이 "지금 기본값을 그대로 둔다"이므로 행동은 바뀌지 않는다.

**더 중요한 관측**: 두 arm 모두 **60% 정지**다. 가드가 그 60% 의 원인이 아니다. arm A 의 랩은
113 / 81 / 256 초, arm B 는 68 / 152 초 — 어느 쪽이든 NPC 를 만나면 길게 박힌다. 즉 남은 레버 1
("교착 **발생**을 0 으로")이 여전히 본체이고, 가드도 복구 노드도 그것을 막지 못한다.

### 2026-08-02 — 교착의 해부: NPC 뒤 줄서기가 아니라 지오메트리 끼임

위 두 bag 의 20 초 이상 정지 구간에서 라이다 섹터별 최소거리와 명령을 봤다
(전방 ±0.35 rad, 좌 0.35~1.6, 우 -1.6~-0.35, 각 구간 중앙값):

| bag | t0 | 길이 | 전방 | 좌 | 우 | \|steer\| | accel |
|---|---:|---:|---:|---:|---:|---:|---:|
| A | 42 s | 21 s | 14.37 | **0.55** | 5.70 | 0.172 | 0.45 |
| A | 159 s | 28 s | 5.61 | 2.04 | 4.43 | 0.389 | 1.35 |
| A | 274 s | 21 s | **0.05** | **0.05** | **0.05** | **0.550** | 1.00 |
| A | 304 s | 43 s | **0.05** | **0.05** | **0.05** | **0.550** | 1.00 |
| A | 373 s | 26 s | 0.10 | 0.07 | 5.52 | **0.550** | 1.00 |
| B | 335 s | **144 s** | 0.24 | 0.26 | 0.23 | 0.126 | **0.00** |

**줄서기가 아니다.** NPC 뒤에 대기 중이라면 전방만 2-5 m 로 막히고 측면은 열려 있고 조향은 작다.
실제로는 전 방향이 0.05-0.25 m 다 — 차가 지오메트리에 끼어 있다. NPC 의 역할은 접촉을 만들어
라인 밖으로 밀어내는 것이고, 끼는 것은 그 다음이다. 추월 능력 문제가 아니다.

**그리고 두 arm 의 실패가 서로 다르다**:

- **arm A: 복구가 작동하는데 못 빼낸다.** `|steer| 0.550` 은 MPC 가 아니라 복구 노드 자신의
  deep-escape 조향(`stuck_recovery_controller.cpp:186`, `±0.55F`)이고, `accel 1.00` 은 그
  후진 명령(`publishCommand(speed=-2.0, acceleration=1.0, steering)`)이다. 즉 21-43 초 동안
  풀 락 후진을 명령하면서 차가 나오지 못한다. 전 방향 0.05 m 는 라이다가 쓸 만한 반사를 못
  받을 정도로 끼었다는 뜻이다.
- **arm B: 복구가 아예 발동하지 않는다.** 144 초 동안 `accel 0.00` — 교착 차단기가 걸리지 않았다.

둘 다 "탈출 실패"이고, 이것이 레버 1 의 실체다.

### 2026-08-02 — 탈출 기동 수정 시도: 목표는 달성, 랩은 후퇴 → 기본값 off

두 가지를 구현했다(둘 다 `stuck_recovery_controller`):

1. **직진 후진** (`RECOVERY_STRAIGHT_ESCAPE`). deep escape 는 `+0.55` ↔ `-0.55` 로 **양쪽 다
   풀 락**만 번갈아 썼다. 풀 락 후진은 뒤가 옆으로 스윙하는데 측정된 끼임에서는 좌·우도
   0.05 m 라 스윙할 공간이 없다. 가장 적은 여유를 요구하는 직진 후진이 한 번도 시도되지
   않고 있었다 → 3 회마다 한 번 직진을 섞는다.
2. **크립 → 후진 승격** (`RECOVERY_CREEP_ESCALATION`). 끼인 차를 뺄 수 있는 후진은 stuck
   경로에만 있고, 그 경로는 nominal 이 "움직여라"(speed ≥ 1.0 **그리고** accel ≥ 0.3)라고
   할 때만 열린다. QP 가 infeasible 하면 MPC 는 ~0 을 명령하므로 문이 닫힌 채다.
   실측: arm B 의 144 초 구간에서 stuck 게이트 충족은 **16.6%** 뿐인데 차는 **71.5%** 를
   0.1 m/s 이하로 있었고, 후진은 한 번도 돌지 않았다 → 크립이 2 회 연속 실패하면 승격한다.

`npc1` 조건 실측 (arm C, 새 로직 5 회 발동 확인):

| arm | 로직 | 공식 완주 | laps@480s | 정지% | 정지 횟수 | 최장 정지 |
|---|---|---:|---:|---:|---:|---:|
| A | 기존 | **3/6** | **3.46** | 60.1 | 23 | 42.8 s |
| B | 기존, 가드 off | 2/6 | 2.79 | 60.0 | 32 | 144.2 s |
| C | **새 탈출** | 2/6 | 2.85 | **55.4** | 37 | **15.8 s** |

**의도한 것은 달성했다** — 최장 정지 42.8 → 15.8 초, 총 정지 60.1 → 55.4%. 레이스를 끝내는
144 초급 갇힘이 사라졌다. **그러나 랩이 후퇴했다**(3.46 → 2.85, 정지 횟수 23 → 37). 빨리
빠져나오는 대신 더 자주 다시 낀다 — 이 레포가 이미 기록한 "급하게 빼내면 스래싱이 된다"와
같은 양상이다.

랩이 판정 기준이므로 **둘 다 기본값 off** 로 두고 환경변수로 남겼다. 켜려면
`RECOVERY_STRAIGHT_ESCAPE=1` / `RECOVERY_CREEP_ESCALATION=1`.

**방법론 실수를 기록한다**: 두 변경을 **동시에** 넣고 한 번 측정했다. 그래서 스래싱이 어느
쪽에서 왔는지 귀속할 수 없고, arm 당 n=1 이라 3.46 vs 2.85 가 노이즈인지도 모른다.
다음에는 **하나씩** 켜서, arm 당 2 회 이상 돌릴 것.

### ★ 2026-08-02 정정 — npc1 단일 실행 비교는 전부 결론 불가였다 ★

**동일 설정을 반복했더니 1.09 랩이 흔들렸다.**

| run | 설정 | laps@480s | 공식 | 최장 정지 | 정지 횟수 |
|---|---|---:|---:|---:|---:|
| A | 기본값 | **3.46** | 3/6 | 42.8 s | 23 |
| **A2** | **기본값(동일)** | **2.37** | 2/6 | 16.5 s | 39 |
| C | 새 탈출 | 2.85 | 2/6 | 15.8 s | 37 |
| B | 가드 off | 2.79 | 2/6 | 144.2 s | 32 |

C(2.85)와 B(2.79)가 **둘 다 A 의 범위 [2.37, 3.46] 안**이다. 따라서:

- **"가드를 끄면 더 나쁘다"(커밋 `e2f4a1d`) — 근거 없음.** 행동(기본값 유지)은 그대로가 맞지만
  이유는 성립하지 않는다. 가드는 여전히 **미측정**이다.
- **"새 탈출이 랩을 깎는다"(커밋 `be35b59`) — 근거 없음.**
- **"새 탈출이 최장 정지를 42.8 → 15.8 초로 줄였다" — 근거 없음.** 기본값인 A2 도 16.5 초다.
  42.8 초는 A 한 번의 값이었을 뿐이다.

7/31 의 npc1 3 회가 2.13/2.13/2.19 로 폭 0.06 랩이었던 것이 오해를 불렀다. 그 조건은 저분산이
아니었거나 다른 국면이었다. **npc1 에서 실행 1 회당 숫자 1 개를 비교하지 말 것.**

### 더 나은 추정량과, 그것이 드러낸 것

실행당 정지 이벤트가 23-39 개씩 있으므로 그 **분포**를 쓰면 표본이 수십 배가 된다:

| arm | 정지 횟수 | **총 정지시간** | 중앙값 | p90 | 최대 |
|---|---:|---:|---:|---:|---:|
| A | 23 | **288 s** | 8.61 | 24.88 | 42.8 |
| A2 | 39 | **300 s** | 6.89 | 12.95 | 16.5 |
| C | 37 | **266 s** | 5.14 | 12.96 | 15.8 |
| B | 32 | **288 s** | 4.60 | 6.35 | 144.2 |

**총 정지시간이 네 설정 모두 266-300 초로 사실상 불변이다** — 480 초의 약 60%. 개입들은 정지를
잘게 쪼갤 뿐 총량을 줄이지 못한다. 랩은 총 정지시간이 결정하므로 랩도 안 움직인다.

이것이 방향을 정한다: **탈출 측 튜닝으로는 이 문제를 못 고친다.** 더 빨리 빠져나오는 것이
아니라 애초에 끼지 않는 것 — 원래의 레버 1 — 이 유일한 길이다.

(순위합 검정으로 B 가 기본값 대비 중앙값이 유의하게 짧게 나오지만, 한 실행 안의 정지들은
독립이 아니고 실행 수준 분산과 정지 수준 분산이 섞여 있어 과대추정이다.)

### 2026-08-02 재정정 — 총 정지시간도 안정적이지 않다

위에서 "총 정지시간은 266-300 s 로 불변"이라 쓰고, 기본값 2 회(288/300, 폭 12 s)만 보고
**"랩보다 8 배 안정적인 지표"**라고 판단했다. 플래그 arm 을 반복하니 틀렸다:

| run | 설정 | laps@480s | 총 정지 | 정지 횟수 | 최장 |
|---|---|---:|---:|---:|---:|
| A | 기본값 | 3.46 | 288 s | 23 | 42.8 s |
| A2 | 기본값 | 2.37 | 300 s | 39 | 16.5 s |
| C | 플래그 on | 2.85 | **266 s** | 37 | 15.8 s |
| C2 | 플래그 on | 1.84 | **326 s** | 40 | 79.8 s |
| B | 가드 off | 2.79 | 288 s | 32 | 144.2 s |

플래그 arm 이 266-326 s 로 **60 s** 흔들린다. 표본 2 개로 분산을 추정한 같은 실수를 두 번
했다. **npc1 조건에서는 랩도 총 정지시간도 n=2 로는 아무것도 구분하지 못한다.**

측정 비용을 적어둔다: 총 정지시간의 SD 가 약 30 s 이므로 30 s 크기의 효과를 보려면 arm 당
**약 16 회**(≈2.7 시간)가 필요하다. 실행 1 회가 10 분이다. 이 조건에서 소형 효과를 A/B 로
쫓는 것은 비용이 맞지 않는다. **솔로는 46.45 / 46.56 으로 0.1 s 재현성이 있으므로, 솔로로 잴
수 있는 것은 반드시 솔로로 잴 것.**

### ★ 산수 하나가 탈출/가드 방향 전체를 닫는다 ★

NPC 는 랩당 약 **149 s** 로 돈다(보드 기록). 480 s 창에서:

- NPC 를 **완벽히 추종**만 해도 480/149 = **3.2 랩**. 현재 최고가 3.46 랩이므로 개선이 아니다.
- 6 랩을 채우려면 랩당 **80 s 이하**여야 한다. 현재 3.46 랩은 랩당 139 s.
- 우리 깨끗한 랩은 46.5 s 이므로 **추월만 하면** 여유가 크다.

즉 **감속·탈출을 아무리 잘해도 6 랩에 도달할 수 없다. 추월이 필수다.**

그런데 추월하려면 NPC 가 플래너의 경로 제약에 들어가야 하는데, NPC 는 v2x 에 실리지 않으므로
(실측) `use_path_constraints_topic` 경로로는 영원히 보이지 않는다. 유일한 NPC 인지 수단인
`lidar_guard` 는 **제동만** 하고 조향을 만들지 않는다. 게다가 `front_limit=1.25 m` 는 8.4 m/s
에서 **0.15 s** 앞이고, 1.6 m/s² 로 멈추려면 22 m 가 필요하다 — 가드는 충돌을 막을 수 있는
거리에 있지도 않으며, 접촉 후 증상을 완화할 뿐이다.

**따라서 남은 유일한 실질적 방향은 라이다에서 얻은 장애물을 플래너의 경로 제약으로 넣는 것**
이다(브레이크가 아니라 조향을 만드는 것). 탈출 기동 튜닝과 가드 임계값 튜닝은 여기서 종료한다.

### ★★ 2026-08-02 — 회피 기능은 지금까지 한 번도 작동한 적이 없다 ★★

그 방향을 설계하려고 배선을 따라가 보니, 새로 만들 것이 거의 없고 대신 **빠진 층이 두 개**다.

| 층 | 상태 |
|---|---|
| `config.yaml`: `use_path_constraints_topic: true`, `use_border_cells_topic: true` | 켜져 있음 |
| MPC 가 `/path_constraints_provider/{path_constraints,border_cells}` 구독 | 구독함 |
| **`path_constraints_provider` 노드 실행** | **어디서도 launch 되지 않음** |
| 그 노드가 필요로 하는 `/aichallenge/objects` 발행 | **실시간 발행자 없음** |
| 정적 대체 `obstacles.csv_path` | `""` (빈 문자열) |
| launch 의 `use_obstacle_avoidance` 기본값 | `false` |

실행 노드 목록(arm A, 17 개)에 `path_constraints_provider` 가 없다. `run_mpc_controller.bash`
는 MPC 만 exec 한다. `/aichallenge/objects` 를 다루는 파일 3 개는 전부 구독자이거나 bag 파서다
(`object_marker.py` 시각화, `obstacle_parser.py` bag→CSV, provider 자신). AWSIM 도 발행하지
않는다(Player.log 의 MultiDomain 발행 목록에 없음).

**즉 장애물 집합이 영구히 비어 있다.** 그리고 이것이 기록을 다시 읽게 만든다 —
"회피 ON 이 OFF 와 솔로 성능 동일(46.87/47.31/47.35)이라 성능 손해 없음, 새 기본값으로 채택"
은 **플래그를 켜도 아무것도 바뀌지 않았기 때문**이다. 손해가 없던 게 아니라 기능이 없었다.
`use_obstacle_avoidance` 는 launch 기본값이 `false` 이기도 하다.

**그래서 할 일은 "회피 기능 개발"이 아니라 배선 복구 + 소스 하나 추가다:**

1. `path_constraints_provider` 를 launch 에 넣는다(이미 launch 파일이 패키지에 있다:
   `multi_purpose_mpc_ros/launch/path_constraints_provider.launch.py`).
2. `/aichallenge/objects` 를 발행하는 노드를 만든다. 계약은 `Float64MultiArray`, stride 4,
   `data[i], data[i+1]` 이 map 좌표 x, y (provider 의 `_obstacles_callback` 참조). 반경은
   provider 가 `obstacles.radius=1.25` 로 붙인다.
3. 그 노드의 입력은 `/sensing/lidar/scan` — NPC 를 보는 유일한 센서다. 벽·콘과 NPC 를 가르는
   방법이 본체이고, 기준 경로 corridor 안쪽에 들어온 반사만 남기는 것이 1 차 후보다.

**검증 주의**: 1 번만 해도 정적 CSV 없이 provider 가 빈 장애물로 제약을 발행하므로, MPC 동작이
바뀔 수 있다. 1 번과 2 번을 따로 측정할 것. 그리고 npc1 조건의 측정 비용(위 참조)을 감안해
효과가 큰 것만 이 조건에서 재고, 작은 것은 솔로에서 잴 것.

### 2026-08-02 — 1 번(배선) 실행: 노드는 살렸고, 켜면 솔로가 39% 느려진다

provider 를 `use_obstacle_avoidance` 에 물려 launch 에 넣자 **크래시가 세 겹**으로 나왔다.
셋 다 "이 노드는 실행된 적이 없다"의 증거다:

1. `MPC.__init__() missing 2 required positional arguments` — 호출의 `True, True` 가
   `max_steering_rate` / `wp_id_offset` 자리에 들어가고, 시그니처가 실제로 끝나는 두 플래그는
   빠져 있었다.
2. `'MPCConfig' object has no attribute 'steer_rate_max'` — `MPCConfig` 생성 호출이 6 인자
   뒤처져 있었다.
3. `MPCConfig.__init__() takes 11 positional arguments but 17 were given` — **근본 원인**:
   `MPCConfig` 는 공유 import 가 아니라 두 파일에 **각각 정의된 데이터클래스**다
   (provider 10 필드, `mpc_controller` 16 필드). 컨트롤러가 발전하는 동안 provider 는 옛
   버전에 얼어붙었고, 아무도 실행하지 않으니 드리프트가 보이지 않았다.

셋을 고치니 provider 가 초기화를 통과해 살아 있고, MPC 가
`wait until path constraints received...` 를 찍는다 — **이 핸드셰이크가 성립한 게 처음이다.**

그러나 **켜면 비싸다.** `solo6lidar`, 장애물 소스는 아직 없으므로 제약은 정적 맵에서만 나온다:

| | 완주 | 평균 | 최고 | 총 |
|---|---:|---:|---:|---:|
| 회피 OFF (기준선, n=2) | 6/6 | **46.45 / 46.56** | 45.96 | ~279 s |
| 회피 ON (provider 가동) | 6/6 | **64.56** | 45.86 | 387 s |

랩: 69.19 / 46.11 / 56.75 / 46.37 / 45.86 / 123.09.

**최고랩이 45.86 으로 기준선과 같다.** 제약이 체계적으로 틀렸다면 전 랩이 나빠져야 하는데
대부분 정상이고 셋만 무너진다 — 간헐적 방해의 형태이고, provider 가 0.5 Hz 로 전 웨이포인트
(~350 개)에 대해 제약을 재계산하는 부하와 맞는다. 솔로 기준선 폭이 0.11 s 인데 차이가 +18 s
(그 160 배)이므로 n=1 이어도 방향은 분명하다.

**결론**: 배선은 살렸으나 당시 구현으로는 켤 수 없었다. → 아래에서 해결.

### 2026-08-02 — provider 캐싱: 비용의 93% 회수, 이제 켤 만하다

원래 루프는 장애물이 바뀌지 않아도 **매 주기 전 웨이포인트(~350 개)에 대해**
`update_path_constraints` 를 다시 돌렸다. 장애물이 그대로면 결과가 같으므로 순수 낭비이고,
`/aichallenge/objects` 에 발행자가 없는 지금은 **항상** 그대로다. 변경이 있을 때만 재계산하고
그 외에는 캐시된 배열을 재발행하도록 바꿨다.

| | 평균 | 최고 | 총 | 완주 |
|---|---:|---:|---:|---:|
| 회피 OFF (기준선, n=2) | 46.45 / 46.56 | 45.96 | ~279 s | 6/6 |
| 회피 ON, 캐시 전 | 64.56 | 45.86 | 387 s | 6/6 |
| **회피 ON, 캐시 후** | **47.77** | 46.24 | 286.6 s | 6/6 |

랩: 47.48 / 46.42 / **53.85** / 46.24 / 46.39 / 46.27.

**정정 (반복 실행 후)**: "초과분의 93% 를 회수했다"는 위 판단은 **근거 없다.** run 1 하나로
낸 것이고, 반복하니 다르다:

| 실행 | 랩 | 평균 |
|---|---|---:|
| 기준선 OFF (n=2) | 전부 45.96-47.82 | 46.45 / 46.56 |
| 캐시 후 run 1 | 47.48 / 46.42 / **53.85** / 46.24 / 46.39 / 46.27 | 47.77 |
| 캐시 후 run 2 | 47.45 / 46.59 / 46.21 / 46.3 / **152.79** / 46.17 | **64.25** |

캐시 전이 64.56 이었으니 run 2(64.25)와 사실상 같다. **캐싱은 문제를 없앤 것이 아니라 모양을
바꿨을 뿐이다.**

**실제 양상**: provider 를 켜면 **실행당 정확히 한 랩이 망가지고, 심각도는 무작위**다
(53.85 s, 152.79 s). 나머지 다섯 랩은 매번 기준선과 같다. 기준선 두 실행에는 그런 랩이 없다.

이것이 원인을 좁힌다. 꾸준한 CPU 부하라면 **모든** 랩이 느려져야 하는데 그렇지 않다 —
**이산적 사건**이고, 지속시간(40-150 s)이 이 문서 앞부분에서 해부한 끼임과 정확히 같은 규모다.
유력한 가설: 회피가 켜지면 MPC 가 자체 계산 대신 토픽으로 받은 제약을 쓰는데
(`set_path_constraints`), provider 의 제약은 차량의 실제 자세가 아니라 경로를 따라 합성한
`pose` 로 계산된다. 그 불일치가 어느 지점에서 QP 를 infeasible 로 만들면 곧바로 끼임이 된다.

**따라서 2 번(라이다 장애물 발행) 이전에 이것부터 고쳐야 한다.** 지금 켜면 장애물 없이도
실행당 한 랩을 잃는다. launch 기본값 `use_obstacle_avoidance: false` 유지.

### 2026-08-02 — 가설 수정: 회피가 끼임을 만드는 게 아니라, 끼임을 비싸게 만든다

autoware 로그의 복구 이벤트를 세어 위 가설을 검증하니 두 군데가 어긋난다:

| run | 회피 | stuck | yield | 최악 랩 | 평균 |
|---|---|---:|---:|---:|---:|
| teacher_solo_01 | OFF | 0 | 0 | 정상 | 46.45 |
| teacher_solo_02 | OFF | **2** | **1** | **정상** | 46.56 |
| avoid_on_solo4 | ON (캐시 전) | 6 | 4 | 123.09 | 64.56 |
| avoid_cached_solo | ON (캐시 후) | **0** | **0** | **53.85** | 47.77 |
| avoid_cached_solo2 | ON (캐시 후) | 4 | 2 | 152.79 | 64.25 |

1. `avoid_cached_solo` 는 복구 이벤트 **0 건인데 53.85 s 랩**이 났다 — 그 랩은 끼임이 아니다.
2. 기준선 `teacher_solo_02` 는 복구가 **3 번 발동했는데 최악 랩이 47.8 s** 다 — 비용 없이 지나갔다.

그러므로 "회피가 끼임을 유발한다"는 틀렸다. 더 맞는 서술은:

> 복구 이벤트는 양쪽 조건에서 모두 일어난다. **회피가 켜졌을 때만 그것이 120-150 s 손실로
> 이어진다.** 기준선에서는 같은 이벤트가 랩타임에 흔적을 남기지 않는다.

기전으로 일관된다 — 차가 밀려난 뒤 provider 의 제약(차량 실제 자세가 아니라 경로를 따라 합성한
`pose` 로 계산)이 **복구된 위치에서도** QP 를 infeasible 로 만들면 라인을 되찾지 못한다.
기준선에서는 MPC 가 자기 자세로 제약을 직접 계산하므로 그런 불일치가 없다.

**단서**: 기준선 n=2, 회피 n=3 이다. 그리고 이 실행들에 `RECORD=0` 을 걸어 **복구 지속시간을
잴 telemetry 를 스스로 없앴다** — 오늘 BC 주행 테스트에서 한 것과 같은 실수다.
**다음 회피 실행은 반드시 `REC_DIR` 로 기록할 것.**

### ★ 2026-08-02 확정 — 회피는 복구를 유발하지 않는다. 5 배 비싸게 만든다 ★

측정 단위를 실행당에서 **이벤트당**으로 바꿨다(`tools/recovery_cost.py`): 복구가 트리거된
시각부터 속도가 2 m/s 이상으로 1 초 이상 회복될 때까지의 시간. 480 s 실행 하나가 랩타임 숫자
하나가 아니라 복구 이벤트 3-6 개를 준다.

| 조건 | n | 이벤트별 회복 시간 | 중앙값 | 최대 |
|---|---:|---|---:|---:|
| 회피 OFF | 3 | 11.0 / 9.9 / 5.9 s | **9.9 s** | 11.0 s |
| 회피 ON | 6 | 69.0 / 68.0 / 63.9 / 32.8 / 19.6 / 13.6 s | **48.4 s** | 69.0 s |

**두 분포가 겹치지 않는다** — 기준선 최대 11.0 s, 회피 최소 13.6 s. 중앙값 5 배 차이.

이것이 "실행당 정확히 한 랩 붕괴"도 설명한다. 복구 이벤트는 뭉쳐서 온다(이 실행은 55 s 안에
6 건). 건당 10 s 면 흔적이 안 남지만 48 s 면 한 뭉치가 랩 하나를 삼킨다. 회피 ON 4 개 실행이
모두 "정상 5 랩 + 붕괴 1 랩"(123.09 / 53.85 / 152.79 / 119.09)이었던 이유다.

**단서**: 기준선 3 이벤트는 한 실행에서, 회피 6 이벤트도 한 실행에서 나왔으므로 실행 수준
교란이 완전히 배제되진 않는다. 다만 분포가 전혀 겹치지 않아 효과 크기가 그 위험을 압도한다.

**다음 후보(미검증)**: 복구가 활성인 동안에는 토픽 제약을 쓰지 말고 MPC 가 자기 자세로 계산한
제약으로 되돌리는 것. → 아래에서 전제가 틀렸음이 확인됐다.

### 2026-08-02 — 왜 5 배인가: 가설 3 개 기각, 정렬 결함 1 개 발견

먼저 전제 정정. MPC 는 회피가 꺼져 있어도 **자기 자세로 제약을 계산하지 않는다.**
`MPC.py:245` 의 분기는 `if use_obstacle_avoidance and not use_path_constraints_topic:`
이므로 기본 경로(회피 off)도 `else` 로 간다. 차이는 "자체 계산 vs 토픽"이 아니라, 저장 배열이
`update_simple_path_constraints` 로 **한 번** 만들어진 것이냐 provider 가 2 초마다 덮어쓰는
것이냐다.

`tools/corridor_compare.py` 로 두 배열을 시뮬레이터 없이 만들어 비교했다(350 웨이포인트, N=20):

| 가설 | 측정 | 판정 |
|---|---|---|
| provider 코리더가 더 좁다 | 폭 평균 3.327 vs 3.339 (차 −0.012 m) | **기각** |
| provider 가 제어 루프를 막는다 | 명령 간격 중앙값 25.0 vs 23.9/24.1 ms, >100 ms 0 건 | **기각** |
| 코리더가 옆으로 밀려 있다 | 중심 차 평균 +0.003 m, 중앙값 +0.002 m | **기각** |

**부호 실수 기록**: 중심 비교를 처음 돌렸을 때 −1.855 m 라는 큰 오프셋이 나왔다. `wp.lb` 는
거리가 아니라 **부호 있는 좌표**(wp0: ub=+3.002, lb=−2.025)인데 이를 거리로 보고 한 번 더
음수화한 탓이다. 중심은 `(ub+lb)/2` 이고 safety margin 은 상쇄된다. 검증 없이 커밋했으면
"코리더가 1.9 m 밀려 있다"는 완전히 틀린 결론이 남을 뻔했다.

**읽다가 발견한 실제 결함 — 행 정렬이 한 칸 어긋난다**:

- `update_simple_path_constraints`: 행 `i` ← `get_waypoint(i + n)`, n=0..N-1
- `update_path_constraints(wp_id, ...)`: 같은 규약으로 `get_waypoint(wp_id + n)`
- 그런데 provider 는 행 `i` 를 `update_path_constraints(i + 1)` 로 채우고 있었다
- `MPC.py:251` 은 행 `model.wp_id + 1` 을 읽으며 "그 행 = 그 인덱스부터의 지평"으로 가정한다

즉 provider 의 모든 행이 웨이포인트 하나(설정 해상도 **0.6 m**)만큼 앞서 있었다. 정상 랩에서는
지평 12 m 중 5% 라 묻히지만, 라인을 벗어나 인접 웨이포인트 간 코리더가 급변하는 복구 중에는
같은 오차가 훨씬 크게 작용할 수 있다. **`wp_id` 로 고쳤다.**

### 2026-08-02 — 정렬 수정: 효과 없음 (가설 4 개째 기각)

`tools/recovery_cost.py` 로 이벤트당 측정:

| 조건 | n | 이벤트별 회복 시간 (s) | 중앙값 |
|---|---:|---|---:|
| 회피 OFF | 3 | 11.0 / 9.9 / 5.9 | **9.9** |
| 회피 ON, 수정 전 | 6 | 69.0 / 68.0 / 63.9 / 32.8 / 19.6 / 13.6 | **48.4** |
| 회피 ON, 정렬 수정 후 | 7 | 76.7 / 75.6 / 71.5 / 26.5 / 20.3 / 6.0 / **200.0** | **71.5** |

**개선되지 않았다** — 오히려 중앙값이 올랐고, 200.0 s 는 측정 상한이므로 **끝까지 복구하지 못한
이벤트**다. 정렬 결함은 실재했고 정합성 차원에서 수정은 유지하지만, 5 배 비용의 원인은 아니다.

기각된 가설 4 개: 코리더 폭 / 제어 루프 타이밍 / 코리더 중심 / 행 정렬.

**남은 유력 후보 — 제 비교의 빈틈**: 위 코리더 비교는 **지평의 첫 열(`[0]`)만** 봤다. MPC 는
N=20 열 전부를 쓴다. 그리고 두 빌더의 성질이 다르다:

- `update_simple_path_constraints` 는 **pose 를 받지 않는다**. 웨이포인트의 정적 ub/lb 뿐이다.
- `update_path_constraints(wp_id, pose, ...)` 는 pose 를 받고, provider 는 그 자리에
  **경로를 따라 합성한 자세**를 넣는다.

따라서 provider 의 배열은 (가짜) 자세에 의존하고 기준선의 배열은 의존하지 않는다.

### 2026-08-02 — 20 열 전체도 동일 (가설 5 개째 기각), 그리고 세 번째 모드 발견

`tools/corridor_compare.py` 를 N=20 전체로 돌리니 열마다 차이가 없다:
|중심 차| 평균 0.026 / p95 0.059 / 최대 0.294 m, |폭 차| 평균 0.012 / p95 0.058 m,
붕괴 셀(ub≤lb) 양쪽 0/7000. **제약의 내용은 사실상 같다.**

원인을 찾다 코드에서 **시도한 적 없는 세 번째 모드**를 발견했다. `MPC.py:245` 의 조건은
`use_obstacle_avoidance and not use_path_constraints_topic` 이므로, **회피 ON + 토픽 OFF** 면
`if` 가지로 가서 매 사이클 **차량의 실제 자세**로 제약을 새로 계산한다. provider 도, 2 초 지연도,
합성 자세도, 배열 덮어쓰기도 없다. 그리고 이 모드에서 장애물은 MPC 자신의 맵으로 들어가므로
**2 단계에서 provider 를 살릴 필요가 없다** — `/aichallenge/objects` 를 MPC 에 직접 먹이면 된다.

launch 게이트를 `use_path_constraints_provider`(기본 off)로 분리하고 config 를
`use_path_constraints_topic: false` 로 바꾼 뒤 측정:

| 모드 | n | 이벤트별 회복 (s) | 중앙값 | 최대 |
|---|---:|---|---:|---:|
| 회피 OFF (기준선) | 3 | 11.0 / 9.9 / 5.9 | **9.9** | 11.0 |
| provider 경유 | 6 | 69.0 / 68.0 / 63.9 / 32.8 / 19.6 / 13.6 | 48.4 | 69.0 |
| provider + 정렬수정 | 7 | 76.7 / 75.6 / 71.5 / 26.5 / 20.3 / 6.0 / 200.0 | 71.5 | 200.0 |
| **자체 계산** | 8 | 24.0 / 23.0 / 19.1 / 5.7 / 66.8 / 65.8 / 40.3 / 6.4 | **23.5** | 66.8 |

**중앙값이 절반 이하로 떨어졌다.** 벌점의 상당 부분이 provider 였다는 뜻이다. 다만 기준선의
2.4 배가 남고, 랩도 74.30 / 116.94 로 둘이 망가졌다. 분포도 이제 일부 겹친다(자체 계산의
5.7 / 6.4 는 기준선 범위 안).

**남은 차이의 유력한 정체**: 자체 계산은 **차량의 실제(=이탈한) 위치**를 기준으로 코리더를 만든다.
기준선의 정적 제약은 **항상 레퍼런스 경로 기준**이다. 이탈 상태에서 전자는 "지금 있는 자리
주변"을 허용 영역으로 주고 후자는 "라인으로 돌아오는 길"을 준다. 회피에는 전자가, 복구에는
후자가 맞다.

### 2026-08-02 — 정확한 기전: `pose` 는 free-segment 선택 시드다

`update_path_constraints(wp_id, pose, ...)` 에서 `pose` 는 딱 한 곳에 쓰인다
(`reference_path.py:973`): 지평 **첫 단계**에서 코리더의 여러 free segment 중 어느 것을 고를지
점수 매길 때의 기준점이다. 이후 단계는 앞 단계 선택을 따라간다.

즉 차가 벽에 밀려 있으면 **차가 갇힌 그 포켓**이 최적 segment 로 뽑히고 계획이 거기 머문다.
기준선의 정적 제약에는 segment 선택 자체가 없어 항상 본선 코리더를 쓴다. 이것이
"자세 기준 vs 경로 기준" 차이의 실체다.

`MPC_RECOVERY_E_Y`(기본 1.0 m) 를 넘는 이탈에서는 시드를 `None` 으로 넘겨 웨이포인트 기준으로
고르게 했다. 이벤트당 측정:

| 모드 | n | 이벤트별 (s) | 중앙값 | 평균 | 최대 |
|---|---:|---|---:|---:|---:|
| 회피 OFF | 3 | 11.0 / 9.9 / 5.9 | 9.9 | 9.0 | 11.0 |
| provider 경유 | 6 | 69.0 … 13.6 | 48.4 | 44.5 | 69.0 |
| 자체 계산 | 8 | 24.0 … 6.4 | 23.5 | 31.4 | 66.8 |
| **자체 + pose-seed** | 4 | 26.4 / 25.3 / 21.4 / 6.0 | 23.3 | **19.8** | **26.4** |

**긴 꼬리가 사라졌다** — 최대 66.8 → 26.4 s, 평균 31.4 → 19.8. 망가진 랩도 2 개 → 1 개
(랩 47.71/46.16/46.39/**76.7**/46.5/46.27, 6/6 완주).

**단서**: n=4 이고 **중앙값은 움직이지 않았다**(23.5 → 23.3). → 반복해서 확인했다, 아래.

### 2026-08-02 — pose-seed 반복: 꼬리 제거는 확인되지 않음 (완만한 개선)

| 실행 | n | 이벤트별 (s) | 중앙값 | 평균 | 최대 |
|---|---:|---|---:|---:|---:|
| 기준선 OFF | 3 | 11.0 / 9.9 / 5.9 | 9.9 | 9.0 | 11.0 |
| 자체 계산 | 8 | 24.0 … 6.4 | 23.5 | 31.4 | 66.8 |
| poseseed run1 | 4 | 26.4 / 25.3 / 21.4 / 6.0 | 23.3 | 19.8 | 26.4 |
| poseseed run2 | 9 | 25.2 / 24.2 / 20.2 / 5.6 / **48.3** / 47.2 / 43.2 / 23.1 / 5.7 | 24.2 | 27.0 | 48.3 |

**run1 의 "최대 26.4 s" 는 재현되지 않았다.** 합산(n=13): 중앙값 24.2(변화 없음), 평균 24.8
(31.4 에서 개선), 최대 48.3(66.8 에서 개선). **완만한 개선이지 해결이 아니다.** 수정은
원리상 옳으므로 유지하되, 회피는 여전히 기준선의 약 2.5 배이고 제출 기본값은 off 다.

**다음 후보 — 설계 차원**: 지금은 회피가 켜지면 **매 사이클** 자세 기반 제약을 계산한다. 그러나
장애물은 드물다(NPC 1 대). → 구현하고 측정했다, 아래.

### 2026-08-02 — 장애물 게이팅: 복구 이벤트 0, 파국 랩 소멸

`MPC._init_problem` 의 분기에 `obstacles_present` 조건을 추가했다. 지도에 장애물이 등록돼
있을 때만 자세 기반 제약을 계산하고, 없으면 정적 경로 기준 배열로 떨어진다 — 회피 off
기준선이 쓰는 바로 그 배열이다. 그 배열은 이제 생성자에서 **항상** 만든다(전에는 회피가
꺼졌을 때만 만들어서 켜면 폴백이 없었다). 시작 시 1 회 비용.

| 모드 | 평균 | 최악 랩 | 복구 이벤트 |
|---|---:|---:|---|
| 기준선 OFF (n=3, eval 포함) | 46.37-46.56 | 47.67 | 0-3 |
| provider 경유 | 64.56 | 123.09 | 6 + 4 |
| 자체 계산 | 62.97 | 116.94 | 6 + 2 |
| pose-seed | 51.62 / 60.03 | 76.7 / 98.47 | — |
| **장애물 게이팅** | **47.86** | **54.55** | **0 / 0** |

랩: 47.89 / 46.28 / 46.08 / 46.00 / 46.36 / **54.55**. **복구 이벤트가 0 건**이고 100 s 급
파국 랩이 사라졌다.

**예측은 빗나갔다**: 솔로에는 장애물이 없으므로 기준선과 **동일**해야 한다고 미리 적었는데,
평균이 47.86 으로 +1.3 s 이고 54.55 s 랩이 하나 남았다.

**반복 결과 "복구 0 건"은 철회한다.** run 1 이 운 좋은 실행이었다:

| 실행 | 평균 | 최악 랩 | stuck | yield |
|---|---:|---:|---:|---:|
| gated run1 | 47.86 | 54.55 | **0** | **0** |
| gated run2 | 55.38 | 87.28 | **6** | **4** |

**그러나 이벤트당 비용은 오늘 최고 기록이다**(`tools/recovery_cost.py`):

| 조건 | n | 이벤트별 (s) | 중앙값 | 평균 | 최대 |
|---|---:|---|---:|---:|---:|
| 기준선 OFF | 3 | 11.0 / 9.9 / 5.9 | **9.9** | 9.0 | 11.0 |
| provider 경유 | 6 | 69.0 … 13.6 | 48.4 | 44.5 | 69.0 |
| 자체 계산 | 8 | 24.0 … 6.4 | 23.5 | 31.4 | 66.8 |
| **장애물 게이팅** | 10 | 38.0 / 34.7 / 28.3 / 25.1 / 19.9 / 12.8 / 8.6 / 5.6 / 7.6 / 6.5 | **16.3** | **18.7** | **38.0** |

10 건 중 4 건이 기준선 범위 안이다. **건당 비용이 기준선의 5 배 → 1.6 배로 좁혀졌다.**

**그래서 문제가 분해된다**: 총 손실 = 빈도 × 건당 비용.
- 건당 비용: 48.4 → 16.3 s (기준선 9.9). 거의 잡혔다.
- **빈도: 기준선 3 건 vs 게이팅 10 건 — 약 3 배. 이제 여기가 본체다.**

즉 회피를 켜면 **끼임 자체가 더 자주 일어난다**. 왜 그런지는 아직 모른다.

**정정**: 위 "빈도 3 배"는 근거가 약하다. 실행별 복구 이벤트는 기준선 (0, 3), 게이팅 (0, 10)
으로 **양쪽 다 n=2 에 0 을 포함**한다. 오늘 반복해서 빠진 함정과 같은 모양이므로, 빈도 차이는
확립되지 않았다.

### ★ 2026-08-02 — 잘못 설계한 실험, 그리고 그것이 드러낸 진짜 병목 ★

건당 비용이 기준선의 1.6 배까지 왔으니 이제 물어야 할 것은 "회피가 얼마나 비싼가"가 아니라
"회피가 실제로 추월을 시켜 랩을 늘리는가"라고 판단하고, 점수가 매겨지는 `npc1` 조건에서
게이팅 회피 ON 을 돌렸다.

결과: **1/6 완주, 단일 랩 194.07 s** (같은 조건 회피 OFF 는 3/6, 2/6).

**그러나 이 실험은 성립하지 않는다.** 바로 앞에서 자세 기반 제약을 `obstacles_present` 에
게이팅했는데, `/aichallenge/objects` 에 발행자가 없으므로 장애물 집합은 **항상 비어 있다.**
즉 게이트가 한 번도 열리지 않았고, 회피 ON 은 **오버헤드만 있고 회피 능력은 0** 인 구성이었다.
추월 이득이 나올 수 없는 설정으로 추월을 측정한 것이다. 돌리기 전에 알았어야 했다.

**그래서 병목이 제자리로 돌아온다**: 오늘 회피 경로에서 고친 것들 — provider 배선(크래시 3 개),
캐싱, 정렬, pose-seed, 게이팅 — 은 전부 **비용 쪽**이다. 그 결과 건당 복구가 48.4 → 16.3 s 로
줄었다. 그러나 **이득 쪽은 한 번도 측정된 적이 없다. 측정할 수가 없다 — 장애물 소스가 없기
때문이다.**

`/aichallenge/objects` 를 라이다에서 발행하기 전에는 회피는 게이팅이든 아니든 아무것도 하지
않는다. **이것이 남은 유일한 작업이다.** 계약은 이미 확인돼 있다: `Float64MultiArray`,
stride 4, `data[i]`, `data[i+1]` 이 map 좌표 x, y. 반경은 소비자가 `obstacles.radius=1.25` 로
붙인다.

### 2026-08-02 — 검출 노드: free-space 소거 기각, 코리더 필터가 후보

`multi_purpose_mpc_ros/lidar_obstacle_publisher.py` 를 만들고 CMakeLists 에 등록했다
(provider 와 같은 `scripts/<name>` + `.bash` 관례). 검증은 시뮬레이터 없이
7/31 의 `npc-with-1` bag 으로 했다 — 스캔과 `kinematic_state` 가 둘 다 있고 NPC 가 있는 실제
주행이다.

**1 차 방식(정적 맵의 free space 에 떨어진 반사 = 동적 장애물) 은 기각.** 이 맵으로는 성립하지
않는다:

| 지표 | 값 |
|---|---|
| 반사 → 가장 가까운 점유 셀 | p50 **1.02 m**, p90 2.40 m |
| 0.6 m 이내 비율 | **36.2%** |
| 자차 위치의 벽 거리 | 중앙값 **2.38 m** (코리더 반폭 ~1.7 m) |
| 검출 클러스터 크기 | 중앙값 **193 빔** / 750 (말이 안 됨) |

프레임은 맞다(맵 origin 89608/43117 이 로그의 트랙 좌표와 일치). 문제는 **맵의 주행가능 영역이
실제 트랙보다 넓다**는 것 — 물리 배리어가 맵상 free space 안에 있어서 그 반사가 전부 동적으로
분류된다. 자차가 벽에서 2.38 m 라는 값이 같은 이야기를 한다.

**2 차 방식(레퍼런스 경로에서의 횡거리로 필터)** 은 훨씬 그럴듯하다. 배리어는 코리더 밖, NPC 는
안이기 때문이다:

| 임계값 | 스캔당 검출 | 0 건 | 1 건 | 2+ | 클러스터 빔(중앙값) |
|---|---:|---:|---:|---:|---:|
| <1.0 m | 0.42 | 178 | 77 | 19 | **20** |
| <1.5 m | 0.64 | 139 | 100 | 35 | 32 |
| <2.0 m | 0.83 | 111 | 105 | 58 | 37 |

클러스터 20 빔은 차량 한 대로 타당하다(193 빔과 대조).

**아직 "검출된다"고 말하지 않는다**: 검출 거리 중앙값이 **1.7 m** 로 의심스럽게 가깝다. NPC 를
랩마다 따라잡는다면 여러 거리에서 잡혀야 한다. 자차 차체나 지면 반사일 가능성이 남아 있고,
그것을 가르기 전에는 노드를 체인에 넣지 않는다. **다음 확인: 검출 위치가 실제로 NPC 를 따라
움직이는지** — 연속 스캔에서 검출점이 트랙을 따라 이동하면 NPC, 자차 주변에 붙어 있으면 자기
반사다.

**주의**: `publishCommand` 의 인자 순서는 `(speed, acceleration, steering)` 이다.
`(-2.0F, 1.0, ...)` 는 음수 목표속도 + 양수 가속으로 바로 위 주석과 일치한다 —
`(accel, speed)` 로 착각하면 부호 버그로 오진한다.

**교훈**: bag 의 토픽 목록으로 실행 조건을 추정하지 말 것. `output/<run>/awsim.log` 첫 줄에
어느 `*.sh` 로 띄웠는지가 찍힌다 — 그것이 정본이다.

## 재시험 금지 (전부 실측)

라인: 우리 336.1 대 KSK 이식 352.3 — KSK 라인은 우리 제어기에서 118.64 초. 그들의 반전이 더
급하다(dkappa/ds 0.053 대 0.038). 우리 라인이 더 낫다.
조향 변화율: 0.25 / 0.50 / 0.70 / 1.40 전부 0.35 보다 나쁘다. low-pass 게인이 둘 다 1.0 이라
하류 감쇠가 없다.
구간 속도: 상한 올리기 3 회 실패, 하한 22->26 실패, s5 30->26 무차별.
Q / QN / N / wp_id_offset: 무차별 또는 악화. R[1] 은 튜닝 대상이 아니다 — 입력 기준 ur 이 0 이라
R[1]>0 은 조향 자체에 벌점(측정: 완주 0 랩, 복구 112·115 회).
3 대 동시로 1 초짜리 파라미터 차이 측정: 불가능. 이 16 코어 박스에서 챔피언 자신의 편차가 103 초.
랩 수 같은 큰 효과만 3 대로 잴 수 있다.
총시간 평균의 세대 간 비교: 불가능. 같은 설정이 313.6 과 348.4 를 낸다. 같은 레이스 안의 순위만
유효하다.

---

# 트랙 2 — AI 트랙 (End-to-End)

## 제출물

Google Form 으로 두 개: **개발 내용 슬라이드**(용량 제한 주의) + **주행 영상**(공개될 수 있음).
결과는 이메일 통보, 우수 팀 영상은 공개.

## 슬라이드 필수 5 항목

1. 주행 데이터 첨부
2. **다른 차량에 대한 회피 및 정지 고려** <- 트랙 1 의 NPC 문제와 같은 문제
3. 모델 아키텍처 설명
4. 학습 데이터 및 평가 내용
5. 팀의 독창성 및 주력 포인트

## 강화학습은 필요 없다

요구는 "End-to-End AI 모델"이고 TinyLidarNet 은 **지도학습(behavioural cloning)**이다. 공식 README
가 명시한다: `LaserScan`(입력) + `AckermannControlCommand`(교사) 를 담은 rosbag -> extract -> train.
**우리 MPC 가 곧 교사**이고 46 초로 잘 도니 라벨 데이터를 무한히 만들 수 있다.
`ml_workspace/reinforcement_learning/` 은 옵션이지 필수가 아니다.

## 이미 있는 것

```
tiny_lidar_net/   1D CNN (conv 5 + fc 4), input_dim 750, output [accel, steer]
                  train.py / extract_data_from_bag.py / convert_weight.py 완비
pilot_net/        이미지 기반 별도 파이프라인
ckpt/tinylidarnet_weights.npy   602 KB, 2026-07-28 학습됨
                  백업 bak_pre_hairpin_finetune / bak_pre_round4 -> 파인튜닝 이력 있음
control_method: tiny_lidar_net | pilot_net | rl_train 모두 런치 연결됨
```

`input_dim: 750` 이 실측 스캔 빔 수와 정확히 일치한다. 이 시뮬레이터로 학습된 모델이다.

## ★ 2026-08-02 — "가속 학습이 안 됐다"의 진짜 원인 (측정됨) ★

차속 입력 부재가 원인이라는 위 추측은 **틀렸다.** 새 데이터 33,429 샘플로 베이스라인을 학습해
채널별로 재보면:

| 채널 | SmoothL1 | 비고 |
|---|---:|---|
| steer | **0.0012** | MAE **0.037 rad**(2.1°). 평균 예측(0.0207) 대비 **17 배** 우수 — 조향 BC 는 잘 된다 |
| accel | **0.3007** | 도달 가능 하한 0.0727 의 **4 배**. 출력이 +0.98 에 고정(std 0.035 vs 타겟 std 0.948) |

즉 val 정체값 0.3037 = accel 0.3007 + steer 0.0012. **손실의 99.6% 가 accel 채널**이다.

원인 두 겹:

1. **출력 범위 불일치.** `lib/model.py` 의 head 는 `torch.tanh` 이라 [-1,1] 이고, 컨트롤러는
   그 값을 `cmd.longitudinal.acceleration` 으로 **물리 단위 그대로** 발행한다
   (`tiny_lidar_net_controller_core.py:99`, `..._node.py:103`). 그런데 MPC 교사는
   `a_max=1.35 / a_min=-1.6` 을 명령하므로 **타겟의 97.6% 가 [-1,1] 밖**이다. 원천적으로 도달 불가.
2. **타겟이 사실상 상수.** 86.4% 가 정확히 +1.35, 10.2% 가 정확히 -1.6 — MPC 는 bang-bang 으로
   포화 명령을 낸다. 도달 가능했더라도 accel 채널에 배울 내용이 거의 없고, 손실 최소해는
   "항상 최대 가속"이다. 모델은 정확히 그 해로 붕괴했고 10.2% 의 제동은 무시된다.

`WeightedSmoothL1Loss` 가 두 채널 평균의 단순 합(가중치 각 1.0)이므로, 배울 수 없는 accel 이
gradient 의 대부분을 차지한다.

### 2026-08-02 — scratch 학습 가중치 주행 테스트: **0 랩 / 480 초, 기각**

위 베이스라인(steer MAE 0.037 rad)을 배포 형식으로 변환해 넣고 solo6lidar 로 주행:
**lap_count 0 / 6, finished=False.** 기존 가중치(55~65 초)로 원복함
(`ckpt/tinylidarnet_weights.npy.bak_pre_bc20260802.npy` 가 그 백업).

변수는 가중치 하나뿐이었다. `control_mode` 는 원래부터 `fixed`(=accel 무시, 조향만 모델)이고
`acceleration` 0.6 도 건드리지 않았다. **전처리 불일치는 아니다** — 배포
(`_preprocess_ranges`: NaN→0, inf→30, clip[0,30], 리사이즈, /30)와 학습
(`clean_scan_array` + `lib/data.py` 의 `clip(0,30)/30`)이 정확히 일치함을 코드로 확인했다.

**유력한 원인(미측정)**: covariate shift. open-loop MAE 0.037 rad 과 closed-loop 0 랩이
동시에 성립하는 전형적인 behavioural cloning 실패다. 학습 데이터는 전부 **교사 분포**
(MPC 가 레이싱 라인 위를 완벽히 도는 상태)이고, 학습자가 조금 벗어난 뒤 만나는 상태는
데이터에 없다. 오차가 누적되면 미학습 영역으로 들어가 출력이 무의미해진다.

그리고 이것이 기존 가중치가 왜 작동하는지도 설명한다 — 그쪽은
**수집→파인튜닝→실측→재수집 3 라운드**를 거쳤고, 그것이 곧 on-policy 보정(DAgger 계열)이다.

**결론: scratch 학습이 아니라 작동하는 가중치에서 파인튜닝할 것.** 새 데이터 33,429 샘플은
그 용도로 쓴다. 이 레포에서 실제로 효과를 낸 유일한 방식이 그것이다.

### TLN 모드에서 stuck_recovery 는 구조적으로 발동하지 않는다 (2026-08-02)

0 랩 실행에 stuck-recovery 이벤트가 **0 건**이었고, 이유가 두 겹이다:

1. `stuck_recovery_controller.cpp:127` 이 `command.longitudinal.speed < 1.0` 이면 조기 리턴한다.
   TLN 노드는 `longitudinal.acceleration` 과 `lateral.steering_tire_angle` 만 채우고
   `longitudinal.speed` 는 건드리지 않으므로 항상 0.0 < 1.0 이다.
2. `reference.launch.xml` 이 tiny_lidar_net 포함 시 `control_cmd_topic` 을 덮어쓰지 않아
   TLN 이 `/control/command/control_cmd` 로 **직접** 발행한다 — 복구 노드를 아예 우회한다.

즉 AI 트랙에는 지금 어떤 교착 안전망도 없다. MPC 트랙의 안전망을 TLN 에 붙이려면 두 곳을 다
고쳐야 한다.

**다음 후보(미검증)**: 코드에 이미 `control_mode: 'fixed'`(고정 가속 + 모델 조향)가 있다.
교사가 86% 최대 가속이니 `fixed` + `acceleration=1.0`(컨트롤러 상한)은 교사의 지배적 행동을
거의 그대로 복제하면서, 잘 학습된 조향만 모델에 맡긴다. 이전에 "`ai` 가 `fixed` 보다 훨씬 빠르다"고
기록된 비교는 `acceleration` 기본값이 **0.1** 일 때의 것이라 이 조건과 다르다.
`accel_weight` 를 낮추는 것도 싼 레버다. 어느 쪽이든 **주행 테스트로 확인할 것** — val loss 가
아니라 랩 완주가 이 트랙의 판정 기준이다.

## 갭 두 개 (아래 1 번은 위 측정으로 재해석됨)

1. **입력이 라이다 단독.** 요구는 2D LiDAR + 이미지 + 차속. 차속 데이터는 이제 있다
   (`speeds.npy`). 다만 위에서 보듯 가속 학습 실패의 원인은 차속 부재가 아니었다 — 요구사항
   충족 목적으로는 여전히 붙여야 하지만, 그것만으로 accel 이 학습되리라 기대하지 말 것.
2. **학습 데이터가 없다.** `dataset/`, `checkpoints/`, `rawdata/` 전부 비어 있다(gitignore).
   가중치만 남고 데이터는 사라졌다.

## 목표

| | 현재 | 목표 |
|---|---|---|
| 학습 rosbag | 0 | **20 랩 이상** (MPC 교사, 라이다 on) |
| 모델 입력 | 라이다만 | **라이다 + 이미지 + 차속** |
| accel 학습 | 실패 기록 | 차속 입력으로 재시도 |
| 완주 (AI 제어) | 미측정 | 6 랩 완주 1 회 이상 |
| 슬라이드 | 없음 | 5 항목 골격 |

## 데이터는 레이스에서 공짜로 나온다

`battle3.sh` 가 `--lidar cpu` 로 돌고 있고 실측 20 Hz 다. 레이스에 rosbag 기록만 켜면 트랙 1 의
스크리닝이 곧 트랙 2 의 학습 데이터 수집이 된다. 두 트랙이 같은 실행을 공유한다.

### 2026-08-01 정정 — 레이스 bag 은 학습 데이터로 거의 못 쓴다

`output/npc-with-{1,2,3}/bag` 에 이미 549 초짜리 3 개가 있었다(스캔 32,932 = 27.4 분). 그러나
**실측 속도 0.5 m/s 미만이 68%** 다. 위의 "랩 3 을 t+164 초에 끝내고 391 초를 서 있었다" 가 데이터
전체의 성격이다. 이 프레임을 복제하면 네트워크는 **서 있기를 배운다.**

`--min-speed 2.0` 으로 실제 주행만 남기면 **5,483 샘플 = 4.6 분**. 20 랩(약 15.3 분) 목표에
**10.8 분 부족**하다. 즉 레이스 bag 은 회피 행동 샘플로만 값이 있고, 주행 라벨은 `solo6lidar.sh`
(6 랩 clean, 막을 NPC 가 없음, 실행당 약 4.6 분)로 3 회 더 받아야 채워진다.

### 2026-08-02 — solo 수집 1회로 데이터 목표 달성, 새 기본값은 페이스 중립

`tools/collect_teacher_data.sh`(경계가 있는 solo6lidar 수집 러너)로 1회 실행:
**14,093 샘플 = 11.7 분, `--min-speed 2.0` 통과율 100%**. 레이스 bag 이 83% 정지였던 것과
대비되며, "solo 는 막을 NPC 가 없으니 전 프레임이 유효 라벨"이라는 가정이 확인됐다.
누적 **19,576 샘플 = 16.3 분**으로 20 랩(약 15.3 분) 목표 **달성**.

스캔 750 빔 전부 유한, 0-range 빔 없음, 조향 ±0.495 rad 로 포화 0 건(한계 0.64).

같은 실행이 트랙 1 측정도 준다 — **새 기본값(TICKS 40 / FUNNEL_RATE 0.15) 솔로 6 랩**:

```
47.53 / 46.27 / 45.96 / 46.12 / 46.36 / 46.46   평균 46.45  최고 45.96  총 278.68s  6/6
```

문서화된 기준선(평균 46.53 / 최고 46.05)과 −0.08s 차이로 **사실상 동일**. 즉 funnel +
antideadlock 은 **솔로 페이스를 깎지 않고** 완주율을 산다 — 이게 이 변경의 전제였고 이제 측정됐다.
(총 278.68s 를 KSK 의 282.155s 와 직접 비교하지 말 것: 이쪽은 `--handicap off` 솔로이고
저쪽은 핸디캡·트래픽이 있는 배틀이다. 기준선 46.53s 와의 비교만 apples-to-apples 다.)

수집 러너에서 배운 것(같은 실수 반복 금지):

- **AWSIM 준비 판정은 컨테이너 안 `Player.log` 의 `state → Start` 로 하라.** `ros2 topic list`
  는 구독자만 있어도 토픽을 표시해서 AWSIM 부팅 중에 5 초 만에 오판한다(8KB 빈 bag).
  `ros2 topic echo` 는 `docker compose exec` 셸이 노드들의 RMW/cyclonedds 환경을 안 물려받아
  건강한 AWSIM 에서도 240 초 타임아웃 난다. `awsim.log`(stdout)에는 Unity 부팅 정보가 없다.
- **프로브의 stderr 를 버리지 말 것.** 2 차 실패의 원인이 안 보인 이유가 그것이다.
- `result-summary.json` 은 `LOG_DIR` 이 아니라 **`aichallenge/result-summary.json`** 에 쓰인다.
  세션 간 살아남으므로 반드시 시작 시점 mtime 을 기준선으로 잡아야 한다.
- AWSIM 은 6 랩 채점 후에도 차를 계속 달리게 한다(283 초에 완주, 705 초까지 주행). 교사 데이터엔
  그 초과 주행이 공짜 라벨이라 기본값은 계속 돌리는 것(`STOP_AT_RACE_END=0`)이고, 랩이 곧 측정인
  트랙 1 스크리닝에서는 켜라.

부수로 확인된 것:

- **명령 속도는 교착 판별에 못 쓴다.** 명령이 0 인 프레임은 7.1% 인데 실제 정지는 67.8% 다.
  컨트롤러는 낼 수 없는 속도를 계속 명령한다. 판별은 `velocity_status` 로만 된다 —
  그리고 그것이 위 "갭 1" 의 그 입력이다. 하나로 둘이 해결된다.
- 필터를 걸면 조향 범위가 ±0.52 rad 로 들어온다(차량 한계 0.64). 즉 앞서 본 포화 조향 0.9 rad
  은 전부 교착·이탈 프레임이었다.
- 세 bag 은 `metadata.yaml` 이 없었다(clean shutdown 없이 죽음). db3 에 topics·QoS·ros_distro·
  타임스탬프가 다 있어서 `tools/rebuild_bag_metadata.py` 로 복원했다. 이제 `ros2 bag play` 도 된다.
- `extract_data_from_bag.py` 버그 두 개를 고쳤다. (1) rosbags 기본 typestore 에 Autoware 타입이
  없어 db3 bag 의 control_cmd 가 **전량 조용히 스킵**되고 "insufficient data" 로 끝났다
  (mcap 은 스키마 내장이라 우연히 동작). IDL 을 `tiny_lidar_net/msgdefs/` 에 넣고 등록한다.
  (2) 출력 디렉토리 이름이 bag 의 basename 뿐이라, 세 bag 이 모두 `bag` 이므로 **서로 덮어써서
  3 개 중 1 개분만 남았다.** 그래도 "성공" 으로 보고됐다.


## 2026-08-02 — 라이다 장애물 검출 전면 철회: 점수 시나리오에 NPC도 라이다도 없다

`aichallenge/simulator_scripts/` 를 나란히 읽고 나서야 전제가 틀렸다는 것을 알았다.

| | eval.sh | online3.sh | parallel.sh | npc1.sh |
|---|---|---|---|---|
| 역할 | 공식 평가 | 3 대 배틀 | 3 대 배틀 | **로컬 개발용** |
| `--npcs` | **0** | **0** | **0** | 1 |
| `--lidar` | **off** | **off** | **off** | cpu |
| `--vehicles` | 1 | 3 | 3 | 1 |
| `--handicap` | off | **on** | **on** | off |
| `--collisions` | on | on | off | on |
| `--timeout` | 600 | 600 | 600 | 480 |
| `--start-mode` | sync | sync | sync | count |

**점수가 나는 어떤 시나리오에도 NPC 가 없고, 라이다가 전부 꺼져 있다.** 경쟁 상대는 NPC 가 아니라
다른 두 제출물이다(`--vehicles 3`). `npc1.sh` 는 개발용이다.

따라서:

* **`lidar_obstacle_publisher.py` 는 배틀에서 실행될 수 없다.** 라이다가 꺼져 있으니
  `/sensing/lidar/scan` 이 없다. 노드는 남기되 런치 체인에 넣지 않는다(이미 안 넣었다).
* 노드 docstring 의 "NPC 가 149 s 에 한 바퀴이므로 480 s 를 따라가면 3.2 랩" 계산은 npc1.sh 에서
  나온 것으로, **점수와 무관하다**.
* 오늘 회피 비용 측면에서 얻은 것(48.4 s → 16.3 s/이벤트)은 유효하지만, 회피가 켜질 이유가
  사라졌다. `USE_OBSTACLE_AVOIDANCE` 는 기본 off 로 둔다.
* 대조군을 찾다가 발견한 `solo6lidar.sh`(0 NPC + lidar cpu) 도 이제 쓸 데가 없다.

**재시험 금지에 추가**: 라이다 기반 장애물 검출 일체. 점수 시나리오에 센서가 없다.

### 대신 봐야 할 것 — 배틀과 eval 의 실제 차이

검증된 6/6(278.23 s, avg 46.37, recovery 0) 은 `eval.sh` 조건이다. 배틀은 세 가지가 다르다:

1. **`--vehicles 3`** — 다른 두 대가 물리적으로 같은 트랙에 있다. `online3.sh` 는
   `--collisions on`. 스타트가 `sync` 라 세 대가 동시에 출발한다. Track 1 목표가
   "deadlock 0" 인데, 실제 deadlock 위험은 벽이 아니라 **다른 차와의 접촉**이다.
2. **`--handicap on`** — eval 과 npc1 은 둘 다 off 였다. 무엇을 하는지 확인한 적이 없다.
3. **`--timeout 600`** — 480 이 아니다. 278 s 짜리 주행에는 여유가 크다.

라이다도 카메라도 꺼져 있으므로 다른 차를 볼 수 있는 유일한 채널은 v2x 다. GOAL.md 에 이미
"NPC 는 v2x 에 안 나온다" 는 측정이 있지만, **다른 차량(vehicles 3)이 v2x 에 나오는지는 별개
문제이고 측정한 적이 없다.** 이것이 다음 확인 대상이다.


## 2026-08-02 (정정) — 권위는 셸 스크립트가 아니라 RaceConfig yaml 이다

위 항목은 `simulator_scripts/*.sh` 만 보고 썼다. 권위 있는 것은
`aichallenge/simulator/AWSIM/AWSIM_Data/StreamingAssets/RaceConfig/eval-{1,2,3,4}p.yaml`
이고, 두 가지가 다르다.

| | 셸 스크립트 | **eval-3p.yaml (공식)** |
|---|---|---|
| `npcs` | 0 | **0** — 일치 |
| `lidar` | off | **on** — 반대 |
| `collisions` | on | **off** — 반대 |
| `handicap` | off | on |
| `timeout` / `laps` / `start-mode` | 600 / 6 / sync | 600 / 6 / sync |

**결론은 유지되지만 이유가 바뀐다.** 라이다 장애물 검출이 무의미한 것은 센서가 꺼져서가
아니라(공식 설정에선 켜져 있다) **피할 대상이 없어서**다: `npcs: 0` 이고 `collisions: off` 라
다른 두 대는 서로를 통과한다. `tools/evolve.py` 의 docstring 이 이미 같은 말을 하고 있었다 —
"With collisions off the cars pass through each other and each is effectively a solo run,
which is what the official 3-vehicle config does as well".

따라서:

* 재시험 금지 항목은 유지: 라이다 기반 장애물 검출. 단 사유는 "충돌이 꺼져 있어 회피 대상이
  없음".
* 같은 이유로 **v2x 회피도 무의미하다.** `USE_OBSTACLE_AVOIDANCE` 에서 v2x 추적을 떼어내
  항상 켜는 변경을 만들었다가 되돌렸다. 검증된 제출물에 이득 없는 복잡도를 남기지 않는다.
* 위 항목의 "3 대가 물리적으로 같은 트랙에 있으니 접촉 deadlock 이 위험" 이라는 서술은
  **틀렸다**. 충돌이 꺼져 있다.

**공식 배틀 조건 확정**: 3 대, NPC 0, 충돌 off, handicap on, 라이다 on, 6 랩, 600 s, sync 출발.
사실상 솔로 주행이다. 남는 것은 **완주 우선, 그다음 페이스** — `evolve.py` 가 이미
lexicographic 으로 최적화하고 있는 바로 그것이다. 회피/인식이 아니라 이 축으로 돌아간다.


## 2026-08-02 — 채점 조건(라이다 on)은 검증 조건보다 느리다 (예비, 팔당 2 런)

검증된 6/6 은 `eval.sh`(`--lidar off`) 에서 잰 것인데 공식 `eval-*p.yaml` 은 `lidar: "on"` 이다.
`solo6.sh` 와 `solo6lidar.sh` 는 이 플래그 하나만 다르므로 깨끗한 쌍이다. A/B/A/B 로 4 런
(`tools/lidar_load_ab.py`, 결과는 `tools/lidar_load_ab.jsonl`).

| | 정상 랩 n | 중앙값 | 평균 | sd | recovery |
|---|---:|---:|---:|---:|---:|
| lidar off | 9 | 46.21 | 46.15 | 0.29 | **0** |
| lidar **on** | 8 | **46.39** | **46.49** | 0.30 | **4** |

(첫 랩은 스탠딩 스타트를 포함하므로 제외.)

랩당 평균 +0.34 s, 중앙값 +0.18 s. 더 무거운 것은 **recovery 0 → 4** 다. 완주가 페이스를
이기므로 채점 조건에서만 나오는 recovery 가 랩타임보다 중요하다.

**예비 결과로만 취급한다**: 팔당 런이 2 개뿐이고, 이 프로젝트에서 런 단위 비교는 여러 번
틀렸다. recovery 4 건도 전부 한 런(solo6lidar-03)에서 나왔다. 2 라운드를 더 돌려 팔당 4 런으로
만드는 중이다.

**기전은 아직 미확정.** CPU 경합 가설을 재려고 컨테이너별 사용률을 샘플링 중인데, 첫 관측은
autoware ~170%, AWSIM ~46%, 코어 16 개(1600% 가용) 라 **박스 전체로는 여유가 크다**. 전체
포화가 아니라면 단일 스레드 제어 루프의 지연이거나, 아니면 노이즈다. 라이다 on 쪽 사용률이
나오면 갈린다.

주의: `WALL=620` 은 너무 좁았다. AWSIM 기동 시간 편차 때문에 6 랩 278 s 가 창에 안 들어가는
경우가 있어(런 00 은 319 s 에 끝났는데 런 02 는 633 s 쓰고 5 랩) `finished` 가 차가 아니라
기동 운을 재고 있었다. 900 으로 올렸다. 랩타임 자체는 영향받지 않는다.


## 2026-08-02 (정정) — recovery 신호 철회, CPU 경합 가설 기각

바로 위 예비 결과의 **"recovery 0 → 4, 채점 조건에서만 발생" 은 철회한다.** 라이다 OFF 인
solo6-04 가 recovery 6 건을 냈다. 팔별 누적은 off 6 건 / on 4 건으로, 라이다와 무관하다.
recovery 는 두 조건 모두에서 가끔 나온다 — 이것 자체가 "deadlock 0" 목표에 대한 별개의
사실이다.

**CPU 경합 가설도 기각.** 런 중 컨테이너별 사용률(20 s 간격 샘플, `cpu_samples.tsv`):

| 런 | autoware | AWSIM sim |
|---|---:|---:|
| solo6-04 (lidar off) | 192.1% | 46.1% |
| solo6lidar-05 (lidar on) | 190.6% | **50.1%** (최대 102.1%) |

라이다는 sim 에 중앙값 4%p 를 더할 뿐이고 autoware 는 그대로다. 코어 16 개(1600% 가용)에
총 240% 이므로 박스는 포화 근처도 아니다. 제어 루프가 CPU 에 굶는다는 설명은 성립하지 않는다.

**남는 사실은 랩타임 차이 하나뿐이고, 그건 유지된다** (off 3 런 / on 2 런):

| | 정상 랩 n | 중앙값 | 평균 |
|---|---:|---:|---:|
| lidar off | 13 | 46.10 | 46.14 |
| lidar on | 8 | 46.40 | 46.49 |

랩당 +0.35 s (0.7%). 6 랩이면 약 2 s. 600 s 타임아웃에 278 s 주행이므로 **완주에는 영향이
없고**, 페이스가 타이브레이크로 작동할 때만 의미가 있다. sim 사용률이 46% 에서 순간 102% 로
튀는 것으로 보아 라이다의 프레임 단위 비용이 AWSIM 스텝 타이밍을 흔드는 쪽이 남은 후보지만,
확인하지 않았고 실익도 크지 않다.

**결론: 라이다 on 은 랩당 0.35 s 를 먹지만 완주를 위협하지 않는다. 이 축은 여기서 닫는다.**


## 2026-08-02 — 완주 판정이 틀려 있었다: 6 랩 완주가 5 랩 실패로 기록돼 왔다

라이다 A/B 의 `finished=False` 를 파고들다 하네스보다 큰 문제를 찾았다.

MPC 의 `"Lap N completed!"` 로그는 **마지막 랩에서 orchestrator 종료와 경합하고 대개 진다.**
solo6 세 런 모두 랩 5 에서 orchestrator 의 `latest link updated` 까지 간격이 정확히 46.0 s —
한 랩 —였는데, 랩 6 을 로그한 것은 solo6-00 뿐이고 그마저 종료와 **같은 초**에 찍혔다.

레이스 종료 시각으로 다시 채점하면:

| 런 | 로그된 랩 | 실제 완주 | 경과 |
|---|---:|---:|---:|
| solo6-00 | 6 | 6 | 278.7 s |
| solo6-02 | 5 | **6** | 278.3 s |
| solo6-04 | 5 | **6** | 277.5 s |
| solo6lidar-01 | 5 | **6** | 279.4 s |
| solo6lidar-03 | 5 | **6** | 280.0 s |

**다섯 런 전부 6 랩을 완주했다.** 480 s 타임아웃인데 278~280 s 에 끝났으니 완주 외에 끝날
방법이 없다.

**이건 이 프로젝트 전반에 영향을 준다.** `evolve.py` 의 `parse_laps` 가 같은 메시지를 세고,
`total_6()` 은 빠진 랩마다 "관측된 최악 랩" 을 벌점으로 매긴다. 즉 **진짜 6 랩 완주가 실패로
채점돼 왔고, 파라미터 탐색은 그 벌점을 신호로 읽어 왔다.** `evolve_log.jsonl` 에 기록된
completion 수치는 이 관점에서 다시 봐야 한다.

`tools/lidar_load_ab.py: race_outcome()` 이 올바른 판정을 한다: 마지막 랩과 종료 사이 간격이
한 랩 길이면 로그되지 않은 랩을 더하고, 타임아웃 한참 전에 끝났으면 완주로 본다.

**recovery 카운트도 같은 뿌리에서 오염돼 있었다.** 1 차 배치의 stuck 이벤트는 전부 orchestrator
종료 **이후** — 끝난 레이스에서 주차된 차에 대해, 종료 8 분 뒤에 — 발생했다. 즉 건수는 하네스가
끝난 런을 얼마나 오래 방치했는지를 잰 것이다. solo6-00 이 0 건인 이유는 유일하게 조기 종료했기
때문이다. 이제 종료 신호에서 루프를 끊어 유휴 시간을 없앴다.


### evolve.py 에 반영: 과거 측정 24 건 중 8 건이 판정이 뒤집힌다

`race_outcome()` / `scored_total()` 을 `evolve.py` 에 넣고 `run_screen`(활성 경로)과
`run_race`(현재 미사용)에 연결했다. `fitness()` 의 1 순위 키가 `finished` 이므로 이 오류는
페이스가 아니라 **정렬 자체**를 뒤집는다.

과거 `output/evolve-*` 로그 전부를 다시 채점한 결과:

| 런 | 로그된 랩 | 실제 완주 | 경과 | 판정 |
|---|---:|---:|---:|---|
| evolve-base | 5 | 6 | 284.3 s | 미완주 → **완주** |
| evolve-base-champ | 5 | 6 | 283.4 s | 미완주 → **완주** |
| evolve-g00/d2 | 5 | 6 | 453.1 s | 미완주 → **완주** |
| evolve-g00-verify/d1 | 5 | 6 | 336.9 s | 미완주 → **완주** |
| evolve-g01/d2 | 5 | 6 | 296.6 s | 미완주 → **완주** |
| evolve-g01-confirm/d1 | 5 | 6 | 314.0 s | 미완주 → **완주** |
| evolve-g03/d3 | 5 | 6 | 443.6 s | 미완주 → **완주** |
| evolve-g03-confirm/d3 | 5 | 6 | 347.4 s | 미완주 → **완주** |

**24 건 중 8 건.** 그리고 뒤집힌 것에 `evolve-base` 와 `evolve-base-champ` 가 들어 있다 —
**기준선 자체가 미완주로 기록돼 있었다.** 세대별 승패 판정은 이 기준으로 다시 읽어야 한다.

타임아웃으로 끝난 런은 안전하다: `finished` 는 `elapsed < timeout - 20` 도 요구하므로,
480 s 에 종료된 런은 마지막 간격이 한 랩처럼 보여도 완주로 승격되지 않는다.


## 2026-08-02 — 세대 재채점 결과: 승자 변화 0. 그리고 진짜 표적을 찾았다 — wp 284-286

**앞 항목의 심각도 서술을 정정한다.** 수정된 완주 판정으로 `evolve_log.jsonl` 의 6 개
스크린/컨펌 전부를 다시 채점했다. DNF → 완주로 바뀐 항목은 4 개지만 **승자가 바뀐 세대는
하나도 없다.** 처음에 gen 1 에서 두 건이 뒤집혔는데, 그건 내가 `scored_total` 에 `elapsed`
(orchestrator 종료 지연 포함)를 쓰면서 넣은 노이즈였다 — 0.8 s 차이로 붙어 있던 두 후보를
0.3 s 로 뒤집은 것이라 결과가 아니라 동전 던지기다. 6 랩이 모두 로그된 경우엔 랩 합을 쓰도록
고쳤고, 그러자 승자 변화는 0 이 됐다.

즉 **파라미터 탐색의 결론은 왜곡되지 않았다.** 기록만 틀렸다.

### 채점 조건 8 레이스의 deadlock 실태

| | |
|---|---|
| 레이스 중 deadlock | **5 이벤트, 8 레이스 중 1 레이스** (solo6lidar-05) |
| 레이스 종료 후 | 32 이벤트 (주행과 무관) |

solo6lidar-05 는 랩 5 까지 정상(47.79/46.66/46.03/46.10/45.97)이다가 t=+265.8 s 에
**waypoint 284** 에서 멈춰 `attempt=4 deep_escape` 까지 갔는데도 못 빠져나오고 5 랩으로 끝났다.
**8 레이스 중 1 번, 이 방식으로 랩을 잃는다.**

### 정체 지점은 무작위가 아니다

`deadlock: commanding ... (wp=N)` 의 N 을 전 런에서 모으면:

| waypoint | 발생 런 수 |
|---|---:|
| 284 | 2 |
| **285** | **4** |
| **286** | **4** |
| 295 / 299 | 1 / 1 |
| 305 / 306 / 308 | 2 / 1 / 1 |
| 328 | 1 |

멈춘 적이 있는 5 개 런이 **전부** 284-286 을 지난다. 전체 정체 구간은 351 개 중 284-328,
즉 랩의 마지막 20% 다. 진짜 in-race deadlock 도 같은 자리에서 났다.

**Track 1 의 "deadlock 0" 목표에 대한 구체적 표적이 생겼다: waypoint 284-286.** 다음은 그
지점의 곡률/코리더 폭/기준속도를 보고 왜 MPC 가 거기서 쐐기에 걸리는지 확인하는 것이다
(`tools/wedge_anatomy.py`, `tools/corridor_compare.py`).

주의: 284-286 사건 대부분은 레이스 종료 후 기록이라 원인이 in-race 와 같다고 단정할 수 없다.
확정된 것은 (a) in-race deadlock 1 건이 거기서 났다는 것과 (b) 이 차가 멈추는 자리가 거기로
몰린다는 것이다.


## 2026-08-02 — wp 284-286: 기하도 속도도 무죄. 원인 미상, 계측을 심었다

정체 지점의 기하를 재고, 두 가지 명백한 가설을 모두 기각했다.

| 구간 | kappa 중앙값 | 폭 중앙값 | 요구 ay |
|---|---:|---:|---:|
| 랩 전체 | 0.080 | 5.573 m | 4.75 m/s² |
| **284-286** | **0.169** | **5.004 m** | **6.1-6.4 m/s²** |
| 랩 최악 | 0.228 (wp 117) | 4.258 m (wp 201) | 15.83 m/s² (wp 117) |

* **곡률**: 284-286 은 랩 중앙값의 2 배지만, wp 117-121 이 더 급하다(0.228). 거기선 안 멈춘다.
* **폭**: 5.00 m 로 중앙값(5.57)보다 좁지만, 가장 좁은 곳은 wp 201(4.258)이다. 거기서도 안 멈춘다.
* **횡부하**: s6 구간(wp 265-289)은 22 km/h = 6.11 m/s 이므로 ay = 6.1-6.4 m/s². **351 개 중
  104-121 위** — 완전히 중간이다. 랩 최대는 15.83, 이 차량의 측정 한계는 18.60.

**"코너가 어려워서" 가 아니다.** 이 축은 닫는다.

로그에는 진단할 것이 없다 — `capture/rosbag are disabled` 이고, 정체 직전 40 줄에 pose 도 e_y 도
솔버 상태도 없다. 1/8 확률 사건을 잡으려고 몇 시간 rosbag 을 기록하는 것은, 이 박스가 이미
과열로 죽은 적 있는 드라이브에 지속 쓰기 부하를 거는 일이라 하지 않는다.

**대신 `mpc_controller` 에 정체 해부용 링버퍼를 넣었다** (30 s / 1200 틱, 메모리에만). deadlock
브레이커가 처음 발동할 때 한 번만 덤프하며, 매 틱의 `wp / e_y / e_psi / v / cmd_v / steer` 와
함께 다음을 판정한다:

> 최대 |e_y| 가 얼마였는지, 그리고 **1.94 m 의 QP 실행불능 경계를 넘었는지**.

브레이커 위 주석은 "|e_y| > max_width - width/sqrt(2) = 1.94 m 면 QP 에 실행 가능점이 없고
0 명령이 나온다" 고 기전을 단언하지만, **그것이 실제로 일어나는지 확인된 적은 없다.** 덤프가
넘지 않았다고 말하면 그 설명 자체가 틀린 것이다. 다음 정체에서 답이 나온다.


## 2026-08-02 — 정체는 "QP 실행불능" 이 아니다: 명령은 살아 있는데 차가 안 움직인다

계측을 단 뒤 8 레이스를 더 돌렸다. 덤프는 안 찍혔는데, **안 찍힌 것 자체가 답이었다.**

| 런 | 랩타임 | recovery | `deadlock: commanding` |
|---|---|---:|---:|
| solo6-08 | 47.30 46.17 46.26 45.91 46.19 | 0 | 0 |
| **solo6lidar-09** | 47.70 46.44 45.96 **67.18** 46.27 | 1 | **0** |
| solo6-10 | 47.62 45.90 45.80 46.33 45.92 | 3 | **0** |
| solo6lidar-11 | 47.92 46.52 46.25 46.20 46.17 | 0 | 0 |
| solo6-12 | 47.53 45.91 46.11 46.24 45.97 | 0 | 0 |

solo6lidar-09 는 랩 3 뒤 11 초 지점에서 `velocity=0.001` 로 멈췄고, 랩 4 를 67.18 s 에 끝냈다
(정상 46 s 대비 **+21 s**). 그런데 **MPC 의 deadlock 브레이커는 이 런에서 한 번도 발동하지
않았다.**

브레이커 조건은 `|v| < 0.15` **그리고** `u[0] < 1.0` 이 40 틱(1 s) 연속이다. 차는 10 초 넘게
0.001 m/s 였으니 첫 조건은 400 틱 넘게 성립했다. 그런데 발동하지 않았다는 것은
**`u[0]` 이 1.0 이상이었다**는 뜻이다 (최소한, 0 근처로 40 틱 연속 내려간 적이 없다).

즉 코드 주석이 단언해 온 기전 —

> "|e_y| 가 1.94 m 를 넘으면 QP 에 실행 가능점이 없고, 실행불능 해는 **0 명령**을 낸다"

— 은 **이 사건을 설명하지 못한다.** 명령은 살아 있었다. 움직이라고 시키는데 안 움직이는 차는
QP 에 굶은 차가 아니라 **물리적으로 막힌 차**다. 벽 접촉 쪽이 유력하다.

**계측을 잘못된 트리거에 달았던 것도 이 때문이다.** 덤프를 브레이커에 걸어 뒀는데, 브레이커는
정작 이 실패 모드를 못 본다. `|v| < 0.15` 만으로 트리거하도록 넓혔고, 덤프 헤더가 정지 시점의
`cmd_v` 를 보고해 두 모드를 직접 가른다:

* `cmd_v >= 1.0` → 물리적 차단
* `cmd_v ~ 0` → 실행불능 QP (주석이 말하는 그 경우)

주의: 여기서 확정된 것은 "이 한 사건이 0 명령 때문이 아니다" 이지, "QP 실행불능은 절대 안
일어난다" 가 아니다. 두 모드가 다 있을 수 있고, 덤프가 매번 어느 쪽인지 말해 줄 것이다.


## 2026-08-02 (정정) — 라이다 랩타임 페널티 철회. n=8 에서 사라진다

팔당 8 런까지 채우자 앞서 두 번 보고한 "+0.35 s/lap" 이 사라졌다.

| | 정상 랩 n | 중앙값 | 평균 | sd |
|---|---:|---:|---:|---:|
| lidar off | 33 | **46.17** | 46.15 | 0.23 |
| lidar on | 32 | **46.20** | 46.91 | **3.71** |

중앙값 차이는 **0.03 s** — 없는 것과 같다. 평균 차이(+0.76)는 전부 solo6lidar-09 의 67.18 s
정체 랩 하나에서 나오고, sd 3.71 대 0.23 이 그것을 그대로 보여준다.

**철회한다**: "라이다 on 은 랩당 0.35 s 를 먹는다". 팔당 2-3 런에서 나온 값이었고 8 런에서
살아남지 못했다. 이 프로젝트에서 런 단위 비교가 틀린 세 번째 사례다. **중앙값을 보되 n 을
채우고 나서 말할 것.**

남는 것: 라이다 on/off 는 페이스에 영향이 없다. 정체 빈도 차이는 recovery 12(off) 대 17(on)
로 갈리지 않는다.

## 2026-08-02 — 첫 덤프는 출발선 인공물. 트리거를 두 번째로 고쳤다

넓힌 트리거로 2 건이 찍혔는데 둘 다 같은 내용이고, 랩 1 보다 **앞선** 로그 라인에서 나왔다:

```
STALL ANATOMY: 40 ticks recorded, stopped at wp=29 with cmd_v=5.47 m/s
  (commanded to move and did not -- physically blocked, not QP-starved);
  max |e_y|=1.566 m at wp=29, NEVER exceeded 1.94 m
  t-1.00s wp=29 e_y=+1.566 e_psi=-2.348 v=-0.000 cmd_v=8.33 steer=+0.005
```

카운트다운 동안 그리드에 정지해 있는 상태다(`e_psi=-2.348 rad = -135 deg`, 아직 정렬 전).
`|v| < 0.15` 만으로는 레이스 시작 전에 걸린다. 게다가 덤프가 런당 1 회라 **출발선 사건이
그 1 회를 소진해서, 뒤에 진짜 정체가 나도 남는 게 없었다.**

고침 두 가지:

1. `|v| > 2.0` 을 한 번이라도 겪은 뒤에만 무장한다.
2. 속도를 회복할 때마다 래치를 풀어 **정체마다 한 번씩** 찍는다(런당 최대 5 회).

덤프 형식 자체는 의도대로 동작했다 — 정지 시점 `cmd_v` 를 보고하고 1.94 m 경계 통과 여부를
판정한다. 출발선 사례조차 `cmd_v=5.47` 로 "명령은 살아 있는데 안 움직인다" 를 정확히 찍었다.


## 2026-08-02 — 정체의 정체가 잡혔다: 언더스티어 → 벽 충돌 → 그 다음이 QP 실행불능

트리거를 고친 뒤 8 레이스에서 **진짜 덤프 3 건**을 잡았다.

| 런 | 정지 wp | cmd_v | max \|e_y\| | 1.94 m 경계 통과 |
|---|---:|---:|---:|---|
| solo6-16 | 285 | 0.00 | **4.399 m** | 정지 78 틱(2.0 s) 전 |
| solo6-16 | 299 | 0.00 | 3.565 m | 정지 788 틱(19.7 s) 전 |
| solo6-18 | 299 | 0.00 | 2.907 m | 정지 128 틱(3.2 s) 전 |

solo6-16 의 첫 사건 마지막 3 초:

```
t-3.00s wp=277  e_y=-0.573  e_psi=+0.271  v=5.775  cmd_v=6.11  steer=+0.019
t-2.50s wp=279  e_y=+0.474  e_psi=+0.473  v=6.075  cmd_v=6.11  steer=-0.088
t-2.00s wp=282  e_y=+1.904  e_psi=+0.648  v=5.814  cmd_v=6.11  steer=+0.045
t-1.50s wp=284  e_y=+3.357  e_psi=+0.629  v=5.913  cmd_v=6.11  steer=-0.083
t-1.00s wp=285  e_y=+4.390  e_psi=+0.652  v=0.042  cmd_v=0.00  steer=+0.000
```

그 앞 12 초는 정상이다 — wp 189-277 내내 |e_y| < 1 m.

**1. 충돌이다.** v 가 5.913 → 0.042 로 0.5 s 만에 떨어진다 = 최소 11.7 m/s². 설정된
`a_min` 은 1.6 m/s² 다. 제동으로는 불가능하다.

**2. 충돌 전에 이미 코스 밖이다.** wp 285 의 코리더는 ub=+3.002 / lb=-2.002 인데 e_y 가
+4.390 — 상한 **1.4 m 바깥**이다. e_y 증가율(1.5 s 에 0.47 → 4.39 m)은 반경 5.9 m
(kappa 0.17) 곡선을 안 따라가고 직진할 때 생기는 이탈량과 일치한다.

**3. QP 실행불능은 원인이 아니라 결과다.** 경계 1.94 m 를 넘긴 뒤에도 **1 초 동안**
cmd_v=6.11 로 정상 명령이 나온다(t-1.50s 에 e_y=3.357 인데 cmd_v=6.11). `cmd_v=0` 은 v 가
0 이 된 **다음** 틱에 나타난다. 브레이커 위 주석은 실행불능을 차를 세우는 원인으로 서술하지만,
실제로는 이미 벽에 박은 차의 뒤따르는 증상이다.

**4. 진짜 원인은 언더스티어다.** e_psi 가 0.27 → 0.47 → 0.65 rad(37°)로 벌어지는 동안 명령
조향은 ±0.09 rad 에 머문다. 이 코너가 요구하는 요레이트는 `kappa·v = 0.17 × 6 = 1.02 rad/s`
인데, 조향 0.09 rad 이 축거 1.087 m 에서 내는 것은 `v·tan(d)/L = 0.44 rad/s` 로 **절반도 안
된다.** 필요한 값은 약 0.18 rad 이고, `steer_rate_max=0.35 rad/s` 로 0.5 s 면 도달 가능하니
레이트 한계 문제가 아니라 **MPC 가 그만큼을 요구하지 않는 것**이다.

### 앞선 추론 정정

"명령은 살아 있는데 안 움직인다 → 물리적 차단" 이라고 썼는데, 이 세 건에는 해당하지 않는다.
solo6-16/18 에서는 브레이커가 정상 발동했고(53 건, 1 건) cmd_v 는 0 이었다. solo6lidar-09 는
별개의 드문 사건이거나 같은 사건의 다른 국면이다. **확정된 것은 위 세 건의 해부다.**

### 다음 표적

wp 277-286 진입에서 조향이 요구량의 절반 이하다. 후보:
* `mpc.Q` 의 e_psi 가중(1.0e8)이 e_y(1.0e6)의 100 배라 헤딩을 먼저 맞추려다 횡오차를 키우는가
* `wp_id_offset=2` 가 이 곡률에서 예측 지점을 뒤로 미루는가
* s6 감속(8.33 → 6.11 at wp 265)과 코너 진입이 겹치면서 종·횡이 경합하는가

셋 다 `evolve.py` 의 유전자이고, gen 0-3 은 **완주 기준이 틀린 채로** 돌았다. 수정된 판정으로
다시 돌릴 값이 있다.


## 2026-08-02 — 12 런씩 완결. 그리고 희귀 사건을 연속량으로 바꿨다

`lidar_load_ab` 를 팔당 12 런까지 채웠다. **정상 랩 중앙값이 양팔 46.19 로 동일하다**
(off n=49, on n=49). 라이다 페널티 철회는 확정이다.

레이스 중 정체 덤프는 4 건 — solo6-16 이 2 건, solo6-18, solo6-22. 전부 lidar off 팔에서
나왔지만 앞 배치에서는 solo6lidar-09 가 냈으므로 라이다와 무관하다. **대략 6 런에 1 번**이다.

| 사건 | 정지 wp | cmd_v | max \|e_y\| |
|---|---:|---:|---:|
| solo6-16 #1 | 285 | 0.00 | 4.399 m |
| solo6-16 #2 | 299 | 0.00 | 3.565 m |
| solo6-18 | 299 | 0.00 | 2.907 m |
| solo6-22 | 299 | 0.00 | 3.130 m |

**4 건 중 3 건이 wp 299 에서 멈춘다.** (solo6-16 의 두 번째는 첫 번째의 여파이므로 독립
사건은 3 건이다.)

### 지평 가설은 닫힌다

`config.yaml:70` 에 이미 있다 — `N: 20 # tested N=30 -> no improvement (53s vs 50s
baseline), reverted`. 예측 지평이 짧아서라는 설명은 이미 시험되고 기각됐다. 재시험 금지.

### 희귀 사건을 매 랩 재는 양으로 바꾼다

정체는 6 런에 1 번이라, 개입 효과를 완주/정체 횟수로 재려면 수십 시간이 든다. **그 앞에 오는
이탈은 매 랩 일어난다.** 그래서 `mpc_controller` 가 랩마다

> `corner peak |e_y| wp275-300: X.XXX m`

를 찍는다. 4 건의 덤프가 이 구간에서 2.9-4.4 m 를 기록했고 코리더 상한은 3.0 m 이므로,
**이 값이 내려가는 것이 개선의 정의**다. 런 3 개면 랩 표본 15 개가 나오니 유전자 하나를
몇 시간이 아니라 30 분에 판정할 수 있다.

기준선을 재는 중이다.


### 코너 이탈 기준선 (21 랩, 4 런)

```
solo6-24        0.812 1.508 1.642 0.822 0.489
solo6lidar-25   0.596 1.051 1.305 4.126 1.192
solo6-26        1.314 0.944 1.048 0.451 0.911
solo6lidar-27   1.121 2.568 0.956 1.016 1.561 0.956
```

| | |
|---|---|
| 중앙값 | **1.048 m** |
| 평균 | 1.257 m |
| 최대 | **4.126 m** |
| 2 m 초과 | 2 / 21 랩 |

**지표가 실패와 붙어 있다**: 4.126 m 랩이 정확히 solo6lidar-25 의 62.95 s 랩이다.

**단, 중앙값은 표적이 아니다.** 1 m 안팎의 이탈은 아무 비용도 없고, 2.568 m 를 기록한 랩조차
46.42 s 로 정상이었다. 판정은 **꼬리** — 2 m 초과 비율과 최대값 — 으로 하고, 중앙값은 "전 구간에서
그냥 더 밖으로 도는" 후보를 걸러내는 용도로만 본다.

`tools/corner_screen.py` 가 이 판정을 자동화한다. `--set KEY=VALUE` 로 config.yaml 스칼라 하나를
바꾼 변종을 만들고(주석 보존을 위해 텍스트 치환), solo6lidar 로 N 런 돌린 뒤 기준선과 나란히
찍는다. 런 3 개면 랩 15 개다.

**첫 후보: `wp_id_offset` 2 → 3.** 진단이 "늦게 돈다" 이므로 예측 지점을 앞으로 미는 것이 기전에
직접 대응한다. 이 유전자는 evolve 유전자 인덱스 6 이고 gen 0-3 은 인덱스 0-3 만 돌았으므로
미시험이다.


## 2026-08-02 — `wp_id_offset=3` 기각. 그리고 코너 지표만으로 판정하면 안 된다

첫 스크리닝 결과가 명확하다.

| 런 | 랩타임 | 정체 | 코너 peak |
|---|---|---:|---|
| wpoff3-00 | 46.6 **64.4 87.9 84.6 77.2** | **5** | 0.46 0.60 0.94 1.89 0.85 |
| wpoff3-01 | **65.2** 44.9 **69.6 104.3 89.1** | **5** | 0.62 0.57 1.74 1.00 1.34 |

기준선은 랩 46 s, 정체 6 런에 1 번이다. **참담하게 나쁘다. 기각.**

### 내 해석이 틀렸다

`wp_id_offset` 은 코너 예견을 늘리는 값이 아니다. `MPC.py:196` 이

```python
self.model.wp_id += self.wp_id_offset
```

로 **예측 지평의 원점 자체를 앞으로 옮긴다**(웨이포인트당 0.6 m, 제어 지연 보상용). 3 으로
올리면 1.8 m 앞에서 문제를 세우는데 상태 `x0/e_y0` 는 현재 위치에서 측정한 값이라, 전 구간에서
기준과 상태가 어긋난다. "먼저 돌게 한다" 는 내 읽기는 틀렸다. **재시험 금지.**

### 지표에 대한 교훈 — 이쪽이 더 중요하다

**코너 peak 은 전부 내려갔는데**(0.46-1.89, 기준선 중앙값 1.048 / 최대 4.126) **차는 훨씬
나빠졌다.** 실패가 지표가 보지 않는 구간으로 옮겨갔을 뿐이다. 국소 지표로 전역 실패를 잰
셈이다.

`corner_screen.verdict()` 를 사전식으로 고쳤다: **랩타임이 먼저**고(정상 랩 중앙값 50 s 초과면
즉시 REJECT), 코너 수치는 그것을 통과한 뒤에만 의미가 있다. 이건 `evolve.fitness()` 가 쓰는
것과 같은 순서이고, 같은 이유다.

### 덤프 해상도

첫 덤프들은 충돌 직전까지 0.5 s 간격이라 조향 부호가 매 표본 바뀌는 것(+0.019, -0.088, +0.045,
-0.083)이 진짜 채터링인지 부드러운 신호의 에일리어싱인지 가릴 수 없었다. 마지막 5 초를 0.1 s
간격(4 틱마다)으로 바꿨다. 언더스티어 진단의 근거가 이 조향 파형이므로 확인이 필요하다.


## 2026-08-02 — 언더스티어의 정체: 조향이 레이트 제한에 붙어 있다 (0.2136 rad/s)

0.1 s 해상도 덤프(solo6-30, 랩 5 가 125.52 s)에서 코너 진입 조향 파형이 처음으로 읽혔다.

```
t-5.00s wp=278 e_y=-0.168 e_psi=+0.336 v=6.027 steer=-0.044
t-4.90s wp=278 e_y=-0.009 e_psi=+0.337 v=6.077 steer=-0.065
t-4.80s wp=279 e_y=+0.207 e_psi=+0.414 v=6.122 steer=-0.086
t-4.70s wp=279 e_y=+0.449 e_psi=+0.395 v=6.161 steer=-0.108
t-4.60s wp=280 e_y=+0.658 e_psi=+0.467 v=6.105 steer=-0.129
t-4.50s wp=280 e_y=+0.917 e_psi=+0.429 v=5.951 steer=-0.150
t-4.40s wp=281 e_y=+1.067 e_psi=+0.516 v=5.768 steer=-0.172
t-4.30s wp=281 e_y=+1.388 e_psi=+0.439 v=5.781 steer=-0.193
```

**채터링이 아니었다.** 앞선 0.5 s 표본에서 부호가 매번 바뀐 것은 에일리어싱이었고, 실제로는
증분이 **0.021 rad / 0.1 s = 0.2136 rad/s 로 완벽히 일정한 램프**다. 일정한 기울기는 자유 제어가
아니라 **제약에 붙어 있다**는 뜻이다.

그 값의 출처가 코드에 있다 (`mpc_controller.py:448`):

```python
scaled_steer_rate_max = mpc_cfg.steer_rate_max / mpc_cfg.steering_tire_angle_gain_var
#                     = 0.35 / 1.639 = 0.2136 rad/s
```

**관측값과 소수점 세 자리까지 일치한다.** 언더스티어의 원인은 가중치도 지평도 아니고
**조향 레이트 제한**이다.

비용도 계산된다: 이 코너가 요구하는 조향은 약 -0.29 rad 이고, -0.044 에서 출발해 도달한 것은
t-3.2s — **1.8 초**가 걸렸다. 그 사이 e_y 는 -0.17 → +3.31 m 로 커진다. 램프가 두 배 빠르면
0.9 초에 도달하고 이탈은 절반 이하가 된다.

### 0.35 는 자체 부과값이다

`array_to_ackermann_control_command` 는 차량에 `steering_tire_rotation_rate = 2.0` 를 발행한다.
**MPC 는 차량 인터페이스에 알리는 값의 1/6 로 스스로를 묶고 있다.**

`GENES` 에 `steer_rate_max=1.40` 이 시험돼 나빴다는 기록이 있으므로(48.9 s 중앙값, recovery 23)
끝까지 올리는 것은 아니다. 유전자 범위 상단 **0.50**(scaled 0.305 rad/s, 램프 43% 가속)을
스크리닝 중이다.

### 부수 관측: QP 가 간헐적으로 실패한다

램프 도중 단일 표본이 0 쪽으로 튄다 — t-4.20s 에 -0.286 이었다가 t-4.10s 에 **+0.029**, 다시
-0.220. t-3.70s -0.242 → t-3.60s **-0.001** → -0.047. 실행불능 해가 0 명령을 내는 그 증상이
이탈 도중에도 간헐적으로 나타나며, 매번 램프를 처음부터 다시 쌓게 만든다. 별도 항목이다.


## 2026-08-02 — `steer_rate_max=0.50` 결과는 보류. 계측이 대상을 흔들고 있다

| 런 | 랩타임 | 정체 | 코너 peak |
|---|---|---:|---|
| srate050-00 | 65.2 78.9 69.9 75.1 73.0 | **5** | 0.71 0.71 1.84 0.79 1.11 |
| srate050-01 | 65.3 69.4 75.2 72.3 70.1 | **5** | 0.56 1.82 1.22 1.01 1.83 |

랩 65-79 s(기준선 46 s)에 정체 5 건. `wp_id_offset=3` 과 **서명이 똑같다** — 코너 peak 은
내려가고, 랩은 ~70 s, 정체는 정확히 런당 5 건. 무관한 두 파라미터가 같은 결과를 낼 이유가 없다.

config 변종은 정상이다 (`diff` 결과 78 행 한 줄만 다름). 그래서 파라미터가 아니라 **공통 요인**을
의심한다. 유력한 것은 내가 방금 올린 덤프 해상도다:

* `_dump_approach()` 는 **제어 루프 안에서** 호출되고, 해상도를 올린 뒤 한 번에 로그 60 여 줄을
  동기적으로 찍는다.
* 런당 최대 5 회이고, `_dumps_left = 5` 다. **나쁜 런들의 정체 수가 정확히 5 다** — 한도에
  닿았다는 뜻.
* 시간 순서가 맞는다: 해상도 상향 **전** 런(solo6-16/18/22)은 덤프 1-2 건에 랩 대부분 정상.
  **후** 런(solo6-30, srate050, wpoff3)은 전부 덤프 5 건에 랩 65-125 s.
* 6 m/s 에서 루프가 50 ms 멈추면 0.3 m 를 눈 감고 간다. 이탈 중이라면 그것이 정체를 만들고,
  정체가 다음 덤프를 부르는 양의 되먹임이 된다.

**따라서 `wp_id_offset=3` 기각도 재검토 대상이다.** 그 결론(랩 87-104 s)이 파라미터 때문인지
계측 때문인지 구분되지 않는다. 앞선 커밋의 기각은 유지하지 않고 **보류**로 내린다. 단
`wp_id_offset` 이 지평의 원점을 옮긴다는 코드 독해 자체는 유효하다.

**값을 바꾸지 않은 대조군**(`steer_rate_max=0.35`, 즉 기본값과 동일한 변종)을 돌리는 중이다.
이것도 랩이 65-79 s 로 나오면 원인은 파라미터가 아니라 하네스/계측이고, 46 s 로 나오면
두 후보의 기각이 진짜다.

이 대조군을 먼저 돌리지 않고 두 후보를 판정한 것이 순서 착오였다.


## 2026-08-02 — 대조군이 깨끗하다. 두 기각은 진짜이고, 레이트 제한은 물리 한계다

값을 바꾸지 않은 변종(`steer_rate_max=0.35`, 기본값과 동일):

| | 랩 중앙값 | 최악 | 정체 | 코너 peak |
|---|---:|---:|---:|---|
| ctrl035 | **46.21** | 47.43 | **0** | n=10 중앙값 1.139 최대 1.880, 2 m 초과 0/10 |

**계측/하네스 탓이 아니다.** 관측자 효과 가설은 기각한다. 따라서:

* `wp_id_offset=3` 기각 — **확정** (보류 해제). 랩 87.9/104.3 s, 정체 5.
* `steer_rate_max=0.50` 기각 — **확정**. 랩 65-79 s, 정체 5.

### 레이트 제한을 올리면 왜 나빠지는가

`scaled_steer_rate_max = steer_rate_max / gain` 은 MPC 가 **자기 모델 안에서** 쓰는 제약이다.
0.35 가 차량의 실제 한계에 맞춰진 값이라면, 0.50 을 주는 것은 MPC 에게 차가 못 하는 일을 할 수
있다고 믿게 하는 것이고, 그러면 차가 따라갈 수 없는 궤적을 계획한다. 측정이 그대로 나왔다.

**즉 램프가 0.2136 rad/s 에 붙어 있는 것은 튜닝 실수가 아니라 물리 한계다.** 조향을 더 빨리
세울 수는 없다.

### 그래서 남는 길: 진입 속도

요구 요레이트는 `kappa · v` 이므로 속도에 선형이다. 조향이 못 따라가면 **느리게 들어가면 된다.**
그리고 이 대회에서 그 거래는 유리하다 — 40 배틀 중 34 가 랩 수로 갈리므로, 랩당 1 s 를 주고
6 런에 1 번의 랩 손실을 없애면 이득이다.

`ref_vel.yaml` 헤더는 per-section 속도 조정이 세 번 실패했다고 기록하지만, 그것은 전부 **페이스를
위해 올리는** 방향이었다("grip ceilings", "braking lead-ins"). **한 구간을 안정성을 위해 내리는
것은 반대 거래이고 시험된 적이 없다.**

`corner_screen.py` 에 `--set-ref SECTION=VALUE` 를 추가했다. 첫 후보는 **s6 (wp 265-289)
22.0 → 18.0 km/h** — 정체가 나는 구간을 6.11 m/s 대신 5.0 m/s 로 통과한다. 요구 요레이트가 18%
줄고, 조향 램프가 필요한 각도에 도달할 시간이 그만큼 생긴다.

### 부수: 런이 간헐적으로 기동 실패한다

`corner-ctrl035-00`, `solo6-28`, `solo6lidar-29` 가 랩 0 으로 끝났다(시작 13 초 후 다음 런으로
넘어감). 대략 8 런에 1 번이다. 스크리너가 3 런을 돌리므로 판정을 막지는 않지만, 표본 수를 셀 때
빈 런을 빼야 한다.


## 2026-08-02 — 첫 실질 개선: s6 22 → 18 km/h 가 모드 A 를 없앤다. 그리고 모드 B 가 분리됐다

| | 랩 중앙값 | 코너 peak 중앙값 | **최대** | 2 m 초과 | 정체 |
|---|---:|---:|---:|---:|---:|
| 기준선 (21 랩) | 46.19 | 1.048 | **4.126** | 2/21 | 약 6 런에 1 |
| **s6=18.0 (15 랩)** | 46.89 | **0.554** | **0.741** | **0/15** | 3 런에 1 |

```
s6slow-00  48.03 46.89 46.60 46.76 47.26   stalls=0  peaks 0.52 0.54 0.70 0.67 0.55
s6slow-01  48.21 47.19 46.90 46.75 64.42   stalls=1  peaks 0.61 0.53 0.52 0.59 0.74
s6slow-02  48.13 47.31 46.73 46.86 46.63   stalls=0  peaks 0.51 0.46 0.69 0.70 0.46
```

**코너 이탈의 꼬리가 사라졌다.** 최대가 4.126 → **0.741 m** 이고 15 랩 전부 0.75 m 이내다.
기준선에는 2.568 과 4.126 이 있었다. 비용은 랩당 **+0.70 s** 로 느린 구간의 예상 대가 그대로이고,
기각된 두 후보와 달리 랩이 망가지지 않았다(중앙값 46.89, 최악 64.42 는 남은 정체 1 건).

거래 계산: +0.70 s/lap = 레이스당 +4.2 s. 타임아웃 600 s 에 주행 278 s 이므로 완주에 아무 위협이
없고, 모드 A 가 발동하면 랩 하나를 잃는다. **40 배틀 중 34 가 랩 수로 갈리므로 명백히 이득이다.**

### 두 실패 모드가 분리됐다

남은 정체 1 건(s6slow-01 의 64.42 s 랩)의 덤프:

```
stopped at wp=282 with cmd_v=5.00 m/s (commanded to move and did not -- physically blocked)
max |e_y|=1.041 m at wp=134, NEVER exceeded 1.94 m, so QP infeasibility is not the cause
```

**이탈이 아니다.** 차는 라인 위에 있고(최대 이탈 1.04 m, 그것도 코너가 아닌 wp 134), 5 m/s 를
명령받고도 움직이지 않는다.

| | 모드 A | 모드 B |
|---|---|---|
| 기전 | 언더스티어 → 이탈 → 벽 → QP 실행불능 | 라인 위에서 물리적으로 막힘 |
| 서명 | `cmd_v=0.00`, \|e_y\| 2.9-4.4 m | `cmd_v>=1.0`, \|e_y\| < 1.94 m |
| 관측 | solo6-16/18/22/30 (4 건) | solo6lidar-09, s6slow-01 (2 건) |
| 상태 | **s6=18.0 이 제거** | **남아 있음** |

solo6lidar-09 에서 "명령은 살아 있는데 안 움직인다 → 물리적 차단" 이라고 추론했다가 세 건의
QP 덤프를 보고 철회했는데, 그때 남긴 유보 — "두 모드가 다 있을 수 있고 덤프가 매번 어느 쪽인지
말해 줄 것이다" — 가 맞았다. **덤프 헤더의 `cmd_v` 판정이 그대로 두 모드를 갈랐다.**

s6=18.0 을 5 런 더 돌려 정체율을 굳히는 중이다. 그 다음이 제출물 반영 + `make eval` 재검증이다.


## 2026-08-02 (정정) — s6=18.0 기각: 모드 A 를 없애고 모드 B 를 유발한다

확인 런 결과가 앞 항목의 결론을 뒤집는다. (배치 2 의 런 00-02 는 이전 배치와 **태그가 겹쳐**
출력 디렉터리 충돌로 빈 런이 됐다. 유효 표본은 2 런.)

두 배치 합계 5 런 25 랩:

| | 런 | 랩 | **정체** | 랩 중앙값 | 최악 랩 | 코너 peak 최대 |
|---|---:|---:|---:|---:|---:|---:|
| ctrl035 (기본값) | 2 | 10 | **0** | 46.21 | 47.43 | 1.880 |
| s6=18.0 | 5 | 25 | **4** | 46.89 | 66.85 | **0.741** |

**코너 이탈은 확실히 좋아졌다**(최대 0.741 m, 2 m 초과 0/25). 그런데 **정체가 늘었다.**
덤프가 이유를 말한다:

```
s6slow-03  wp=133  cmd_v=8.33  |e_y|=2.793  ← 이탈, 그러나 s6 코너가 아닌 새 위치
s6slow-03  wp=280  cmd_v=5.00  |e_y|=0.788  ← 모드 B
s6slow-04  wp=282  cmd_v=5.00  |e_y|=1.483  ← 모드 B
s6slow-01  wp=282  cmd_v=5.00  |e_y|=1.041  ← 모드 B
```

기준선 24 런에 모드 B 는 1 건(solo6lidar-09)이었는데 **s6slow 는 5 런에 3 건**이다. 그리고 정지
시점의 `cmd_v=5.00` 은 정확히 새 s6 속도값이다 — 그 구간을 5.0 m/s 로 통과하는 동안 멈춘다.

**완주 기준으로 기각한다.** 페이스도 나쁘고(+0.7 s/lap) 완주도 나쁘다.

### 코너 지표가 개선됐는데 차가 나빠진 두 번째 사례

`wp_id_offset=3` 에 이어 두 번째다. 국소 지표는 두 번 다 옳게 움직였고 두 번 다 오답이었다.
두 가지를 고쳤다:

1. **지표를 랩 전체로 넓혔다.** s6=18.0 은 이탈을 wp 133-134(2.793 m)로 **옮겼는데** 275-300
   밴드는 그것을 못 봤다. 이제 랩마다 `lap peak |e_y|: X.XXX m at wp=N` 을 함께 찍어 새 hotspot 이
   처음 생길 때 보인다.
2. **판정에 정체율을 넣었다.** s6=18.0 은 랩 중앙값 46.89 로 어떤 랩타임 기준도 통과하는데
   5 런에 4 정체였다. `runs` 당 정체가 0.5 를 넘으면 REJECT 한다.
3. **태그 충돌을 막았다.** `--batch` 접미사를 추가했다. 이전 배치의 `/output/<tag>` 가 남아 있으면
   런이 즉시 죽는다 — 이번에 3 런을 그렇게 잃었다.

### 현재 상태

| | 상태 |
|---|---|
| 모드 A (이탈 → 벽 → QP 실행불능) | 기전 확정. 조향 레이트 0.2136 rad/s 물리 한계. **해결책 없음** |
| 모드 B (라인 위에서 막힘) | 기전 미상. `cmd_v>=1.0` 인데 v=0 |
| 시도해 기각 | `wp_id_offset=3`, `steer_rate_max=0.50`, `s6=18.0` |
| 이전에 기각(문서) | `N=30`, per-section grip ceiling, braking lead-in, `steer_rate_max=1.40` |
| 검증된 제출물 | 6/6, 278.23 s, avg 46.37 — **영향 없음** |

대조군을 4 런으로 늘려(`ctrlB`) 기본값의 정체율 기준선을 제대로 세우는 중이다. 지금 대조군은
2 런 0 정체뿐인데, 세션 전체 기준선은 6 런에 1 정체였다. 개입을 판정할 기준 자체가 흐리다.


## 2026-08-02 (최종 정정) — 코너 지표는 채점량을 예측하지 못한다. s6=18.0 은 무차이

4 런 대조군을 세우고 모든 런을 모아 계산하니 **내 앞선 두 결론이 다 과했다** — "첫 실질 개선"도,
그 뒤의 "기각" 도.

| | 런 | **6 랩 총시간** | 랩 중앙값 | 정체 | 정체 비용/런 | 최악 랩 |
|---|---:|---:|---:|---:|---:|---|
| 기준선 | 6 | **294.3 s** | 46.20 | 2 | 13.3 s | 125.7 / 60.5 |
| s6=18.0 | 5 | **293.5 s** | 46.83 | 4 | 6.7 s | 64.4 / 66.9 / 64.4 |

**채점되는 양에서 차이가 없다** (294.3 대 293.5). 구조는 바뀐다 — s6=18.0 은 정체를 더 자주 내지만
(0.80/런 대 0.33/런) 각 정체의 비용이 절반이다(6.7 대 13.3 s/런). 모드 B 는 64-67 s 랩에 갇히는
반면 모드 A 는 4.4 m 밖 벽에 박혀 125.7 s 까지 간다. 두 효과가 상쇄된다.

기준선 정체는 **둘 다 모드 A** 였다:
```
ctrlBb-00  wp=284  cmd_v=0.00  |e_y|=4.354
ctrlBb-01  wp=304  cmd_v=0.00  |e_y|=2.967
```
그래서 "s6=18.0 이 모드를 A→B 로 바꾼다" 는 관찰은 유효하다. 틀린 것은 **정체율 근거의 기각**이다:
0.33 대 0.80 은 런 4-5 개로 구분되지 않는다.

### 지표가 죽었다

`corner peak |e_y|` 는 4.354 → **0.741** 로 극적으로 좋아졌는데(45 랩 대 25 랩) **6 랩 총시간은
그대로다.** 이 지표는 채점량을 예측하지 못한다.

세 후보 모두에서 같은 일이 일어났다:

| 후보 | 코너 peak | 실제 결과 |
|---|---|---|
| `wp_id_offset=3` | 개선 (최대 1.89) | 랩 87-104 s. 참담 |
| `steer_rate_max=0.50` | 개선 (최대 1.84) | 랩 65-79 s. 참담 |
| `s6=18.0` | 개선 (최대 0.74) | 총시간 무차이 |

**세 번 다 지표가 좋아졌고 세 번 다 이득이 없었다.** 국소 이탈량을 줄이는 것은 이 차의 레이스
결과와 무관하다. 이탈은 실패의 원인이 아니라 동반 증상이었다.

**따라서 `corner_screen.py` 의 코너 지표 기반 스크리닝은 폐기한다.** 유효한 판정량은 **6 랩
총시간** 하나뿐이고, 그것은 희귀 정체가 분산을 지배하므로 팔당 15-20 런(약 2 시간)이 필요하다.
30 분에 유전자 하나를 판정하려던 계획은 성립하지 않는다. 계측 자체(`STALL ANATOMY` 덤프,
`lap peak |e_y|`)는 진단에 여전히 유용하다 — 두 모드를 가른 것이 그것이다.

### Track 1 현재 상태 — 정직하게

| | |
|---|---|
| 검증된 제출물 | **6/6, 278.23 s, avg 46.37** — 이 세션의 어떤 것도 건드리지 않았다 |
| 모드 A | 기전 확정(조향 레이트 0.2136 rad/s = 0.35/1.639, 물리 한계). **해결책 없음** |
| 모드 B | 기전 미상. `cmd_v>=1.0` 인데 v=0. 라인 위에서 막힌다 |
| 기준선 정체율 | 0.33/런, 비용 13.3 s/런 |
| 이번 세션 기각 | `wp_id_offset=3`, `steer_rate_max=0.50` |
| 이번 세션 무차이 | `s6=18.0` (구조는 바꾸지만 총시간 동일) |
| 문서상 기존 기각 | `N=30`, per-section grip ceiling, braking lead-in, `steer_rate_max=1.40` |

**이 세션에서 제출물을 개선하지 못했다.** 얻은 것은 진단(모드 A 의 완전한 인과 사슬, 모드 B 의
분리), 계측(덤프), 그리고 세 가지 실패한 개입의 기록이다. 그리고 완주 판정 버그 수정 — 이것은
앞으로의 모든 측정에 영향을 준다.


## 2026-08-02 — 모드 B: 내 덤프 판정 문구가 틀렸다. 그리고 계측이 원인 시점을 못 본다

모드 B 사건(corner-s6slow-04, wp 282)의 틱 추적:

```
t-5.00s wp=282 e_y=-0.114 v=+0.445 cmd_v=5.00   ← 이미 거의 정지, 가속 중
t-4.00s wp=283 e_y=-0.292 v=+1.084 cmd_v=5.00   ← 여기서 막힘
t-3.90s wp=283 e_y=-0.297 v=+0.425
t-3.30s wp=283 e_y=-0.304 v=+0.002
t-3.20s wp=283 e_y=-0.305 v=-0.045              ← 뒤로 구르기 시작
t-2.20s wp=282 e_y=-0.204 v=-0.505
```

전진 가속률이 **0.64 m/s²** 로, config 주석의 "이 차량이 실제로 내는 값 0.64" 와 일치한다.
그리고 전진 → 막힘 → 후진이 반복된다. 이것은 원래의 정체가 아니라 **이미 recovery 가 개입한
상태**다 — 전진은 creep, 후진은 reverse escape.

### 덤프의 판정 문구를 정정한다

런치를 확인하니 (`reference.launch.xml:224`) MPC 의 `control_cmd` 는
`nominal_control_cmd` 로 remap 되고 `stuck_recovery_controller` 가 그 사이에 앉는다.
`onNominalCommand()` 는 `runRecovery()` 가 참이면 **MPC 명령을 버린다**.

따라서 `cmd_v >= 1.0` 은 **"명령했는데 차가 안 움직였다" 가 아니다.** recovery 가 운전대를 잡은
동안 MPC 가 가고 싶어했다는 뜻일 뿐이고, 차량이 실제로 받은 것은 recovery 의 명령이다.
덤프 문구를 그렇게 고쳤다.

**즉 "모드 B = 물리적 차단" 이라는 해석은 근거가 없다.** 두 모드의 실제 구분은 이것뿐이다:

| | 모드 A | 모드 B |
|---|---|---|
| MPC 자신의 해 | `cmd_v=0.00` — QP 실행불능 | 정상값 유지 |
| 이탈 | 1.94 m 초과 | 1.94 m 미만 |

모드 B 에서 차가 왜 멈췄는지는 **여전히 모른다.**

### 계측이 원인 시점을 놓치고 있었다

링버퍼는 30 s(1200 틱)인데 출력은 최근 15 s 까지만이었다. **정지의 시작이 버퍼 안에 있는데 안
찍혔다** — 세 건의 모드 B 덤프가 recovery 진동만 보여준 이유다. 두 가지를 고쳤다:

1. 헤더에 **`last above 2 m/s`** 를 찍는다 — 그 시점의 wp / e_y / e_psi / cmd_v 가 정지가
   시작된 지점이다.
2. 성긴 출력이 **버퍼 전체 30 s** 를 덮는다(1 s 간격 30 줄 + 마지막 5 s 는 0.1 s 간격).

이 계측으로 4 런을 돌리는 중이다. 로그량이 주행을 흔들지 않는다는 것은 ctrlB 가 확인했다
(정체 2 건에 덤프를 찍고도 나머지 랩은 46.0-46.5 s).


## 2026-08-02 — 모드는 둘이 아니라 하나다: 이탈 → 접촉 → 정지

넓힌 창(버퍼 30 s 전체 + 정지 시작 지점 표시)으로 기본 설정 4 런을 돌렸다. 정체 3 건, 전부
같은 이야기다.

```
modeBc-01  wp=282  last above 2 m/s @ t-1.12s: e_y=+4.224 v=6.016  cmd_v=0.00
modeBc-03  wp=285  last above 2 m/s @ t-1.05s: e_y=+4.443 v=3.436  cmd_v=0.00
modeBc-00  wp=133  last above 2 m/s @ t-3.55s: e_y=+2.810 v=2.229  cmd_v=8.33
```

**셋 다 이탈이 먼저다.** 앞의 둘은 4.2-4.4 m 밖에서 전속으로 달리다 1 초 만에 멈춘다(충돌).
modeBc-00 은 wp 133 에서 2.81 m 밖에 있고 8.33 m/s 를 명령받으며 2.2 → 0 으로 3.5 초에 걸쳐
감속한다 — 벽에 긁히며 서는 모습이다.

**따라서 A/B 두 모드라는 구분을 철회한다.** 하나의 기전이고, 관측되는 차이는 두 가지 부수
변수일 뿐이다:

1. **정지의 급격함** — 정면으로 박으면 0.5 초에 멈추고(11.7 m/s²), 스치면 3.5 초에 걸쳐 선다.
2. **QP 상태** — 그 지점의 코리더 폭에 따라 실행불능이 되기도(cmd_v=0.00) 아니기도(8.33) 한다.

### 내 1.94 m 경계는 위치 의존이다

덤프가 쓰는 `1.94 m = max_width - width/sqrt(2)` 는 코리더 폭 3.0 인 구간에서 유도된 값인데,
wp 133 처럼 폭이 다른 곳에도 그대로 적용하고 있었다. modeBc-00 이 "경계를 넘었는데 cmd_v=8.33"
인 것은 모순이 아니라 **거기서는 경계가 더 크기 때문**이다. 헤더의 경계 판정은 폭 3.0 구간에서만
읽을 것.

### 기준선 정체율 정정: 0.63/런

| 배치 | 런 | 정체 |
|---|---:|---:|
| ctrlB | 4 | 2 |
| modeB (기본값) | 4 | 3 |
| **합계** | **8** | **5 = 0.63/런** |

앞서 "0.33/런" 이라고 쓴 것은 4 런 표본이었다. **s6=18.0 의 0.80/런은 이 변동 안에 완전히
들어간다** — 정체율 근거의 기각이 틀렸다는 것이 이것으로 확정된다(이미 철회함).

스크리너의 REJECT 임계도 0.5 → **1.0** 으로 올렸다. 0.5 는 대조군 자신에게 REJECT 를 띄운다.

### Track 1 결론

배포된 설정에서 남은 손실은 **런당 0.63 회, 회당 약 13 s**, 전부 같은 기전이다:
조향이 레이트 제한(0.2136 rad/s = 0.35/1.639, 물리)에 붙어 코너를 못 따라가고 → 코리더 밖으로
나가고 → 접촉한다. 레이트 제한을 올리는 것은 측정으로 기각됐고(MPC 가 차가 못 하는 일을 계획하게
됨), 진입 속도를 낮추는 것은 총시간 무차이였다.

이탈을 줄이면서 레이트 제한을 건드리지 않는 남은 길은 **레이싱 라인의 곡률**뿐이다
(`tools/optimise_line.py`, `tools/widen_line.py`). 현재 라인은 |kappa|max 0.228 이고 문제
구간은 0.17 이다. 이것은 큰 변경이고 `ref_vel.yaml` 헤더에 광범위한 선행 작업이 기록돼 있다.


## 2026-08-02 — 다음 후보: `max_width` 3.0 → 4.0 (코리더 폭, 미시험)

라인 재최적화로 가기 전에 더 직접적이고 시험된 적 없는 레버가 있다.

solo6-30 의 0.1 s 추적에서 조향 램프 도중 단일 표본이 0 으로 튄다:

```
t-4.20s steer=-0.286
t-4.10s steer=+0.029   ← QP 실패
t-4.00s steer=-0.220
```

이탈 중에 QP 가 간헐적으로 포기하면 그 틱의 조향이 사라지고 램프를 다시 쌓아야 한다. 램프가
필요한 각도에 도달하는 데 이미 1.8 초가 걸리는데, 그 사이의 실패 하나하나가 이탈을 키운다.
**코리더를 넓히면 실행 가능 영역이 커져 이 실패가 줄어든다.**

`config.yaml:51` 의 주석이 결정적이다:

```
max_width: 3.0  # testing: narrower corridor to see if it forces tighter tracking
                # through wp82-88/129-134
```

즉 **현재 값 3.0 은 의도적으로 "좁힌" 실험값**이고(유전자 범위 [2.8, 4.0] 의 하단), 그 결과는
주석에 기록돼 있지 않다. 그리고 주석이 지목한 **wp 129-134 가 정확히 modeBc-00 이 멈춘 자리**다
(wp 133, 이탈 2.83 m).

`max_width` 는 evolve 유전자 인덱스 4 이고 gen 0-3 은 인덱스 0-3 만 돌았으므로 **넓히는 방향은
한 번도 시험되지 않았다.** 4.0 으로 4 런 돌리는 중이다.

판정 기준(이번에는 처음부터 옳게): **6 랩 총시간이 유일한 판정량**이다. 기준선 294.3 s,
정체 0.63/런. 코너 peak 은 세 번 연속 오답이었으므로 참고만 한다.


## 2026-08-02 (Track 1 마감) — 어떤 개입도 기준선을 이기지 못한다. 그리고 정체는 랩을 안 뺏는다

모든 후보를 채점량 하나로 나란히 놓았다(`tools/corner_screen.jsonl`, 빈 런 제외).

| 팔 | 런 | **6 랩 총시간** | sd | 랩 중앙값 | 정체/런 | 6/6 완주 |
|---|---:|---:|---:|---:|---:|---:|
| **기준선** | 10 | **289.3 s** | 25.2 | 46.20 | 0.50 | **10/10** |
| `max_width=4.0` | 4 | 293.6 | 15.4 | 46.59 | 0.50 | 4/4 |
| `s6=18.0` | 5 | 293.5 | 10.2 | 46.83 | 0.80 | 4/5 |
| `steer_rate_max=0.50` | 3 | 436.2 | 12.8 | 74.06 | 5.00 | 2/3 |
| `wp_id_offset=3` | 3 | 448.1 | 5.8 | 80.88 | 5.00 | 2/3 |

기준선 289.3 ± 8.0(표준오차), width40 293.6 ± 7.7. 차이 4.3 ± 11.1 — **구분되지 않는다.**
점추정으로는 둘 다 기준선보다 나쁘다. `max_width` 넓히기도 기각한다(사유: 무차이이고 랩당
+0.39 s).

흥미로운 부수 관찰: 두 무차이 후보는 **분산을 줄인다**(sd 25.2 → 15.4 / 10.2). 파국적 정체를
중간 정도 정체로 바꾸기 때문이다. 완주가 위태로운 상황이라면 가치가 있겠지만, 기준선이 이미
10/10 이라 살 이유가 없다.

### 가장 중요한 수치: 기준선 10 런 전부 6 랩 완주

**정체는 랩을 뺏지 않는다.** 런당 0.50 회 발생하고 회당 약 13 s 를 쓰지만, 600 s 예산에 289 s
주행이라 여유가 311 s 다. 10 런에서 한 번도 완주를 놓치지 않았다.

Track 1 목표는 "deadlock 발생을 0 으로" 였는데, **실제 위험 지표(완주)로는 이미 0 이다.**
남은 것은 타이브레이크용 시간 약 8 s/레이스이고, 이 세션에서 시험한 어떤 파라미터로도 줄지
않는다.

### Track 1 최종 상태

| | |
|---|---|
| 제출물 | `submit/aichallenge_submit.tar.gz` — **6/6, 278.23 s, avg 46.37**, `make eval` 검증 |
| 기준선 재확인 | 10 런 289.3 s, **10/10 완주**, 정체 0.50/런 |
| 실패 기전 | 조향이 레이트 제한(0.2136 rad/s, 물리)에 붙어 코너 이탈 → 접촉. 단일 기전 |
| 이번 세션 기각 | `wp_id_offset=3`, `steer_rate_max=0.50`, `s6=18.0`(무차이), `max_width=4.0`(무차이) |
| 남은 유일한 각도 | 레이싱 라인 곡률 재최적화 — 큰 변경, 선행 작업 광범위 |

**제출물은 변경하지 않는다.** 시험한 모든 것이 그것보다 못하거나 같다.


## 2026-08-03 — 라이브 주행에서 드러난 것: 위험한 이탈은 코너(285)가 아니다

사용자에게 주행을 보여주려고 `solo6lidar` 를 창을 띄워 돌렸다. 랩 47.77 / 45.89 / 46.23 /
46.19 / 46.49 s, 정체 0. 그런데 **어제 넓힌 랩 전체 지표가 곧바로 새로운 사실을 보여줬다.**

| 랩 | 코너 밴드(275-300) | **랩 전체 최대** |
|---:|---:|---|
| 1 | 0.751 | **2.677 m @ wp 138** |
| 2 | 1.322 | 1.322 m @ wp 285 |
| 3 | 0.835 | 0.978 m @ wp **87** |
| 4 | — | 1.544 m @ wp 285 |
| 5 | — | 1.364 m @ wp 285 |

**wp 285 는 매 랩 1.3-1.5 m 로 일정하다.** 예측 가능하고 위험선(2.8 m 이상)에 닿지 않는다.
위험한 것은 **wp 87 과 138 의 불규칙한 이탈**이고, 랩 1 의 2.677 m 는 어제 정체가 시작된
값(2.79 / 2.83 m)에 거의 닿았다.

이 두 구간이 정확히 `config.yaml:51` 주석이 지목한 **wp 82-88 / 129-134** 다. 그 주석은
`max_width: 3.0` 을 "그 구간의 추종을 조이려고 좁힌" 실험이라고 적고 있었다 — 즉 이 문제는
전에도 알려져 있었다.

### 어제 세 후보를 잘못 판정한 이유가 여기 있을 수 있다

`wp_id_offset=3`, `steer_rate_max=0.50`, `s6=18.0` 을 전부 **코너 밴드 지표**로 스크리닝했고
세 번 다 지표는 좋아지고 결과는 나빠지거나 무차이였다. 이제 보면 **밴드가 위험한 구간을 아예
안 보고 있었다.** 특히 `s6=18.0` 은 s6 구간(wp 265-289)의 속도만 낮추므로 wp 87/138 에는 아무
영향이 없고, 실제로 s6slow-03 의 정체는 wp 133 에서 났다.

**다음 표적: wp 82-88 과 129-134.** 285 가 아니다. 이 구간의 곡률/폭/속도를 wp 285 와 같은
방식으로 해부하는 것이 남은 일이고, 지표는 이미 랩 전체를 보므로 준비돼 있다.


## 2026-08-03 — 진짜 격차는 폴이 아닌 그리드에서의 첫 코너다 (2/2 재현)

공식 3 대 조건(`eval3p.sh` = eval-3p.yaml)에서 2 레이스, 6 대분:

| | 레이스 1 | 레이스 2 |
|---|---|---|
| 폴 (d2) | **284.4 s**, 정체 0 | **284.5 s**, 정체 0 |
| d1 | 363.1 s — 랩 82.1, 95.2 | 355.1 s — 랩 109.1, 60.6 |
| d3 | 353.3 s — 랩 121.3 | 349.8 s — 랩 72.8, …91.2 |

**두 번 다 같은 결과다.** 폴은 284 s 로 솔로(278 s)와 거의 같고, 나머지 둘은 350-363 s.
3-6 랩은 여섯 대 모두 46 s 대로 정상이다. **손실 전부가 오프닝 랩이다.**

사용자가 화면을 보고 짚었다 — "첫 코너에서 일등만 잘 가고 둘은 충돌남". 데이터가 일치한다:
d3 의 정체가 `wp 33`, d2 의 랩 1 최대 이탈이 `wp 32`. 어제 나는 그 wp 33 정체를 출발선
인공물로 넘겼는데 **아니었다. 첫 코너였다.**

기전은 이미 아는 것과 같다: 그리드는 wp 29 근처이고 어제 덤프가 그 자리에서 `e_psi=-2.348 rad`
(-135 deg) 를 찍었다. 첫 코너가 wp 32-33 인데 차는 그리드에서 8.33 m/s 를 명령받는다. 폴이 아닌
슬롯은 라인에서 더 벗어나 있고, 조향은 0.2136 rad/s 물리 한계라 정렬을 못 끝낸다.

### 이것이 이 프로젝트 측정 전체의 맹점이었다

솔로 런은 **항상 라인 위에서 혼자 출발한다.** 그래서 어제 24 런, 팔당 12 런까지 채워 잰
모든 것 — 라이다 부하, 코너 이탈, `wp_id_offset`, `steer_rate_max`, `s6`, `max_width` — 이
**레이스당 8 s 규모의 정체 비용**을 놓고 다퉜다. 실제 격차는 **80 s** 이고 다른 구간에 있었다.

### 다음에 재개할 지점

* **오프닝 랩 대책.** 시험 시작만 하고 중단: `s1` (wp 1-29) 30 → 20 km/h,
  `tools/eval3p_race.py --set-ref s1=20.0`. 그리드에서 코너까지 정렬 여유를 주는 것.
  판정은 **폴이 아닌 두 대의 총시간**으로 해야 한다(폴은 이미 284 s 로 깨끗하므로 정보가 없다).
* **AI 트랙 실측.** 아직 한 번도 6 랩을 돌려본 적이 없다. 안전망 결선은 고쳤다
  (`control/tiny_lidar_net.launch.xml` 이 `nominal_control_cmd` 로 발행 → `stuck_recovery`
  경유). 빌드 후 `CONTROL_METHOD=tiny_lidar_net` 로 `solo6lidar` 부터.
* `eval3p_race.py` 는 `--set` / `--set-ref` 를 받으므로 3 대 조건에서 바로 스크리닝할 수 있다.

**제출물은 이 세션에서 변경하지 않았다** — `submit/aichallenge_submit.tar.gz`, 6/6, 278.23 s,
`make eval` 검증본 그대로.


## 2026-08-03 — 구간 속도 인하는 복구를 깨뜨린다 (2 회 독립 관측). 그리고 랩 1 은 실제로 고칠 수 있다

### 복구 자체는 배포 설정에서 정상이다

정체 에피소드 지속시간(연속 stuck/yield 이벤트를 15 s 공백으로 묶어 측정):

| | 에피소드 | 중앙값 | 최대 | 총합 | 30 s 초과 |
|---|---:|---:|---:|---:|---:|
| 기준선 3 대 (2 레이스, 6 대) | 11 | **11.0 s** | **21.0 s** | 119 s (대당 20 s) | **0** |
| `s1=20` 3 대 (1 레이스, 3 대) | 16 | 13.0 s | **65.0 s** | 303 s (대당 101 s) | **3** |

배포 설정은 중앙값 11 s, 30 s 넘는 에피소드 0 건이다. **복구는 병목이 아니다.**
(한 번 "복구가 병목" 이라고 썼는데 그것은 깨진 후보 쪽 숫자를 본 것이었다. 철회.)

### 구간 속도를 낮추면 복구가 깨진다 — 두 번 관측

| 후보 | 표적 지표 | 실제 결과 |
|---|---|---|
| `s6=18.0` | 코너 이탈 4.35 → 0.74 m | 그 구간에서 `cmd_v=5.00` 으로 멈추는 정체 3 건 |
| `s1=20.0` | 랩 1 82/121 → **53/52 s** | 65 s 에피소드, 대당 정체 손실 20 → 101 s |

두 후보 모두 **겨눈 지표는 개선했고 복구를 망가뜨렸다.** 기전은 관성이다: 속도가 낮으면
탈출 추진력이 부족해 escape 가 실패하고, 실패가 반복돼 에피소드가 길어진다. `s6=18.0` 의
정지 시 `cmd_v` 가 매번 정확히 그 구간 속도였던 것이 같은 이야기다.

**구간 속도 인하는 재시험 금지에 추가한다** — 겨냥한 구간이 어디든.

### 다만 오프닝은 실제로 고칠 수 있다

`s1=20` 의 랩 1 은 **82.1 / 121.3 s → 53.4 / 52.2 s** 였다. 폴이 아닌 두 대가 잃던 70-80 s 의
대부분이 사라졌다. 즉 **"느리게 출발하면 오프닝이 낫다" 는 실재한다.** 문제는 s1 이 매 랩
지나는 구간이라 그 대가를 6 번 치른다는 것뿐이다.

**다음 후보: 랩 1 에만 적용하는 속도 제한.** 설정값이 아니라 `ReferenceVelocityConfigulator`
쪽 코드 변경이 필요하다. 판정은 슬롯별로, 폴이 아닌 두 대의 총시간으로 한다
(`tools/eval3p_race.py` 가 이미 슬롯별 대조를 출력한다).

### 정정 기록: wp 32-33 은 첫 코너가 아니다

곡률 0.03(랩 최대 0.228), 폭 6.0 m, 그리드 방향과 **0.5 deg** 이내로 정렬. 내가 근거로 삼은
`wp=32/33` 로그는 카운트다운 중 정지한 차에서 나온 교착 차단기 줄이었고, `lap peak` 지표도
그 정지 구간을 포함하고 있었다. 둘 다 `_has_moved` 로 게이팅했다(차단기 자체는 원복 — 출발
실패한 차의 구조를 막으면 안 된다).


## 2026-08-03 — 공식 업스트림 최신 확인: AI 트랙은 측위가 없다. 그리고 오프닝 손실은 크래시가 아니다

`upstream/dev` = **2026-07-30** (오늘 8/3 기준 최신 본선). 우리 `dev` 가 **6 커밋 뒤처져**
있었고 그중 둘이 직접 관련된다:

```
79c351e  feat: add e2e scripts and rviz setting (#257)      <- AI 트랙 시나리오
74a90c8  chore: disable wall recovery in dev and parallel   <- 의도적으로 off
```

가져왔다: `aichallenge/simulator_scripts/e2e.sh`, `config/autoware-e2e.rviz`, `docs/plan/`.

### AI 트랙 조건 (`e2e.sh`, 업스트림 최신)

| | AI 트랙 | SW 트랙 (eval.sh) |
|---|---|---|
| 차량 | **4 대** | 1 대 |
| 타임아웃 | **300 s** | 600 s |
| `collisions` | **on** | off |
| `start-random` | **on** | off |
| `handicap` | off | off |
| **`imu` / `gnss` / `v2x`** | **전부 off** | on |
| `camera` / `lidar` | cpu / cpu | off / off |

**`imu off` + `gnss off` 는 측위가 없다는 뜻이고, 따라서 MPC 는 AI 트랙에서 원리적으로 작동할
수 없다.** `reference.launch.xml:111` 의 `imu_gnss_poser` 가 입력을 못 받으므로 초기 자세도
`kinematic_state` 도 없다. 라이다만 남는다 — TinyLidarNet 이 유일한 후보다.

그리고 **6 랩에 300 s** 다. 우리 MPC 의 깨끗한 기록이 278 s 인데 그 MPC 를 쓸 수 없다. 랩당
50 s 이내로 라이다만으로 6 랩. 정체 한 번이면 탈락이다. 4 대 + 충돌 on + 랜덤 그리드이므로
회피도 필수다.

### `wall-recovery off` 는 공식 의도다

`74a90c8` 이 dev/parallel 스크립트에서 **명시적으로 껐다.** 내가 "공식 yaml 에 항목이 없으니
기본값 on 일 수 있고 그러면 우리 테스트가 더 가혹한 것" 이라고 세운 가설은 **기각**이다.

### 3 대 오프닝 손실은 크래시가 아니다

d1 은 랩 1-2 를 82.06 / 95.20 s 로 달렸는데 **정체 덤프가 0 건이다.** 첫 덤프는 t+298 s, 이미
랩 3 이후다. 덤프는 `|v| < 0.15` 가 1 s 지속되면 찍히고 5 회분이 남아 있었으므로,
**그 두 랩 동안 한 번도 멈추지 않았다 — 절반 속도로 달렸을 뿐이다.**

출발 위치도 아니다. d3 의 카운트다운 덤프가 세 레이스 모두 동일하게
`wp=33, e_y=0.876 m, NEVER exceeded 1.94 m` 다. 실행불능 경계 안이다.

`official.yaml`(StreamingAssets/Race, schemaVersion 2)이 3 대 그리드 좌표를 주지만
"RacingKarts root 기준 로컬 좌표" 라서 월드 변환을 모르면 쓸 수 없다(raw 로 넣으면 99 km,
map origin 을 더하면 304 m — 둘 다 무의미). 실측 e_y 쪽이 신뢰 가능하다.

**따라서 남은 설명은 `handicap: on` 이다** — 3 대 설정에서 차마다 속도를 제한할 수 있는 유일한
장치이고, 멈추지 않은 채 절반 페이스, 랩 3 부터 정상 복귀라는 관측과 맞는다. AWSIM 로그에
handicap 출력이 없고 업스트림 문서에도 동작 설명이 없어 아직 확정하지 못했다.

**이것이 확정되면 오프닝 80 s 는 우리 결함이 아니라 대회의 평준화 장치이고, 고칠 대상이
아니다.** 확정 방법: 같은 3 대 시나리오를 `--handicap off` 로 돌려 비교. (`parallel.sh` 가
정확히 그 조건 — 3 대 / sync / handicap off / collisions off.)


# 2026-08-03 — 공식 규칙 원문 확인. 어제·오늘 결론 여러 개가 뒤집힌다

출처: 공식 문서 `aichallenge-documentation-racingkart/competition/sw-class.html`,
`ai-class.html`, `submission.html`. **셸 스크립트와 RaceConfig yaml 로 추론한 것이 여러 군데
틀렸다.**

## SW 부문 공식 규칙

| 항목 | 규칙 |
|---|---|
| 형식 | **3~4 대 동시 레이스** |
| 예선 | 내 제출 + 비슷한 레이팅 상대 + **NPC 1 대** (3 대) |
| 랩 / 제한 | 6 랩 / 10 분, **완주 순서로 순위** |
| **충돌** | 벽·타차 충돌, 또는 부정 가속 입력 시 **일정 시간 속도 제한 페널티** |
| 핸디캡 | 주행 순위에 따라 가감속·속도 파라미터 조정(밸런스용) |
| 허용 센서 | IMU, GNSS, 조향각, 휠 오도메트리, 기어, **V2X(타차 위치)** |
| 금지 | 고의적 위험 주행, 진로 방해, 시뮬레이터 해킹. 결선에 코드 리뷰 |
| 일정 | 예선 **7/1 ~ 9/1**, SIM 결선 **9/19**(상위 32), 실차 결선 **9/20**(상위 8~16) |

## AI 부문 공식 규칙

| 항목 | 규칙 |
|---|---|
| 허용 센서 | 카메라, LiDAR, 조향각, **휠 오도메트리**, 기어 |
| 금지 | **GNSS 등 SW 부문 센서** → 절대 위치 없음 → **MPC 사용 불가** |
| 예선 | **발표자료 + 주행 영상 심사, 8 월** (코드 제출이 아니다) |
| 최종 | 예선 점수 + 결선 주행 합산 |
| 참가 조건 | **AI 부문 팀은 SW 부문에도 반드시 참가** |

## 제출

포털 `https://aichallenge-board.jsae.or.jp` → Login → Submit Code →
`submit/aichallenge_submit.tar.gz`(공식 `create_submit_file.bash` 규격) → **상대 레이팅 범위
선택**(기본: 한 등급 위). 처리 약 30 분, 이후 `result-summary.json` / rosbag / `autoware.log`
다운로드.

## 뒤집히는 결론들

### 1. 오프닝 랩 80 s 손실 = 충돌 페널티다 (핸디캡이 아니다)

규칙에 **"충돌 시 일정 시간 속도 제한"** 이 명시돼 있다. d1 이 랩 1-2 를 82.06 / 95.20 s 로
달렸으면서 **정체 덤프가 0 건**이었던 것이 이것으로 설명된다 — 멈춘 적이 없고, 속도 제한이
걸려 있었다. 랩 3 부터 46 s 로 복귀한 것은 페널티 만료다.

**따라서 오프닝 손실은 우리 결함이며 고칠 수 있다. 고치는 방법은 충돌을 피하는 것이다.**
속도 인하나 라인 튜닝이 아니다.

### 2. 예선에 NPC 가 있다 — 2026-08-02 "NPC 없음" 항목 철회

어제 `eval-3p.yaml`(`npcs: 0`)과 셸 스크립트만 보고 **"점수 나는 어떤 시나리오에도 NPC 가
없다"** 고 썼다. 공식 규칙은 예선이 **내 차 + 상대 + NPC** 라고 명시한다. 그 항목과, 거기서
파생된 다음 결정들을 **철회한다**:

* "라이다 장애물 검출은 피할 대상이 없으므로 무의미" → **피할 대상이 있다.**
* "v2x 회피도 무의미" → **V2X 는 공식 허용 센서이고 타차 위치를 준다.**

`multi_purpose_mpc_ros` 에는 `v2x_vehicle_tracker.py` 와 `v2x_obstacle_avoidance` 설정이
이미 있다. 어제 나는 v2x 추적을 `USE_OBSTACLE_AVOIDANCE` 에서 떼어내 상시 동작시키는 변경을
만들었다가 **되돌렸다.** 그 되돌림의 근거가 사라졌다.

### 3. collisions 는 켜져 있다고 봐야 한다

`eval-3p.yaml` 은 `collisions: "off"` 지만, 공식 규칙은 충돌에 페널티를 부과한다 — 충돌이
일어나지 않으면 페널티 조항이 무의미하다. 3 대 레이스 관측(랩 1-2 만 반토막, 멈춤 없음)이
페널티와 일치하므로, **채점 환경에서는 충돌이 발생하고 페널티가 걸린다**고 본다.

## 다음 우선순위 (완전히 재정렬)

1. **SW 제출** — 검증본을 그대로 올린다(6/6, 278.23 s). 예선 마감 9/1 이므로 반복 제출 가능.
2. **충돌 회피를 켠다.** 되돌린 v2x 상시추적 변경을 복원하고, 3 대 조건에서 **충돌 횟수와
   랩 1-2 시간**으로 판정한다. 이것이 80 s 규모의 유일한 표적이다.
3. **AI 트랙 실측 + 영상.** 예선이 슬라이드+영상이고 8 월 마감이다. TLN 은 아직 6 랩을 돌린
   적이 없다. 안전망 결선은 공식 지시(`nominal_control_cmd`)대로 고쳐 뒀다.
4. 어제 하루의 파라미터 스크리닝(코너 이탈, `s6`, `max_width`, `steer_rate_max`,
   `wp_id_offset`)은 **전부 잘못된 표적**이었다. 레이스당 8 s 를 다투는 동안 80 s 가 충돌
   페널티로 나가고 있었다.


## 2026-08-03 — 공식 예선 조건에서 완주 0/6. 회피는 최적화가 아니라 필수다

처음으로 **공식 예선 조건**을 재현해 돌렸다(`aichallenge/simulator_scripts/prelim.sh`:
차량 2 대 + **NPC 1 대**, `collisions on`, `handicap on`, `lidar cpu`, 6 랩 / 600 s, sync).
지금까지 제출물은 솔로 `eval.sh` 에서만 검증됐다.

| 레이스 | d1 | d2 |
|---|---|---|
| 00 | **1 랩** (198.4 s) | **1 랩** (204.9 s) |
| 01 | 3 랩 (185.7 / 66.7 / 96.1) | 3 랩 (169.0 / 181.0 / 165.8) |
| 02 | 3 랩 (230.8 / 61.2) | 2 랩 (260.5 / 192.1) |

**6 대 전부 600 s 타임아웃. 6 랩 완주 0/6. 정체 28 건.** 솔로 기준선은 278 s 에 6/6 이다.
슬롯별로도 참담하다: slot 1 이 363 s → 592 s (+234), slot 2 가 284 s → 592 s (+308).

### 원인은 저장소가 이미 적어 둔 것이다

`reference.launch.xml:48-51`:

> "회피를 끈 결과는 해결하지 않고 받아들인다: 장애물 인식이 없으면 차는 앞차를 그대로
> 들이받고, 추돌은 차를 멈추는 페널티를 부른다. 그 답은 처음부터 앞서 나가는 것과, 접촉이
> 일어나면 stuck_recovery_controller 에 맡기는 것이다."

그 "받아들인 결과" 가 **1~3 랩**이다. 랩 169~261 s 는 박고 페널티로 멈추고 다시 박는 것이다.
"처음부터 앞서 나간다" 는 전략은 NPC 가 실재하는 예선에서 성립하지 않는다.

**따라서 v2x 회피 복원(우선순위 2)은 최적화가 아니라 제출 가능 여부의 전제다.**

### 아직 가려야 할 것: `collisions` 플래그

`prelim.sh` 는 `collisions on` 인데 공식 `eval-3p.yaml` 은 `off` 다. 규칙 문서는 충돌에
페널티를 명시하므로 on 이라고 봤지만, 이 차이가 0/6 과 6/6 을 가를 수 있다. 같은 시나리오에
충돌만 끈 `prelimnc.sh` 로 3 레이스 돌리는 중이다.

* off 에서도 무너지면 → NPC 자체(앞을 막는 것)가 원인이고 회피가 필수.
* off 에서 6/6 이면 → 붕괴는 전적으로 차-차 접촉이고, 보드의 실제 플래그가 무엇인지가
  제출 판단을 좌우한다.

**어느 쪽이든 지금 tarball 을 그대로 올리는 것은 보류한다.** 예선은 9/1 까지이므로 회피를
복원해 검증한 뒤 올리는 편이 낫다.


## 2026-08-04 — 붕괴는 전적으로 차-차 접촉이다. 회피를 복원했다

같은 예선 시나리오(차량 2 + NPC 1, handicap on, lidar on, 6 랩/600 s)에서 `collisions`
플래그만 바꿨다:

| | 6 랩 완주 | 총시간 중앙값 | 정체 |
|---|---:|---:|---:|
| `collisions on` (`prelim.sh`) | **0/6** | 592 s (전부 타임아웃) | 28 |
| `collisions off` (`prelimnc.sh`) | **6/6** | **288.3 s** | 3 |

**충돌이 물리적이지 않으면 NPC 가 앞에 있어도 우리 차는 288 s 에 6 랩을 끝낸다** — 솔로 278 s 와
거의 같다. 즉 NPC 의 존재 자체는 문제가 아니고, **물리 접촉이 전부**다.

슬롯별로도 확인된다: `collisions off` 에서 slot 1 이 363 → 285 s (-75), slot 2 가 284 → 310 s
(+24). 앞서 3 대 레이스(제출물 3 대, NPC 없음, collisions off)에서 본 350-363 s 는 NPC 가 없는
대신 우리 차 3 대가 서로 만든 결과였다.

계측 주의: `prelimnc` 의 d1 은 랩타임이 78 / 97 / 143 / 191 / 238 / 290 으로 **누적값**을
보고한다(증분이 47 s 씩). `race_outcome` 의 `elapsed`(285.0 s)가 신뢰 가능한 값이고 그것이 6 랩
합계와 일치한다. d2 는 정상적으로 랩별 값을 보고한다. 이 도메인별 차이는 원인 미상이며 별도
항목이다.

### 회피 복원 (우선순위 2 실행)

공식 yaml 은 `collisions: off`, 규칙 문서는 충돌 페널티를 명시한다 — 보드의 실제 설정을 확정할
수 없다. 그런데 **위험이 비대칭**이다:

* 보드가 off 인데 회피를 넣었다 → 손실 거의 없음(장애물이 없으면 게이트가 닫힌 채 지금과 동일)
* 보드가 on 인데 회피가 없다 → **1~3 랩**

그래서 넣는다. `mpc_controller.py` 에서 v2x 추적을 `USE_OBSTACLE_AVOIDANCE` 에서 떼어내 상시
동작시키고(`V2X_AVOIDANCE=0` 으로 끌 수 있음), 회피는 실제 v2x 차량이 나타날 때만 작동한다.
2026-08-02 에 만들었다가 잘못된 전제로 되돌린 그 변경이다.

검증 순서:
1. `collisions on` 예선 조건 — 0/6 이 개선되는가 (진행 중)
2. `make eval` 솔로 — 6/6, 278 s 가 유지되는가 (동등성 확인)


## 2026-08-04 — v2x 회피는 NPC 를 못 본다. 저장소가 이미 알고 있었다

`collisions on` 예선 조건에서 v2x 회피를 켜고 돌린 결과: **0/4 완주, 592 s 타임아웃, 정체
대당 4.75 건** — 회피 없을 때(대당 4.7 건)와 사실상 동일하다. **효과 없음.**

이유가 저장소에 이미 적혀 있었다. `aichallenge/simulator_scripts/battle3.sh` 주석:

> 보드가 돌리는 것은 Autoware 2 대 + AWSIM NPC 1 대, 6 랩, **타임아웃 480 s**(600 아님).
> NPC 는 우리 MPC 에게 보이지 않는다: Autoware 1 대 + NPC 1 대에서
> `/v2x/vehicle_positions` 가 **0 메시지**였고, KSK 의 배틀 bag 은 v2x 가 정확히 2 대만
> 나열하며 NPC 는 없다. **NPC 는 LiDAR 만 볼 수 있는 느린 장애물**이다(~149 s/랩).

세 가지가 따라온다:

1. **v2x 는 상대 차만 본다.** NPC 를 못 보므로 v2x 회피로는 이 붕괴를 막을 수 없다. 방금 0/4 가
   그 확인이다. (회피 복원 자체는 상대 차에 대해 유효하므로 유지한다 — 혼자일 때 비용 0.)
2. **NPC 를 볼 수 있는 것은 라이다뿐.** 2026-08-02 에 만들어 "피할 대상이 없다"며 접은
   `lidar_obstacle_publisher.py` 는 **맞는 도구였다.** 접은 근거가 틀렸다.
3. **타임아웃은 480 s 다.** 내가 만든 `prelim.sh` 는 600 으로 썼다 — `battle3.sh` 가 이미
   정확한 시나리오였고 나는 중복을 만들었다. 앞으로 `battle3.sh` 를 쓴다.

### 왜 추돌하는가 — `lidar_guard` 는 벽 전용이다

`lidar_guard.py` 는 상시 동작하지만 **벽 직전 회피 장치**다: 전방 콘 ±0.44 rad(±25°),
임계 `front_limit=1.25 m`. 8.33 m/s 에서 **0.15 초 앞**만 본다.

8.33 m/s 로 2.3 m/s 짜리 NPC 를 따라잡으면 `a_min=1.6 m/s²` 로 감속하는 데 **약 20 m** 가
필요하다. 가드가 보는 것의 **16 배**다. 그러니 그대로 박는다.

넓은 콘을 그냥 멀리 늘리면 안 된다 — ±25° 는 20 m 앞에서 ±9 m 를 덮으므로 코너마다 벽에 걸려
페이스가 죽는다.

### 넣은 것: 좁고 먼 전방 lead 가드 (기본 off)

`lidar_guard.py` 에 벽 가드와 **별개**의 검사를 추가했다:

* `lead_limit` (기본 **0.0 = 비활성**) — 이 거리 안에 앞차가 있으면 개입 시작
* `lead_halfangle` (0.10 rad ≈ 5.7°) — 우리 라인 위의 차만 걸리게 좁게
* `lead_gap` (3.0 m) — 이 간격에서 속도 0 이 되도록 감속

허용 속도 = `max(0, lead - lead_gap) * 0.75`. 12 m 에서 6.75 m/s, 8 m 에서 3.75, 3 m 에서 0 —
전속으로 도착하는 대신 뒤에 붙는다.

**기본 off 로 두고 측정한다.** 오늘 실패한 가설이 여럿이라 미측정 변경을 제출물에 넣지 않는다.
`LEAD_LIMIT` 환경변수로 켠다(런치 인자 `lead_limit`, `run_autoware.bash`·compose 에 배선).

지금 `battle3.sh` + `LEAD_LIMIT=12.0` 으로 2 레이스 돌리는 중이다. 판정: 6 랩 완주 수와 총시간
(기준선은 같은 시나리오에서 0/6).


## 2026-08-04 (정정) — 0/6 은 내가 `collisions on` 을 가정해서 만든 것이다. 공식은 off

사용자가 "망했는데 차라리 전에 제출한 걸 돌려보라" 고 해서 과거 기록과 맞춰 보니 내 측정이
틀렸다.

| 조건 | 6 랩 완주 | 총시간 |
|---|---|---|
| 공식 `eval-3p.yaml` 이 지정하는 것 | — | `collisions: off` |
| **collisions off + NPC** (`prelimnc.sh`) | **6/6** | **288 s** |
| collisions on + NPC (`prelim.sh`, 내 가정) | 0/6 | 592 s 타임아웃 |
| 과거 기록 (GOAL.md 머리말, 채점 포맷 12 레이스) | 9/12 | 347.5 평균 / 285 최고 |

**collisions off 에서 우리 차는 6/6 / 288 s 다 — 과거 기록(9/12, 347.5 s)보다 낫다.** 회귀가
아니었다. 규칙 문서의 "충돌 시 속도 제한 페널티" 문구를 보고 `--collisions on` 으로 돌린 것이
0/6 을 만들었고, 공식 RaceConfig 는 off 다.

하네스는 정상이었다: prelim 배치에 준비 경고 0 건, 두 대 모두 grounded + control mode 성공.
(경고가 뜬 것은 앞선 3 대 배치뿐이다.)

### 이 구간의 작업 평가

* **v2x 회피 복원** — 보드가 쓰지 않을 조건을 겨눈 것이었다. 다만 상대 차에 대해서는 유효하고
  혼자일 때 게이트가 닫혀 무해하므로 유지한다. **`make eval` 동등성 확인은 아직 미완.**
* **lead 가드** — `LEAD_LIMIT=12` 는 랩을 하나도 못 끝냈다. `lead_gap=3.0` 이라 정면 3 m 안에
  뭐라도 있으면 속도 0 인데 헤어핀에서는 벽이 3 m 안이다. **좁은 콘도 벽과 차를 구분 못 한다.**
  거리만으로는 원리적으로 안 되고, 구분하려면 거리 변화율을 자차 속도와 비교해야 한다(벽은
  자차 속도로 접근, 느린 차는 그보다 느리게). 기본 off 이므로 제출물 영향 없음. **미검증.**
* **`prelim.sh`** — `battle3.sh` 의 중복이고 타임아웃이 600 으로 틀렸다. `battle3.sh` 를 쓴다.

### 결론: 제출물은 그대로 올린다

`submit/aichallenge_submit.tar.gz` (2026-08-02, 5.6 MB)는 이 세션에서 **한 번도 변경되지
않았다.** `make eval` 로 6/6 / 278.23 s 검증됨(`submit/eval_verified_20260802.json`), 오늘
만든 실험물은 하나도 들어 있지 않다. 공식 형태(collisions off + NPC)에서 288 s / 6/6 이 나오므로
올릴 근거가 충분하다.

포털 `https://aichallenge-board.jsae.or.jp` → Login → Submit Code.


## 2026-08-04 — AI 트랙 첫 실측: 네트워크가 속도를 내지 않는다. 복구 노드는 MPC 전용이었다

`control_method=tiny_lidar_net` 을 처음으로 돌렸다(solo6lidar, 라이다 on, 6 랩).

### 1. TLN 은 `speed: 0.0` 을 출력한다 — 랩 0

라이브로 두 번 확인:

```
/control/command/nominal_control_cmd  20.2 Hz
  steering_tire_angle: -0.216      <- 조향은 살아 있다
  speed: 0.0                       <- 속도가 0
  acceleration: 0.6
/vehicle/status/velocity_status  longitudinal_velocity: 0.0
```

`TinyLidarNetNode is ready.` 는 뜨고 20 Hz 로 발행하는데 속도가 0 이라 차가 안 움직인다.
원인은 이미 이 문서에 있다: BC 학습 데이터의 가속 타깃이 **97.6% 가 tanh 출력 범위 밖이고
86.4% 가 단일 값**이었다. 네트워크는 속도를 배운 적이 없다.

**AI 트랙은 모델 재학습 없이는 한 랩도 못 돈다.** 예선이 슬라이드+영상 심사인데 "주행 영상"
자체를 만들 수 없는 상태다.

### 2. 복구 노드와 lidar_guard 는 `control_method == 'mpc'` 그룹 안에 있었다

실행된 노드 목록에 둘 다 없다:

```
robot_state_publisher, vehicle_velocity_converter, imu_corrector, gnss_poser,
gyro_odometer, imu_gnss_poser_node, ekf_localizer, simple_trajectory_generator_node,
tiny_lidar_net_controller_node, component_container, relay, autostart_orchestrator_node
```

어제 "무조건 실행된다" 고 읽은 것이 틀렸다. 그래서 TLN 출력 토픽을 `nominal_control_cmd` 로
바꾼 내 수정이 **사슬을 끊었다** — 받는 노드가 없어 `/control/command/control_cmd` 트래픽이
0 이 됐다. 공식 README 가 지시하는 배선은 맞지만 그것을 받을 노드를 먼저 띄워야 했다.

`stuck_recovery_controller` 를 모든 `control_method` 그룹 **밖**으로 옮겼다(`lidar_guard` 는
`/control/command/mpc_cmd` 를 구독하므로 MPC 전용으로 남겼다). **미검증이다** — TLN 이 속도 0
이라 검증이 불가능하고, MPC 경로에서는 원래 켜져 있었으므로 변화가 없어야 하지만 확인하지
않았다.

XML 주석에 `--` 를 넣어 파일을 깨뜨렸다(이 세션 두 번째 같은 실수). 고쳤다.

### AI 트랙에 실제로 남은 일

1. **속도 타깃을 고쳐 재학습.** `speeds.npy` 추출과 `--min-speed` 필터는 만들어 뒀다. 레이스
   bag 은 68% 가 정지 프레임이라 못 쓰고 `solo6lidar` 주행 3 회분이 더 필요하다.
2. 규칙상 허용 센서에 **휠 오도메트리와 조향각**이 있다. 현재 TLN 은 스캔만 쓴다 — 속도
   피드백을 입력에 넣으면 속도 회귀가 훨씬 쉬워진다.
3. 예선 마감 8 월, 결선 조건은 4 대 / 300 s / 충돌 on / 랜덤 그리드.

**제출물은 무변경이다.** `submit/aichallenge_submit.tar.gz`(8/2, 6/6, 278.23 s)에 어제·오늘의
실험이 하나도 들어 있지 않다.


## 2026-08-04 (정정, 가장 중요) — AI 트랙이 안 움직인 이유는 학습이 아니라 한 줄 누락이었다

`tiny_lidar_net_controller_node.py` 가 명령을 만들 때

```python
cmd.longitudinal.acceleration = float(accel)
cmd.lateral.steering_tire_angle = float(steer)
```

**`cmd.longitudinal.speed` 를 설정하지 않는다.** 기본값 0.0 이 그대로 차량에 간다. 차량은
speed 를 따르므로 움직일 수 없다. MPC 는 speed 와 acceleration 을 둘 다 넣는다.

`speed: 0.0` 을 보고 "네트워크가 속도를 못 배웠다" 고 쓴 것은 **틀렸다.** 증거:

* 출력층은 정상이다 — `fc4_weight` (2,10) absmax 1.21, `fc4_bias` absmax 0.23, 0 이 아니다.
* 조향은 살아 있었다(-0.216 rad).
* **weights 파일 4 개의 출력층이 사실상 동일하다** — 어느 것을 써도 speed 필드가 비어 있으니
  당연하다. 현재 파일은 `bak_pre_bc20260802` 와 md5 까지 같다(8/2 복원은 제대로 됐다).

`GOAL.md` 의 "가속 타깃 97.6% 가 tanh 범위 밖" 은 별개 문제로 남지만 **속도 0 의 원인은
아니었다.**

### 고침: 가속을 적분해 속도 목표를 만든다

```python
self._target_v = clamp(self._target_v + accel * accel_scale * dt, 0, v_max)
cmd.longitudinal.speed = self._target_v
```

`v_max` 기본 8.33 m/s(30 km/h, 기준 프로파일 상한), `accel_scale` 1.0. 측정 결과:

```
전:  nominal speed 0.0    control_cmd 트래픽 없음   vehicle 0.00 m/s   랩 0
후:  nominal speed 8.33   control_cmd 19.8 Hz       vehicle 3.52 m/s
```

**AI 트랙이 처음으로 움직인다.** 복구 노드도 함께 붙었다(노드 2 개 확인) — 그룹 밖으로 옮긴
변경이 작동한다.

### 단, 아직 완주는 못 한다

3.52 m/s 까지 올라간 뒤 0.10 m/s 로 떨어지고 랩 0, stuck 1 건. BC 모델이 조향을 충분히 배우지
못해 벽에 걸리는 것으로 보인다. **"속도 0" → "달리지만 서툴다"** 로 올라온 단계다.

남은 일:
1. 조향 품질 — 재학습 또는 finetune. 데이터는 `solo6lidar` 주행이 더 필요하다(레이스 bag 은
   68% 가 정지 프레임).
2. 규칙이 허용하는 **휠 오도메트리를 입력에 추가** — 지금은 스캔만 쓴다. 속도 피드백이 있으면
   적분 대신 직접 회귀가 가능하다.
3. 그 다음에 AI vs 제출물 3 대 대결이 의미를 갖는다(지금은 AI 가 장애물 역할만 한다).

**제출물은 여전히 무변경이다.**


## 2026-08-04 — `screen3` 은 라이다를 끈다. 과거 스크리닝 전체가 존재하지 않는 차를 재고 있었다

사용자 제안대로 로컬 후보 설정들을 서로 붙이는 토너먼트(`tools/tournament.py`)를 만들어
`screen3` 으로 돌렸다. 결과가 전원 참담했다 — **챔피언(제출 검증본 `config.yaml`)까지 1 랩**:

| 라운드 | 결과 |
|---|---|
| tourney-00 | 세 대 모두 completed=0, 정체 2-4 건 |
| tourney-01 | `csvmt_ay13` 2 랩 / `csvmt_ay11` 2 랩 / **`config.yaml` 1 랩**, 각 473.9 s, 정체 5 건 |

챔피언은 같은 3 대 조건(`prelimnc`, 충돌 off)에서 **288 s 에 6 랩**을 돈다. 크래시도 중복 노드도
없었다(`stuck_recovery` 1 개, `lidar_guard` 1 개, traceback 없음). 챔피언의 단일 랩 기록은
**310.9 s**, wp 40 에서 `|e_y| 3.528 m` 로 이탈한 뒤다.

**원인: `screen3.sh` 는 `--lidar off` 다.**

`lidar_guard` 는 전방 1.25 m 안에 벽이 있으면 속도를 5.5 m/s 로, 가속을 0.45 로 제한한다. 라이다가
꺼지면 `_scan` 이 None 이라 가드는 순수 통과가 되고, **차는 벽을 전속으로 만난다.** 스치고 지나갈
접촉이 310 s 짜리 쐐기가 된다.

그리고 **공식 RaceConfig 는 전부 `lidar: "on"`** 이다(`eval-{1,2,3,4}p.yaml`).

### 파급

* **과거의 모든 `screen3` 스크리닝이 가드 없는 차를 재고 있었다.** `evolve.py` 의 gen 0-3,
  거기서 나온 유전자 판정, `GOAL.md` 머리말의 "9/12 완주" 도 이 조건에서 나온 값이면 재해석이
  필요하다. (어느 시나리오였는지는 기록에 명시돼 있지 않다.)
* `lidar_guard` 는 지금까지 "작은 안전층" 으로만 취급됐는데, **없으면 6 랩이 1 랩이 된다.**
  이번 세션에서 측정된 가장 큰 효과다.

### 조치

`arena3.sh` 를 만들었다 — `screen3` 에 공식이 설정하는 두 플래그(`--lidar cpu`,
`--handicap on`)를 넣은 것. 3 대 / NPC 0 / 충돌 off / 480 s 는 그대로라 한 레이스에 독립 표본
3 개가 나온다. 토너먼트를 여기로 돌렸고 로그를 지우고 재시작했다.

**교훈: 시나리오 플래그가 결과를 지배한다.** 이 세션에서 세 번째다 — `collisions on/off` 가
0/6 대 6/6, `--lidar off/cpu` 가 1 랩 대 6 랩, `timeout 480/600` 이 완주 판정. 설정을 확인하지
않고 측정하면 측정이 아니다.


## 2026-08-04 — 토너먼트 하네스 결함 두 개: 슬롯 고정과 준비 대기 부족

`arena3`(라이다 on)로 바꿔도 결과가 나빴다. 그런데 패턴이 있었다 — **챔피언이 항상 slot 1 이고
항상 최하위**다:

| 라운드 | slot1 (챔피언) | slot2 | slot3 |
|---|---|---|---|
| screen3-00 | 0 랩 | 0 랩 | 0 랩 |
| screen3-01 | **1 랩** | 2 랩 | 2 랩 |
| arena3-00 | **0 랩** | 1 랩 | 4 랩 |
| arena3-01 | **1 랩** | 4 랩 | 1 랩 |

같은 챔피언이 2 대 조건(`prelimnc`)에서는 288 s 에 6 랩을 돈다. 그리고 다른 다중차 런에서도
slot 1 이 일관되게 뒤졌다(`prelim` 에서 363 s 대 284 s).

**즉 slot 1 이 측정 가능하게 불리한 그리드이고, 나는 챔피언을 거기에 고정해 놓고 "진다" 고 판정하고
있었다.** `evolve.py` 의 docstring 이 정확히 이것을 경고한다 — *"그리드 배치를 레이스마다
회전시켜 결과가 앞줄 인공물이 아니게 한다."* 그 경고를 그대로 밟았다.

두 번째: **`not every car reported ready` 경고가 3 대 라운드마다 떴고 2 대 라운드에는 한 번도
안 떴다.** `wait_grounded` 기본 240 s 로는 Autoware 3 개 + AWSIM 이 다 준비되지 못한다.
`--start-mode sync` 는 신호 하나로 전 차량을 동시에 풀므로 준비되지 않은 차는 **제어 없이
출발한다.**

고침:
* 라운드마다 출전자 순서를 회전(`entrants[rnd % 3:] + entrants[:rnd % 3]`).
* `wait_grounded` 타임아웃 240 → **420 s**, 차량 간 기동 간격 4 → 8 s.
* 로그를 지우고 재시작.

**이전 토너먼트 결과 전부(screen3 2 라운드 + arena3 2 라운드, 12 대분)를 폐기한다** — 슬롯이
고정돼 있어 설정 비교로 쓸 수 없다.

교훈이 누적된다: 이 세션에서 측정을 망친 원인이 시나리오 플래그(`collisions`, `--lidar`,
`timeout`) 세 개, 하네스 결함(컨테이너 정리, 태그 충돌, 슬롯 고정, 준비 대기) 네 개다.
**측정 장치를 먼저 검증하지 않으면 측정이 아니다.**


## 2026-08-04 — arena3 에서 첫 완주: `config_csvmt_plus` 6 랩 444.3 s (slot 3)

회전을 넣은 첫 라운드(rnd 0 이라 아직 회전 전 배치):

| 슬롯 | 설정 | 결과 |
|---|---|---|
| 3 | **`config_csvmt_plus`** | **6 랩 444.3 s** (완주) |
| 2 | `config_top36` | 2 랩 473.4 s |
| 1 | `config.yaml` (챔피언) | 0 랩, 정체 3 건 |

**arena3 에서 완주가 가능하다**는 것이 확인됐다 — 앞선 12 대분이 전부 0-4 랩이었던 것은 조건이
불가능해서가 아니었다. 그리고 slot 우위가 다시 나타난다.

챔피언은 arena3 에서 아직 완주 기록이 없는데, **지금까지 항상 slot 1 에 배정됐다.** 회전이
라운드 1 부터 돌기 시작했으므로(`config.yaml` 이 slot 3 으로) 다음 라운드가 설정과 슬롯을 가른다.

### 준비 경고는 오탐이 아니다 — 정말 느리다

세 대 모두 결국 `control mode request: success=True` 를 받는다(각 1 회 기록). 그런데 라운드
간격이 959 s 다 = 준비 420 s + 레이스 480 s + 정리. **즉 3 대는 준비에 420 s 를 다 쓴다.**
`--start-mode sync` 는 신호 하나로 전부 풀므로, 그 시점에 아직 준비되지 않은 차는 제어 없이
출발한다. 420 s 로 늘린 것이 상황을 개선했지만 (`csvmt_plus` 완주) 여유가 없다.

이것 자체가 slot 1 불리의 후보 설명이다: slot 1 이 가장 먼저 기동돼 가장 오래 대기하므로, 그
사이 상태가 어긋날 여지가 크다. 회전 데이터가 쌓이면 "slot 1 이 나쁜가" 와 "먼저 기동된 차가
나쁜가" 를 가를 수 있다 — 회전은 설정만 돌리고 기동 순서는 슬롯 순서 그대로이므로.


## 2026-08-04 — 회전 데이터: 챔피언이 arena3 에서 0/3 완주, csvmt 계열이 이긴다

| 라운드 | slot1 | slot2 | slot3 |
|---|---|---|---|
| 00 | `config.yaml` **0 랩** | `top36` 2 랩 | **`csvmt_plus` 6 랩 444.3 s** |
| 01 | `csvmt_ay11` 4 랩 | `csvmt_ay13` 5 랩 | `config.yaml` 3 랩 |
| 02 | `expA` 4 랩 | `config.yaml` 3 랩 | **`csvmt_ramps` 6 랩 405.4 s** |

**챔피언은 세 슬롯 전부에서 완주하지 못한다(0/3: 0, 3, 3 랩).** 슬롯 인공물이 아니다. 그리고
slot 1 도 0/4/4 랩이므로 **"slot 1 이 나쁘다" 는 앞 항목의 읽기는 약해진다** — 회전이 그것을
분리해 준 셈이다.

가장 깨끗한 비교는 같은 slot 3 이다: **챔피언 3 랩 vs `csvmt_plus` 6 랩 vs `csvmt_ramps` 6 랩.**
설정 차이는 실재한다.

모든 차가 정체 5 건(덤프 상한)에 걸리므로 arena3 는 거친 조건이다. 그 안에서 `csvmt_ramps` 가
405.4 s 로 가장 좋다.

### 단, arena3 는 공식 형태가 아니다

arena3 는 **제출물 3 대, NPC 없음**이다. 공식 예선은 **2 대 + NPC 1** 이고, 그 형태(`prelimnc`)
에서 챔피언은 **288 s 에 6/6** 을 돈다. 교통 상황이 다르므로 arena3 판정이 그대로 옮겨진다고
가정할 수 없다.

그래서 **공식 형태에서 직접 맞붙인다**: `config.yaml` vs `config_csvmt_ramps`,
`prelimnc`(2 대 + NPC, 충돌 off, 라이다 on) 3 레이스, 슬롯 회전. `eval3p_race.py` 에
`--configs` 를 추가해 슬롯별로 다른 설정을 돌리고 레이스마다 회전시킨다.

이 결과가 제출물 교체 여부를 결정한다. 교체하면 tarball 재생성 + `make eval` 재검증이 따른다.


### arena3 누적 (5 라운드)

| 설정 | 기록 (랩, 슬롯) | 완주 |
|---|---|---|
| `config.yaml` (챔피언) | 0(s1), 3(s3), 3(s2), 0(s1), **5**(s3) | **0/5** |
| `config_csvmt_ramps` | **6 랩 405.4 s**(s3) | 1/1 |
| `config_csvmt_plus` | **6 랩 444.3 s**(s3) | 1/1 |
| `config_csvmt_ay13` | 5(s2) | 0/1 |
| `config_csvmt_ay11` | 4(s1) | 0/1 |
| `config_expA` | 4(s1) | 0/1 |
| `config_top36` | 2(s2) | 0/1 |
| `config_horizon` | 2(s1) | 0/1 |
| `config_qtime` | 2(s2) | 0/1 |

챔피언은 슬롯 3 에서 3 랩과 5 랩을 냈고 슬롯 1 에서는 두 번 0 랩이다. **슬롯 효과는 있지만
챔피언 자체가 arena3 를 완주하지 못한다**(5 시도, 0 완주).

도전자 쪽 완주는 각각 **표본 1 개**다. `csvmt_ramps` 405.4 s 가 최고 기록이지만 한 번뿐이므로
확정으로 쓰지 않는다.

**결정은 공식 형태(`prelimnc`: 2 대 + NPC)의 맞대결에 맡긴다** — arena3 는 제출물 3 대에 NPC 가
없어 교통이 다르고, 그 조건에서 챔피언은 288 s 6/6 을 낸 적이 있다.


## 2026-08-04 — arena3 토너먼트 7 라운드 최종: 챔피언 0/7 완주

| 설정 | 완주 | 완주 시 총시간 |
|---|---|---|
| **`config_officialline`** | 1/1 | **367.3 s** |
| `config_csvmt_ramps` | 1/1 | 405.4 s |
| `config_csvmt_plus` | 1/1 | 444.3 s |
| **`config.yaml` (챔피언)** | **0/7** | — |
| `config_top36` | 0/2 | — |
| 나머지 8 개 | 0/1 | — |

챔피언 기록(랩/슬롯): **0/s1, 3/s3, 3/s2, 0/s1, 5/s3, 2/s2, 4/s1.** 세 슬롯 전부에서 7 번
시도해 한 번도 완주하지 못했다. 이것이 이 표에서 가장 강한 단일 사실이다 — 제출 검증본이
arena3 를 완주하지 못한다.

### 남은 교란과 한계

* **완주한 세 설정이 모두 slot 3 이다.** 챔피언도 slot 3 을 두 번 받았고(3 랩, 5 랩) 완주하지
  못했으므로 slot 3 은 충분조건이 아니다. 그러나 필요조건일 가능성은 배제되지 않았다.
* **완주자마다 표본이 1 개다.** 405.4 s 나 367.3 s 를 설정의 실력으로 확정할 수 없다.
* arena3 자체가 공식 형태가 아니다(제출물 3 대, NPC 없음).

### `config_officialline` 재평가 대상

367.3 s 는 이 세션에서 본 arena3 최고 기록이다. 그런데 `ref_vel.yaml` 헤더는 공식 라인을
**기각**했다고 적는다 — *"두 번 DNF, 그중 하나는 앞줄에서. 최소 클리어런스는 1.68 m 로 이 라인의
1.25 m 보다 넓지만, 10 m 더 길고 그 대가가 교통에서 여유보다 크다."*

그 기각은 **라이다가 꺼진 `screen3` 조건에서 내려졌을 가능성이 있다**(오늘 확인: `--lidar off` 는
`lidar_guard` 를 제거하고 6 랩을 1 랩으로 만든다). 클리어런스가 넓은 라인은 가드가 없을 때 특히
불리할 이유가 없으므로 단정할 수 없지만, 재시험 근거는 충분하다.

공식 형태 맞대결을 두 개 걸었다: `config.yaml` vs `config_csvmt_ramps`(진행 중),
이어서 `config.yaml` vs `config_officialline`.


## 2026-08-04 — 공식 형태 맞대결: arena3 판정은 전이되지 않는다

`prelimnc`(2 대 + NPC 1, 충돌 off, 라이다 on, 슬롯 회전) 3 레이스, `config.yaml` vs
`config_csvmt_ramps`:

| 설정 | 슬롯 | 완주 | 총시간 | 정체 |
|---|---|---|---|---|
| `config.yaml` | 1 | 6 랩 | **284.8 s** | 0 |
| `csvmt_ramps` | 2 | 6 랩 | 357.3 s | 4 |
| `csvmt_ramps` | 1 | 6 랩 | 333.5 s | 4 |
| `config.yaml` | 2 | 6 랩 | **498.0 s** (480 s 초과 → 미완주) | 5 |
| `config.yaml` | 1 | 6 랩 | **284.4 s** | 0 |
| `csvmt_ramps` | 2 | 6 랩 | 288.6 s | 2 |

| | 완주 | 완주 시 평균 |
|---|---:|---:|
| `config_csvmt_ramps` | **3/3** | 326.5 s |
| `config.yaml` (챔피언) | **2/3** | **284.6 s** |

**arena3 에서 0/7 이던 챔피언이 공식 형태에서는 2/3 에 284.6 s 로 훨씬 빠르다.** "arena3 판정이
그대로 전이된다고 가정할 수 없다" 는 유보가 옳았고, arena3(제출물 3 대, NPC 없음)는 설정 선택의
근거로 쓸 수 없다.

성격 차이가 뚜렷하다: 챔피언은 **284 s 아니면 498 s 로 양극단**(정체 0, 0, 5)이고
`csvmt_ramps` 는 288-357 s 로 일정하다(정체 2, 4, 4). 완주가 지배하는 Elo 배틀에서 낮은 분산은
실제 가치가 있지만 **3/3 대 2/3 은 한 레이스 차이이고 n=3 이라 결정할 수 없다.**

부수 관측: `csvmt_ramps` 가 한 레이스에서 **정상 랩 중앙값 44.47 s** 를 냈다 — 챔피언의 46 s 대보다
빠르다. 랩 페이스는 더 좋고 정체가 더 많은 구조다.

**결론: 제출물을 바꾸지 않는다.** 어느 쪽도 사전식 기준에서 확신을 갖고 우세하지 않다.

계측 버그 하나 고침: `one_race_per_slot` 이 `lap_peaks`/`peak_wps` 를 결과에 넣지 않아 요약
단계가 `KeyError` 로 죽었다(데이터 자체는 jsonl 에 남아 무손실).

다음: `config_officialline` 맞대결(arena3 최고 367.3 s, 그리고 `ref_vel.yaml` 이 기각했다고
기록한 라인) 진행 중.


## 2026-08-04 — `config_officialline` 이 앞선다 (2 레이스, 3 번째 진행 중)

`prelimnc` 맞대결, 슬롯 회전을 풀어 정리:

| 레이스 | `config.yaml` | `config_officialline` |
|---|---|---|
| 00 | slot1: 5 랩 **592.5 s 타임아웃** | slot2: 6 랩 **300.7 s** |
| 01 | slot2: 6 랩 350.3 s | slot1: 6 랩 **290.6 s** |

| | 완주 | 완주 시 평균 |
|---|---:|---:|
| **`config_officialline`** | **2/2** | **295.7 s** |
| `config.yaml` | 1/2 | 350.3 s |

`officialline` 의 랩은 47-48 s 대로 챔피언의 46 s 보다 느리지만 **완주를 놓치지 않는다.** 그리고
arena3 최고 기록(367.3 s)도 이 설정이었다. 두 조건에서 일관된 첫 후보다.

### 기각 기록이 뒤집히는 방향이다

`ref_vel.yaml` 헤더는 공식 라인을 이렇게 기각한다:

> "official line + baseline: 3 레이스 중 1 완주, 앞줄에서도 DNF 두 번. 최소 클리어런스 1.68 m 로
> 이 라인의 1.25 m 보다 넓지만 10 m 더 길고, 그 대가가 교통에서 여유보다 크다."

지금 측정은 반대다. 가능한 설명 두 가지:
1. 그 기각이 **라이다 꺼진 조건**에서 내려졌다 — 오늘 확인했듯 `--lidar off` 는 `lidar_guard` 를
   제거해 6 랩을 1 랩으로 만든다. 클리어런스가 넓은 라인이 가드 없이 특별히 불리할 이유는 없지만,
   조건 자체가 다르면 순위는 뒤집힐 수 있다.
2. 그 기각이 **틀린 완주 판정**으로 내려졌다 — 오늘 고친 버그로 과거 측정 24 건 중 8 건이
   DNF → 완주로 바뀌었다. "3 레이스 중 1 완주" 가 실제로는 2-3 완주였을 수 있다.

어느 쪽이든 **재평가 근거가 충분하다.** 3 번째 레이스 뒤 판단한다.

교체 조건: 완주에서 명확히 우세하고 페이스 손실이 감당 가능할 때. 현재 2/2 대 1/2 은 그 방향이지만
n=2 다. 교체하면 tarball 재생성 + `make eval` 재검증이 따른다.


## 2026-08-04 — `config_officialline` 3/3 완주 293.4 s 대 챔피언 1/3. 단, 변수가 두 개다

`prelimnc` 3 레이스, 슬롯 회전을 푼 결과:

| 레이스 | slot 1 | slot 2 |
|---|---|---|
| 00 | `config.yaml` **592.5 s DNF** | `officialline` **300.7 s** |
| 01 | `officialline` **290.6 s** | `config.yaml` 350.3 s |
| 02 | `config.yaml` **592.7 s DNF** | `officialline` **288.9 s** |

| | 완주 | 완주 시 평균 |
|---|---:|---:|
| **`config_officialline`** | **3/3** | **293.4 s** |
| `config.yaml` (챔피언) | 1/3 | 350.3 s |

**두 슬롯 모두에서 `officialline` 이 낫다** — 불리한 slot 1 에서 유일하게 완주하고(290.6 s), 유리한
slot 2 에서도 288.9 / 300.7 대 350.3 이다. 슬롯 배분이 불균등했지만(챔피언이 slot 1 을 두 번)
결론은 그 교란을 넘는다.

### 그러나 교체하기 전에 변수를 분리해야 한다

`config_officialline.yaml` 은 챔피언과 **두 곳**이 다르다:

```
csv_path:                    traj_top36_blend45.csv  ->  traj_mincurv.csv
use_path_constraints_topic:  false -> true
use_border_cells_topic:      false -> true
```

두 번째가 실질적이다. `use_path_constraints_topic: true` 면 MPC 는 코리더 제약을 토픽에서 받는데,
제출 런치는 `path_constraints_provider` 를 띄우지 않으므로(기본 false) 토픽이 비고 MPC 는
**정적 코리더 폴백**을 쓴다(2026-08-02 에 무조건 생성하도록 고친 그것). 즉 **매 사이클 제약을
재계산하지 않는다** — 그리고 오늘 진단한 QP 실행불능 쐐기는 바로 그 재계산에서 나왔다.

그러므로 3/3 이 **라인 때문인지 제약 소스 때문인지 갈리지 않는다.** 라인 교체는 큰 변경(길이,
클리어런스, 곡률이 모두 바뀜)이고 플래그는 한 줄이므로, 플래그만 켠 `config_ptopic.yaml`
(챔피언 라인 유지)으로 4 레이스를 돌려 분리한다.

* 플래그만으로 3/3 이 나오면 → **라인을 건드리지 않고** 이득을 얻는다. 훨씬 안전한 변경.
* 안 나오면 → 라인이 원인이고, 그때 라인 교체를 별도로 판정한다.

`ref_vel.yaml` 이 기각한 것은 **라인**이므로, 플래그 쪽이 원인이라면 그 기각과 모순도 없다.


## 2026-08-04 — 플래그만으로는 안 된다: 라인과 짝으로 작동하는 것으로 보인다

분리 실험(`config_ptopic` = 챔피언 라인 + 제약 플래그만 켬) 첫 레이스:

| 슬롯 | 설정 | 결과 |
|---|---|---|
| 1 | `config.yaml` | 6 랩 318.3 s |
| 2 | **`config_ptopic`** | **0 랩** (완주 기록 없음) |

**플래그만 켜면 오히려 못 단다.** `officialline` 의 3/3 은 플래그가 아니라 **라인
(`traj_mincurv`)** 에서 오는 것으로 보인다.

기전 가설: 플래그를 켜면 MPC 가 **정적 코리더**를 쓰고, 그 코리더는 라인에서 계산된다.
`ref_vel.yaml` 기록에 따르면 클리어런스가 **blend45 는 1.25 m, 공식 라인은 1.68 m** 다. 좁은
라인에 정적 코리더를 쓰면 여유가 없어 진행하지 못하고, 넓은 라인에서는 성립한다. 즉 **라인과
플래그는 짝으로 작동한다** — 어느 하나만 옮기면 안 된다.

이는 `ref_vel.yaml` 의 기각 기록과도 정합적이다. 그 기각은 공식 라인을 **동적 제약**과 함께
평가했을 것이고(플래그가 기본 false), 그 조합에서는 10 m 더 긴 라인의 대가만 드러났을 수 있다.
정적 코리더와 짝지으면 클리어런스가 이득으로 바뀐다.

n=1 이고 `config_ptopic` 이 랩을 아예 내지 못했으므로 기동 실패 가능성도 남아 있다. 남은 3 레이스로
확인한다.

**결정 규칙 갱신**: 플래그 단독이 확인 기각되면, 교체 후보는 **`config_officialline` 전체**(라인 +
플래그)가 된다. 그 경우 변경 범위가 크므로 `make eval` 재검증이 특히 중요하다 — 라인이 바뀌면
`ref_vel.yaml` 의 구간 속도 매핑(wp 기준)도 의미가 달라질 수 있다.


## 2026-08-04 — "slot 1 불리" 는 내가 만든 인공물이었다. 하네스 결함 8 번째

사용자가 "제대로 한 거냐" 고 물어서 핵심 결과를 스스로 감사했고, 체계적 편향을 찾았다.

`officialline` 3 레이스 전부에서 **d2 가 d1 보다 정확히 429 s 늦게** 준비됐다:

```
race 00   d1 ready 1785823267   d2 ready 1785823696   (+429 s)
race 01   d1 ready 1785824336   d2 ready 1785824765   (+429 s)
race 02   d1 ready 1785825166   d2 ready 1785825595   (+429 s)
```

429 = 내 `wait_grounded` 타임아웃 **420 s** + 9. 즉 **두 번째 차는 출발 신호가 나간 뒤에야
준비된다.** `autostart_orchestrator` 는 `/awsim/state == Grounded` 를 기다리는데, 두 번째 차량은
sync 해제 이후에만 Grounded 가 되는 것으로 보인다. **모든 차의 준비를 기다린 뒤 출발시키는 것은
교착이고, 타임아웃 만료로만 풀린다.**

대가는 이미 준비된 차가 치른다: **먼저 기동된 차는 그리드에서 420 s 를 대기**하며, 그 동안 교착
차단기가 AWSIM 이 붙잡고 있는 차에 1.5 m/s 를 계속 명령한다.

**따라서 이 세션 내내 관찰한 "slot 1 이 불리하다" 는 그리드 성질이 아니라 내 하네스 인공물이다.**
`prelim`(363 대 284), `screen3`/`arena3`(챔피언이 slot 1 에서 0 랩), `officialline` 맞대결(챔피언이
slot 1 에서 두 번 DNF) — 전부 이 편향 아래 측정됐다.

### 고침과 검증

준비 대기를 **첫 번째 차만, 90 s 상한**으로 바꿨다(`eval3p_race.py`, `tournament.py`). 해제가
있어야 준비되는 차는 해제를 받는다.

검증됨 — 같은 시나리오에서 준비 간격이 **429 s → 8 s**:

```
d1 ready 1785829359   d2 ready 1785829367   (+8 s)
```

첫 랩도 49.70 / 51.10 s 로 두 대 다 정상 출발했다. 이전 조건에서는 한 대가 592 s 타임아웃으로
끝났다.

### 재측정 중이며, 이전 다중차 결과는 전부 보류다

`config.yaml` vs `config_officialline`, `prelimnc`, 슬롯 회전, 4 레이스. 이 결과만이 교체 판단의
근거가 된다.

**보류로 내리는 것**: `officialline` 3/3 대 챔피언 1/3, `csvmt_ramps` 3/3 대 2/3, arena3 7 라운드
순위, `prelim` 의 오프닝 랩 80 s 손실 — 모두 420 s 그리드 대기 아래 측정됐다.

**제출물은 무변경으로 확인됨**: `submit/aichallenge_submit.tar.gz`, md5 `867823a8bb51ad75ae364ca78ca8cb2a`,
아카이브본과 동일, 이 세션 커밋 이력에 없음, 오늘 실험물 0 개 포함.


## 2026-08-04 — 되돌리기 검증: 복구가 정상으로 돌아왔고 기준선이 재현된다

`config.yaml` 대 `config.yaml`, `prelimnc` 2 레이스(= 재현 확인, 비교 아님):

| | 되돌리기 전 | 되돌린 후 |
|---|---|---|
| 차당 stuck 이벤트 | **31-37** | **0, 0, 3** |
| slot 1 총시간 | 363 / 355 s | **284.9 / 283.7 s** |
| 6 랩 완주 | 1/3 ~ 2/3 | **3/3** (랩을 낸 차 기준) |

**두 수정이 모두 확인됐다.**

1. **준비 대기 수정**이 "slot 1 불리" 를 제거했다 — 같은 슬롯이 363/355 s 에서 **284.9/283.7 s** 로,
   이제 가장 빠른 슬롯이다. 그 불리함은 그리드 성질이 아니라 먼저 기동된 차가 420 s 를 대기하며
   교착 차단기의 명령을 받던 내 하네스 인공물이었다.
2. **v2x 회피 기본 OFF + lidar_guard 원복**이 정체 폭주를 제거했다 — 차당 31-37 → 0-3.

283.7-284.9 s 는 검증본의 솔로 278.23 s 와 정합한다. **기준선이 재현된다.**

한 대(race 00 의 d2)가 랩 0 으로 끝났다 — 기동 실패이고 대략 8 런에 1 번 나온다. 표본에서 제외.

### 사용자 지적이 정확했다

"충돌복구가 안 되네" 와 "내가 하던 거도 될 텐데" 둘 다 맞았다. 복구가 안 된 것은 복구 로직 문제가
아니라 **내가 MPC 명령 사슬에 넣은 두 변경** 때문이었고, 검증본에는 그 코드가 없다. 되돌리자
복구가 즉시 정상화됐다.

**교훈: 검증된 기준선이 재현되는지 확인하지 않고 후보를 붙이면, 측정하는 것은 후보가 아니라 내
변경이다.** 이 세션에서 보류로 내린 결과 대부분이 이 실수다.

### 이제 진짜 비교

`config.yaml` vs `config_officialline`, `prelimnc`, 슬롯 회전, 4 레이스 — 깨끗한 하네스와 재현되는
기준선 위에서. 이것만이 교체 판단의 근거가 된다.


## 2026-08-04 — 깨끗한 하네스에서의 진짜 맞대결: 직접 대결 2-1 로 챔피언 우세

`config.yaml` vs `config_officialline`, `prelimnc`, 슬롯 회전, 재현되는 기준선 위 3 레이스:

| 레이스 | `config.yaml` | `config_officialline` | 같은 레이스 승자 |
|---|---|---|---|
| 00 | slot1 **284.2 s** (정체 0) | slot2 314.8 s (정체 2) | **챔피언** |
| 01 | slot2 **592.7 s / 1 랩** (정체 2) | slot1 290.4 s (정체 2) | officialline |
| 02 | slot1 **283.5 s** (정체 0) | slot2 397.2 s (정체 4) | **챔피언** |

| | 완주 | 완주 시 평균 | **직접 대결** |
|---|---:|---:|---:|
| `config.yaml` | 2/3 | **283.85 s** | **2 승** |
| `config_officialline` | **3/3** | 334.1 s | 1 승 |

### 판정 지표를 고친다: 집계 완주율이 아니라 직접 대결이다

보드는 **레이스 내 완주 순서**로 순위를 매긴다. 그러므로 두 후보를 같은 레이스에 넣었을 때 누가
앞서는지가 판정 지표다. 집계 완주율(3/3 대 2/3)로 보면 `officialline` 이 낫지만, **같은 레이스에서
챔피언이 완주하면 50-110 s 빠르므로 이긴다.** 3 레이스 중 2 승이다.

성격 차이는 그대로다 — 챔피언은 283-284 s 아니면 파탄, `officialline` 은 290-397 s 로 항상 완주.
챔피언의 DNF 1 회를 `officialline` 이 이기지만, 챔피언은 나머지 2 회를 크게 이긴다.

이번 실패가 **slot 2** 에서 났다는 점도 중요하다(이전에는 slot 1). 준비 대기 수정으로 슬롯 상관이
사라졌다는 확인이다.

### 현재 판단: 제출물 유지

`submit/aichallenge_submit.tar.gz`(md5 `867823a8bb51ad75ae364ca78ca8cb2a`, 6/6 / 278.23 s)를
그대로 둔다. 4 번째 레이스로 확인하지만 방향이 바뀔 가능성은 낮다.

**앞서 보류로 내린 `officialline` 3/3 대 1/3 은 이제 정식으로 철회한다** — 그 1/3 은 챔피언이
420 s 그리드 대기 편향 아래 두 번 slot 1 을 받은 결과였다. 깨끗한 측정에서는 2/3 이고, 직접 대결로는
챔피언이 앞선다.

`csvmt_ramps` 3/3 대 2/3 도 같은 편향 아래 측정됐으므로 보류 유지. 다시 재려면 직접 대결 지표로
해야 한다.


## 2026-08-04 — 4 레이스 완료. 두 지표가 갈리고, 아직 결정할 수 없다

race 03: `officialline` 332.2 s 완주, 챔피언은 **기동 실패**(랩 0, 경과 없음)로 무효.

| | 완주 | 완주 시 평균 | 직접 대결 |
|---|---:|---:|---:|
| `config.yaml` | 2/3 (1 회 무효) | **283.85 s** | **2 승** |
| `config_officialline` | **4/4** | 333.65 s | 1 승 |

**두 지표가 반대를 가리킨다.**

* **직접 대결**: 챔피언 2-1. 같은 레이스에서 완주하면 50-110 s 빠르다 → 유지
* **완주율**: `officialline` 4/4 대 2/3 → 교체

그리고 `GOAL.md` 머리말이 스스로 정한 원칙은 후자를 지지한다 — *"40 배틀 중 34 가 랩 수로 갈린다.
완주가 1 순위, 총시간은 랩 동수일 때만 작동하는 타이브레이크."* 상대가 완주하지 못하는 레이스에서
우리가 완주하면 이기는데, **챔피언은 3 번에 1 번 그 기회를 스스로 버린다.**

반대로 상대가 완주하면 타이브레이크가 작동하고, 그때는 챔피언(283.85 s)이 `officialline`
(333.65 s)보다 낫다. 참고로 KSK 는 282.155 s 다 — **그 페이스 상대에게는 챔피언도 타이브레이크에서
지고**, 그러면 남는 것은 "상대가 못 끝낼 때 내가 끝내는가" 뿐이므로 다시 완주율이 이긴다.

**정직한 결론: 아직 결정할 수 없다.** 완주율 차이가 n=3-4 에서 1-2 레이스이고, 어느 쪽이 유리한지는
상대 필드의 완주율에 달렸는데 그것은 로컬에서 측정할 수 없다. 예선이 9/1 까지이므로 표본을 쌓는다 —
6 레이스 추가 진행 중.

**판정 기준을 명시한다**: 완주율이 유의하게(예: 8 레이스 이상에서 2 회 차 이상) 앞서면 교체하고,
그 안이면 직접 대결 우세와 페이스 우위를 근거로 유지한다. 어느 쪽이든 교체 시 tarball 재생성 +
`make eval` 재검증.

제출물은 계속 무변경이다.


## 2026-08-04 (결론) — 8 레이스 적용: 미리 정한 기준에 따라 제출물 유지

| | 완주 | 완주 시 평균 | 직접 대결 |
|---|---:|---:|---:|
| **`config.yaml` (챔피언)** | **5/6** | **295.6 s** | **4 승** |
| `config_officialline` | 8/8 | 323.1 s | 2 승 |

레이스별(슬롯 회전 풀어서, 기동 실패는 무효 처리):

```
trueh2h-00  챔피언 284.2 O  vs  off 314.8 O   챔피언
trueh2h-01  챔피언 592.7 X  vs  off 290.4 O   off
trueh2h-02  챔피언 283.5 O  vs  off 397.2 O   챔피언
trueh2h-03  챔피언 무효     vs  off 332.2 O   무효
more-00     챔피언 285.7 O  vs  off 317.7 O   챔피언
more-01     챔피언 무효     vs  off 321.1 O   무효
more-02     챔피언 284.1 O  vs  off 321.8 O   챔피언
more-03     챔피언 340.4 O  vs  off 289.9 O   off
```

**표본이 늘자 완주율 격차가 좁혀졌다.** 4 레이스 시점의 2/3 대 4/4 가 8 레이스에서 **5/6 대 8/8** —
차이는 **1 레이스**다. 미리 정한 기준은 "8 레이스 이상에서 2 회 차 이상이면 교체" 였으므로 **기준
미달이고, 규칙대로 챔피언을 유지한다.**

기준을 미리 못 박아 둔 것이 여기서 값을 했다. 4 레이스 시점(2/3 대 4/4)에 교체했다면 표본 부족에
근거한 결정이었을 것이다.

부수 관측: 챔피언은 완주할 때 283-286 s 에 몰려 있고(4 회) 한 번 340.4 s 를 냈다. `officialline` 은
289.9-397.2 s 로 넓게 퍼진다. 즉 챔피언은 **좋을 때 더 좋고 나쁠 때 더 나쁜** 분포이며, 8 레이스
기준으로는 그 거래가 유리하다.

### 최종 상태

| | |
|---|---|
| 제출물 | `submit/aichallenge_submit.tar.gz`, md5 `867823a8bb51ad75ae364ca78ca8cb2a`, **무변경** |
| 검증 | `make eval` 6/6, 278.23 s, 평균 46.37 s |
| 공식 형태 재확인 | 5/6 완주, 완주 시 평균 295.6 s, 직접 대결 4-2 |
| 남은 문제 | 완주율 5/6 — 6 레이스에 1 번의 파탄이 곧 배틀 1 패. `GOAL.md` 목표는 12/12 |
| 원인 | 조향이 물리적 레이트 한계(0.2136 rad/s)에 붙어 코리더 이탈 → 접촉 |
| AI 트랙 | 달리기 시작했으나 랩 0. 조향 학습 + 휠 오도메트리 입력 추가가 남음 |


## 2026-08-04 — 36 초 랩은 도달 불가능하고 필요하지도 않다

요청받아 검토했다. 결론은 **불가능**이고, 근거가 셋이다.

### 1. 1 위 팀의 최고 랩이 44.010 초다

`GOAL.md` 머리말: *"참고 페이스: KSK(학생부 1 위) 6 랩 282.155 초, **최고 랩 44.010**."*

우리 깨끗한 랩은 44-46 초이고 6 랩 278.23 초로 **이미 KSK 의 282.155 초를 앞선다.** 36 초는 이
대회에서 존재하지 않는 페이스다.

### 2. 필요한 스케일링이 붕괴점을 한참 넘는다

| | |
|---|---|
| 현재 속도 프로파일 | 22-30 km/h, **평균 27.5 km/h** (`lap_time_model` 출력) |
| 336.1 m 를 36 s | 평균 **33.6 km/h** 필요 → **x1.22** |
| 재시험 금지 기록 | x1.10 이 신뢰 한계, x1.12 → 49.58 s 2 랩 손실, x1.15 → 붕괴 |

x1.15 의 붕괴 사유가 이미 기록돼 있다: **횡부하 20.93 대 이 차량 실측 능력 18.60.**

### 3. 물리적으로 막힌다

현재 라인 `|kappa|max = 0.228`. 36 초 평균속도 9.34 m/s 로 그 코너를 돌면

```
ay = 0.228 x 9.34^2 = 19.9 m/s^2  >  18.6 (실측 한계)
```

즉 **어디서도 감속하지 않는다는 가정**이라야 36 초가 나오고, 그것은 물리적으로 불가능하다.
필요 조건은 `|kappa|max <= 18.6 / 9.34^2 = 0.213` 인데, 그것조차 전 구간 최고속을 전제한다.

### `ref_vel.yaml` 의 "36.319 s 주행" 은 랩타임이 아니라 라인 출처다

그 문구는 **라인 기하의 출처**를 가리킨다(파일명이 `traj_top36_*` 인 이유). 우리 제어기가 대회
룰에서 낸 랩타임이 아니다. 실제로 그 원본 라인을 모델에 넣으면:

| 라인 | 예측 랩 (현재 속도 프로파일) |
|---|---|
| `traj_top36_line` | **41.53 s** |
| `traj_megamax` | 41.69 s |
| `traj_crescendo` | 42.36 s |
| `traj_top36_blend45` (현재) | 43.13 s |
| `traj_ksk363` | 43.97 s |
| `traj_mincurv` / `traj_mintime` | 44.45 s |

모델은 시뮬레이터보다 약 3 초 낙관적이다(현재 라인 43.13 예측 대 46-47 실측). **라인만 바꿔
얻을 수 있는 최대치는 예측 41.5 초 = 시뮬레이터 환산 약 44.5 초**로, 현재보다 약 2 초 빠르다.

**재시험 금지에 추가: 36 초 랩 목표.** 1 위의 최고 랩이 44.010 초이고, 필요 스케일링 x1.22 는
측정된 붕괴점 x1.15 를 넘으며, 최저 곡률 코너에서 물리 한계를 초과한다.

### 그러면 어디를 봐야 하나

`GOAL.md` 머리말이 이미 답을 적어 뒀다 — *"페이스는 이미 끝났다. 교착이 한 번도 없는 실행은
랩당 46.7 초, 6 랩 환산 280 초로 KSK 의 282.155 를 넘는다. **남은 손실 전부가 교착이다.**"*

오늘 8 레이스로 재확인된 것도 같다: **완주 5/6.** 6 레이스에 1 번의 파탄이 곧 배틀 1 패이고,
그것이 1 위(레이팅 810)와 우리(734) 사이의 실질 격차다. 페이스 2 초가 아니라 완주율이다.


# 2026-08-04 — GOAL.md 의 페이스 주장은 틀렸다. 물리 최소는 31.25 초다

사용자가 "그 문서 믿지 마" 라고 해서 문서 인용을 버리고 실측과 물리로만 다시 계산했다. **결론이
정반대로 나왔다.**

## 1. 36 초 주행은 실재한다 — 텔레메트리에 있다

`env/final_ver3/traj_top36_telemetry.csv` 는 실측 주행 기록이다:

```
랩          36.87 s / 325.2 m   (ds/v 적분)
속도        7.55 ~ 9.86 m/s,  평균 8.85   (= 31.9 km/h)
종가속도    +0.825 ~ -2.174 m/s^2
곡률        |kappa| max 0.2451
횡가속도    max 20.09,  p99 18.25,  p90 12.81 m/s^2
```

**차량이 실제로 20.09 m/s^2 횡가속도를 냈다.** 문서의 "이 차량 실측 능력 18.60" 은 이 데이터와
모순이다. 설정 `ay_max` 가 20.0 이므로 그 한계에 붙어 달린 기록이다.

**제동도 -2.174 m/s^2 를 냈다.** 우리 `a_min = -1.6` 은 실측보다 보수적이다.

내가 앞 항목에서 "36 초는 x1.22 스케일링이 필요하고 불가능" 이라고 쓴 것은 **336.1 m 로 나눈
잘못**이다. 그 주행은 **325.2 m** 경로였고 필요한 것은 x1.16 였다. **그 항목을 철회한다.**

## 2. 우리 라인의 물리적 최소는 31.25 초다

곡률 제한 `v = sqrt(ay_max/|kappa|)` + 전진 가속 패스 + 후진 제동 패스로 계산:

| 라인 | 길이 | \|kappa\|max | 최소 랩 |
|---|---:|---:|---:|
| **`traj_megamax`** | 327.8 m | **0.2099** | **29.83 s** |
| `traj_top36_telemetry` | 325.2 m | 0.2451 | 30.14 s |
| `traj_crescendo` | 335.1 m | 0.2374 | 30.79 s |
| **`traj_top36_blend45` (현재)** | 337.8 m | 0.2301 | **31.25 s** |
| `traj_top36_line` | 349.0 m | 0.2281 | 31.97 s |
| `traj_mincurv` / `traj_mintime` | 348.7 m | 0.3895 | 33.89 s |

**우리가 지금 쓰는 라인이 이미 31.25 초를 허용한다.** 실제로는 46-47 초를 낸다 — **15 초를
흘리고 있다.** 병목은 라인도 차량도 아니라 **속도 프로파일**이다: 물리 프로파일은 평균 10.83 m/s
를 쓰는데 `ref_vel` 은 22-30 km/h(평균 7.6 m/s)에 묶여 있고, `v_max` 는 40 km/h 인데 프로파일이
30 에서 멈춘다.

`traj_mincurv` 가 이름과 달리 `|kappa|max 0.3895` 로 가장 나쁘다는 것도 나왔다 — 오늘
`config_officialline` 이 47-48 초 랩을 낸 이유다.

## 3. 왜 이 방향이 안 시도됐나: 기록된 실패는 전부 균일 스케일링이다

`x1.10 / x1.12 / x1.15` 는 **코너 속도까지 같이 올린다.** 그리고 오늘 규명한 대로 코너 속도의
한계는 접지력이 아니라 **조향 레이트(0.2136 rad/s = 0.35/1.639)** 다 — 접지가 허용해도 조향각을
제때 세울 수 없다. 그래서 코너를 올리면 이탈하고 붕괴한다.

**직선 속도에는 그 제약이 없다.** 곡률을 아는 프로파일, 즉 이미 최고값(30)인 구간만 올리고
22/25 구간은 그대로 두는 것은 시도된 적이 없다:

| 프로파일 | 예측 랩 | 평균 |
|---|---:|---:|
| 현재 (22-30) | 43.99 s | 27.5 km/h |
| **직선 36** (22-36) | **41.51 s** | 30.0 km/h |
| **직선 40** (22-40) | **40.04 s** | 31.1 km/h |

모델은 시뮬레이터보다 약 3 초 낙관적이므로(현재 43.99 예측 대 46-47 실측) 환산 43-44 초 —
**랩당 2.5-4 초, 6 랩에 15-24 초**다.

`ref_vel_straights36.yaml`, `ref_vel_straights40.yaml` 을 만들었고, `eval3p_race.py` 에
`--refs` 를 추가해 슬롯별로 다른 속도 프로파일을 붙일 수 있게 했다. `ref_vel.yaml` 대
`ref_vel_straights40.yaml` 로 4 레이스 맞대결 진행 중.

## 철회 목록

* "36 초 랩은 도달 불가능" — 336.1 m 로 나눈 계산 착오. 실측은 325.2 m 에서 36.87 s 다.
* "이 차량 실측 횡가속도 능력 18.60" — 텔레메트리가 20.09 를 보여준다.
* "페이스는 이미 끝났다, 남은 손실 전부가 교착" — 물리 최소 31.25 초 대 실측 46 초이므로
  **페이스에 15 초가 남아 있다.** 교착이 유일한 손실원이 아니다.
* `a_min = -1.6` 이 적절하다는 전제 — 실측은 -2.174 를 낸다.


## 2026-08-05 — 직선 리프트 단독은 더 나쁘다. 빠진 절반은 제동이다

`ref_vel.yaml` 대 `ref_vel_straights40.yaml`, `prelimnc` 4 레이스, 슬롯·프로파일 회전:

| 프로파일 | 완주 | 평균 | 정체 합 |
|---|---:|---:|---:|
| 현재 (22-30) | 3/3 | **287.5 s** | **1** |
| 직선 40 (22-40) | 4/4 | 355.9 s | **16** |

**모델 예측(40.04 대 43.99 s)과 반대다.** 모델은 정체를 모델링하지 않는다 — 정체 16 건이 페이스
이득을 전부 먹었다.

**다만 랩 페이스 이득은 실재한다**: 한 레이스에서 정상 랩 중앙값 **45.52 s** 가 나왔다(기준선
46.95). 즉 빠르게 달릴 수는 있고, 코너에서 잃는다.

기전은 오늘 진단과 이어진다. 직선이 빨라지면 **코너 진입 속도**가 올라가고 거기서 조향 레이트
한계(0.2136 rad/s)가 물린다. 그리고 그 속도를 털어낼 제동이 부족하다 —
**`a_min = -1.6` 인데 36 초 텔레메트리는 -2.174 를 냈다.**

`a_min = -2.17`(실측이 보여준 값) + 직선 40 을 짝지어 4 레이스 진행 중. 문서에 "a_min -2.7 시험 →
동일" 기록이 있지만 그것은 **직선이 느린 상태에서** 잰 것이다 — 털어낼 속도가 없으면 제동 여유는
드러나지 않는다.

## 2026-08-05 — 공식 리더보드 확인: 공개 페이지에는 순위가 없다

`https://aichallenge-board.jsae.or.jp/public/team` 에서 확인:

* 우리 팀은 **メタモン (Korean Electronics and Telecommunications Research Institute)**,
  **제출 88 회로 7 위**.
* 1 위 HIROSHIMA MOMIJI NS(히로시마시립대) 147 회, 2 위 Tasty GarlicSand(닛산 도치기 자동차대)
  146 회, 3 위 B-CORE(SUBARU) 119 회.
* **이 페이지는 제출 횟수(努力値)만 보여준다** — 레이팅, 랩타임, 부문/코스, 배틀 결과는 없다.
* `/public/live` 는 JS 렌더링이라 내용을 읽을 수 없다. 실제 순위는 로그인 뒤에만 보인다.

### 따라서 미확인으로 표시하는 수치

`GOAL.md` 머리말의 다음 값들은 **내가 확인할 수 없다.** 공개 페이지에 없고, 이전 세션의 로그인
상태 관찰로 보인다:

* "레이팅 734, 일반부 코스 2 내 22/60"
* "넘어야 할 1 위 CRESCendo 810"
* "KSK(학생부 1 위) 6 랩 282.155 초, 최고 랩 44.010"

마지막 것은 특히 중요하다 — 내가 "36 초는 불가능" 이라고 논증할 때 그 44.010 을 근거로 썼다.
텔레메트리가 36.87 s 를 보여주므로 그 논증은 이미 철회했지만, **1 위의 실제 페이스를 모르면 페이스
목표의 기준선이 없다.** 포털 로그인 확인이 필요하다.


## 2026-08-05 — 1 등에는 40 초가 필요하다. 웨이포인트별 프로파일이 40.90 초를 예측한다

사용자 지적으로 앞 항목의 "페이스는 순위와 무관" 을 **정정한다.** 보드를 다시 읽으면:

* **1 위 S-drive 는 최고 랩 40.009**, 2 위 KSK 42.865, 3 위 Sunday Drivers 42.530.
* 4·5 위(57.823 / 56.743)와 22 위(147.999)를 보고 "페이스 무관" 이라 쓴 것은 **과잉 일반화**였다.
* 정확한 독법: **중위권은 신뢰성만으로 버티지만 1 위는 페이스도 갖고 있다.** 36.319(8 위)와
  38.384(6 위)는 페이스만으로는 부족함을 보여주고, 1 위는 둘 다 갖췄다.

우리 최고 랩은 실측 44.12 초다. **40 초까지 4 초, 9%.** 우리 라인의 물리 최소가 31.25 초이므로
40 초는 물리적으로 안쪽이다.

### 30 km/h 상한은 클램프가 아니다

`ReferenceVelocityConfigulator.MAX_VELOCITY = 30.0` 은 **마커 색상 정규화에만** 쓰인다(231 행의
`ColorRGBA`). 속도를 제한하지 않는다. 그리고 `get_ref_vel` 은 키를 정렬해 순회하므로 **구간 개수
제한이 없다** — 웨이포인트마다 한 항목을 두는 것이 코드 변경 없이 가능하다.

`load_ref_path` 는 x, y, psi, kappa 만 읽고 경로 CSV 의 `vx_mps` 는 **무시한다.** 속도는 오직
`ref_vel.yaml` 에서 온다.

### 직선 리프트가 실패한 진짜 이유

10 개 구간으로는 **코너 직전 제동 램프를 표현할 수 없다.** 30 km/h 구간이 코너 입구까지 30 을
유지하므로, 그것을 40 으로 올리면 차는 초과 속도를 그대로 안고 진입한다 — 조향 레이트
0.2136 rad/s 로는 돌 수 없다. 그래서 정체 16 건, 355.9 s 였다.

### 웨이포인트별 프로파일 (`tools/make_ref_vel.py`)

곡률 상한 `v = sqrt(ay/|kappa|)` + 전진 가속 + 후진 제동, 닫힌 트랙이므로 wrap 2 회 수렴. 인접
중복은 합친다(`get_ref_vel` 이 다음 키까지 값을 유지하므로 동일).

| 프로파일 | 예측 랩 | 속도 | 평균 | 구간 수 |
|---|---:|---|---:|---:|
| 현재 (10 구간) | 43.99 s | 22-30 | 27.5 | 10 |
| per-wp, ay 6.5 | 44.15 s | 20-40 | 28.0 | 314 |
| **per-wp, ay 8.0** | **40.90 s** | 21.5-40 | 30.1 | 315 |

`ay 6.5`(현재 프로파일의 함축값)로는 중립이다 — **이득은 코너 예산을 8.0 으로 올리는 데서 온다.**
그것이 전에 붕괴시킨 축이지만, 이번에는 **웨이포인트마다 제동 램프가 붙어 진입이 관리된다.**
그리고 8.0 은 텔레메트리가 증명한 20.09 의 절반도 되지 않는다.

`ref_vel.yaml` 대 `ref_vel_perwp_ay8.0.yaml` 4 레이스 진행 중. 판정은 완주 우선, 그다음 총시간.


## 2026-08-05 — 웨이포인트별 프로파일 실패: 40.90 예측, 60.63 실측. 페이스 축 종료

| 프로파일 | 완주 | 평균 | 정체 | **정상 랩 중앙값** |
|---|---:|---:|---:|---:|
| 현재 10 구간 | 4/4 | 325.9 s | 5 | **46.70 s** |
| per-wp, ay 8.0 | 3/4 | 404.4 s | 18 | **60.63 s** |

랩이 빨라지는 게 아니라 **14 초 느려졌다.** 이탈도 폭증했다 — 최대 |e_y| **4.700 m**, 중앙값 2.721
(기준선 1.2-1.8), **wp 284-287 집중**.

### 내 가정이 틀렸다

"제동 램프가 코너 진입을 관리한다" 고 썼는데, **제약은 진입 속도가 아니라 코너 호를 유지하는
능력**이다. 올바른 속도로 진입해도 그 곡률을 돌 조향 레이트가 없으면 그대로 밀려난다. 램프는
문제를 옮기지 못한다.

**따라서 페이스 축은 결정적으로 닫힌다: 코너 속도는 조향 레이트가 상한이고, 어떤 속도
프로파일로도 살 수 없다.** 이 세션에서 시도한 세 가지가 모두 같은 이유로 실패했다 —
직선 리프트(정체 16), 직선+제동 확대(정체 4-5/레이스), 웨이포인트별(정체 18).

**재시험 금지에 추가**: 속도 프로파일을 통한 페이스 개선 일체(균일 스케일, 구간별, 웨이포인트별,
제동 확대 병행 포함).

### 남은 유일한 물리적 레버: 요구 곡률

조향 레이트가 상한이므로 **곡률을 낮추면 같은 레이트로 더 빠르게 돌 수 있다.**

| 라인 | 길이 | \|kappa\|max | 물리 최소 |
|---|---:|---:|---:|
| 현재 `traj_top36_blend45` | 337.8 m | 0.2301 | 31.25 s |
| **`traj_megamax`** | **327.8 m** | **0.2099** | **29.83 s** |

`traj_megamax` 는 10 m 짧고 최대 곡률이 **9% 낮다.** 같은 조향 레이트로 그만큼 빠른 코너가
가능하다는 뜻이고, 40 초에 남은 유일한 경로다.

`config_megamax.yaml` 은 참조 경로 한 줄만 바꾼다. 처음 작성한 것은 `csv_path` 정규식이
**장애물 항목을 먼저 잡아** 궤적 파일을 장애물로 지정했다 — 그대로 돌렸으면 라인을 수백 개
장애물로 읽었을 것이다. 정확한 값 매칭으로 다시 쓰고 변경 줄 수가 1 인지 확인했다.
4 레이스 진행 중.


## 2026-08-05 — 우리 랩 실측 분포, 그리고 페이스 기전은 부스트다 (기본 off, 안전망 우회)

### 이 세션 정상 랩 367 개의 실제 분포

| | |
|---|---|
| 44 초 미만 | **2 개** |
| 44-47 초 | **144 개** |
| 47 초 이상 | 221 개 |
| 중앙값 | 47.76 |

설정별 최고 랩: `config_csvmt_ramps` **43.99**, `config_wpoff3` 44.87,
`ref_vel_straights40` 45.28, `config.yaml` 45.80, `config_officialline` 46.96.

**44-47 초대는 많다(144 개). 그러나 40.0 근처는 없고 최고가 43.99 다.** 내가 "정상 랩 중앙값"
으로만 보고해 온 것을 정정한다 — 최고 랩은 그보다 2-3 초 빠르다.
(`base` 의 20.32 초는 잘린 런의 부분 랩이다. 336 m 에 물리적으로 불가능.)

### 보드의 `BEST LAP` 은 단일 랩이다

평균 40 초가 필요한 것이 아니라 **한 랩만 40.0 이면 된다.** 그리고 시나리오에 `--boosts 2` 가
있다. 이 구분이 중요하다 — 지속 페이스와 단일 최고 랩은 다른 문제다.

### 페이스 기전을 찾았다: `use_boost_acceleration` (기본 False)

`mpc_controller.py:1074-1091`:

```python
if USE_BUG_ACC:
    if |v| > 44 km/h or (|v| > 38 and |max_delta| > 12deg):  acc = a_min/3*2   # 제동
    elif |v| > 41 km/h or |u[1]| > 10deg:                    acc = a_max
    else:                                                    acc = 500.0      # 부스트
```

즉 **조향이 10 도 미만일 때 가속 500 으로 41 km/h 까지 밀고, 코너에서 스스로 물러난다.** 이것은
내가 웨이포인트 프로파일로 만들려던 "직선 전속 + 코너 감속" 과 같은 것인데, **웨이포인트 인덱스가
아니라 조향각으로 게이팅한다.** 조향각이 실제 제약이므로 내 방식보다 정확히 맞다.

41 km/h = 11.4 m/s 를 유지하면 336 m 가 **29.5 초**다. 현재 프로파일은 22-30 km/h 다.
`BUG_VEL = 40.0` 은 config 의 `v_max` 와 같으므로 속도 상한 이득은 없고, **가속 500 이 차이를
만든다.**

### 대가: 안전망을 우회한다

`mpc_controller.py:588-596` — `USE_BUG_ACC` 가 켜지면 발행 토픽이
`/boost_commander/command`(`AckermannControlBoostCommand`)로 바뀌고 **`control_cmd` 도
`control_cmd_raw` 도 만들지 않는다.** 따라서 `lidar_guard` 와 `stuck_recovery_controller` 가
입력을 받지 못한다.

**부스트 = 페이스를 얻고 복구를 잃는다.** 아카이브의 `boost_abandoned`,
`boostfix_inert`, `width1.50_boost_disabled` 가 이 결론의 흔적으로 보인다.

그리고 보드가 말하는 것은 **완주가 순위를 만든다**는 것이다(5 위가 최고 랩 56.7 초로 19 위를
이긴다). 우리 문제는 완주 5/6 이다. **복구를 버리고 페이스를 사는 거래는 보드 구조와 반대
방향이다.**

다만 `BEST LAP` 이 단일 랩 통계라는 점은 남는다 — 이기지 못한 레이스에서도 최고 랩은 기록된다.
한 제출물로 두 가지를 동시에 낼 수는 없으므로, **부스트는 "완주를 포기하고 최고 랩만 노리는
별도 제출" 로만 의미가 있다.** 예선이 9/1 까지이고 제출 횟수 제한이 확인되지 않았으므로 선택
가능한 전략이다.

### `traj_megamax` 첫 레이스

d1(기준선) 5 랩 283.8 s 정체 0, d2(megamax) **랩 0**(기동 실패). 두 번째 레이스는
`no car reported ready inside 90 s` — 90 s 상한이 이번에는 짧았다. 표본이 부족하므로 판정 보류.


## 2026-08-05 — 부스트는 규칙 위반이다. 그리고 `config_csvmt_ramps` 가 두 지표에서 낫다

### 부스트: 시험하지 않는다 (성능이 아니라 규칙 문제)

`control/mpc.launch.xml:16-23` 의 주석이 이미 경고한다 — *"실제 대회에서 허용되지 않을 수 있어
기본 off"*. 그리고 공식 SW 규칙이 명시한다:

> 코스의 벽이나 타차량에 대한 충돌 등이 발생한 경우 · **부정한 가속도가 입력된 경우**,
> 페널티로 일정 시간 속도가 제한됩니다

`acc = 500.0 m/s^2` 는 명백히 "부정한 가속도" 다. **충돌과 동일한 페널티를 받는다.** 즉 부스트는
페이스를 사는 것이 아니라 페널티를 사는 것이다. 측정할 필요가 없다.

**재시험 금지에 추가: `use_boost_acceleration`.** 사유는 규칙 위반.

이것으로 페이스 축이 완전히 닫힌다 — 속도 프로파일 3 회 실패, 조향 레이트 상향 실패, 부스트는
규칙 위반, 라인 곡률만 표본 부족으로 미결.

### `config_csvmt_ramps` 재평가 — 우리 최고 랩 43.99 초를 낸 설정

| 레이스 | 결과 | 정체 | 최고 랩 |
|---|---|---:|---:|
| h2h-00 | 6 랩 357.3 s | 4 | **43.99** |
| h2h-01 | 6 랩 333.5 s | 4 | (누적 표기) |
| h2h-02 | 6 랩 288.6 s | 2 | 44.12 |
| tourney-02 | 6 랩 405.4 s | 5 | 46.56 |

**4 레이스 전부 6 랩 완주(4/4).** 챔피언은 5/6 이고 최고 랩 45.80 이다.

보드는 **완주로 순위를 매기고 `BEST LAP` 을 별도 열로 보여준다.** 그 구조에서 `csvmt_ramps` 는
**두 지표 모두 낫다** — 완주 4/4 대 5/6, 최고 랩 43.99 대 45.80. 약점은 평균 총시간
(326-405 s 대 283.85 s)뿐이다.

앞서 나는 "직접 대결 승패"(챔피언 2 승 대 1 승)로 유지 판정했다. 그런데 보드는 challenger 를
**다른 팀**과 붙인다 — 우리 두 설정끼리의 승패가 아니라 임의 상대에 대한 완주 확률과 최고 랩이
기록된다. **그 기준으로는 판정이 뒤집힐 수 있다.**

6 레이스로 다시 잰다. 기준을 미리 고정한다: **완주율이 앞서고 최고 랩도 앞서면 교체**, 평균
총시간 열세는 감수한다(보드가 완주로 순위를 매기므로).


## 2026-08-05 — 솔로로 재니 설정 판정이 뒤집혔다. 그 코너의 원인도 특정됐다

### 이전 설정 비교는 전부 충돌 운을 재고 있었다

사용자 지적: *"아 저거는 3대 돌린거라 충돌 복구"*. 맞다. `config_csvmt_ramps` 의 총시간
326-405 초는 3 대 레이스의 복구 랩을 담고 있었고, 같은 런의 랩 열은 `49.6 60.4 44.5 44.6 44.5
44.1` 이다 — 트래픽이 걷히면 44 초대다. **다차 레이스의 평균 총시간으로 설정을 비교하면 페이스가
아니라 충돌 운을 재는 것이다.**

같은 하네스·같은 시나리오·솔로, n=3:

| 설정 | 완주 | 평균 총시간 | 깨끗한 랩 | 개별 |
|---|---:|---:|---:|---|
| `config_csvmt_ramps` | 3/3 | 287.0 | 44.2-44.8 | 283.2 / 283.6 / 294.1 |
| 챔피언 `config.yaml` | 2/3 | 371.5 | 45.9-46.3 | 278.7 / 367.1 / 468.6 |

**네 지표 모두 후보가 앞선다.** 앞선 "챔피언이 평균 총시간에서 낫다(283.85 대 326-405)" 판정을
철회한다 — 솔로 수치와 3 대 수치를 맞대고 있었다.

### 그런데 통째로 승격하면 안 됐다 — 변수 두 개가 섞여 있었다

`config_csvmt_ramps` 는 `config.yaml` 과 **두 곳**이 다르고, 두 번째는 `config.yaml` 자신의
주석이 이미 유죄 판정을 내려뒀다:

> `use_path_constraints_topic: true` … *런당 랩 하나가 망가지고 복구가 5 배 느려진다*

솔로 랩이 정확히 그렇다: **런당 망가진 랩 하나, 70.30 / 60.63 / 61.24.** 즉 후보는 *빠른 속도
프로파일 + 이미 유해하다고 측정된 라우팅* 의 조합이었다.

`config_ramps_nocst` = 프로파일만, 라우팅 off. 측정 (n=3, 4 번째 진행 중):

| 런 | 총시간 | 정체 | 랩 |
|---|---:|---:|---|
| 1 | 282.3 | 2 | 46.27 44.70 44.58 44.16 44.37 |
| 2 | **268.1** | 1 | 46.12 44.68 44.36 44.49 44.29 |
| 3 | 325.3 | 3 | 46.32 44.34 44.77 **90.39 55.54** |

**268.1 초는 이번 세션 최고이고 제출본의 검증값 278.23 초를 앞선다.** 다만 3 번 런에 90 초 랩이
있다 — 라우팅을 끄는 것은 망가진 랩을 **없애지 않고 드물게** 만든다.

### 반복되는 그 코너: 원인은 이탈 지점이 아니라 그 6 waypoint 앞이다

사용자 지적: *"저 코너 하나 계속 충돌나는거도 있고"*. 이탈은 wp 284-287 과 133-134 에 몰린다.
나는 그 지점들의 **기하** 를 조사하며 무죄 판정을 내렸다 — wp 117 이 더 급하고(|kappa| 0.228)
wp 201 이 더 좁은데(4.258 m) 거기선 안 멈춘다는 이유로. **그 판정이 틀린 이유:** 구속조건은
조향각이 아니라 조향 **레이트** 다. 완만한 코너라도 진입 전이가 급하면 초과한다.

    d(delta)/dt = v · (dkappa/ds) · L / (1 + (kappa·L)²)   대   0.35/1.639 = 0.2135 rad/s

| | |
|---|---|
| 천장 초과 지점 | 0, 1, 66, 67, 76, 112-115, 164, 194-199, 206-211, 216, 219, 220, 229, 230, 234, 237-239, 244-249, 252-254, **275-279, 281**, 317, 318, 337-340, 348, 350 |
| wp 280-292 비율 | 0.8 0.9 0.5 0.1 0.0 0.1 0.4 0.7 0.6 0.5 0.4 0.2 |

**wp 275-281 이 천장을 넘거나 붙어 있고, 이탈은 그 6 지점 뒤에서 정점을 찍는다.** 차가 그
구간에서 라인을 놓치고 누적 횡오차가 코리도 경계에 닿으면 QP 가 infeasible 이 된다 — 원인이
아니라 결과다.

**부수 결과: 133-134 는 다른 고장이다.** 창 118-140 에는 천장 초과 지점이 **하나도 없다**. 두
hotspot 을 한 원인으로 묶으려던 시도를 철회한다.

`tools/rate_cap_ref_vel.py` 는 shipped 프로파일을 **초과 지점에서만 낮춘다**:

| 범위 | 초과 지점 | 변경 지점 | 예측 랩 비용 |
|---|---:|---:|---:|
| 전역 | 54 | 139/351 | **+3.15 s** (사실상 랩 전체 감속) |
| hotspot 한정 | 6 | 34/351 | **+0.60 s** |

국소만 시험한다. **라인 평활화 축은 이미 닫혀 있다** — `config.yaml:50` 에
`smoothing_distance: 5 -> much worse (76.59s vs ~50s baseline), reverted`.

### 복구가 나쁜 이유: 탈출 방향을 동전 던지기로 고른다

사용자 지적: *"충돌 복구를 너무 못하네"*. `stuck_recovery_controller.cpp` 는

    recovery_steering_ = -recovery_steering_;   // 시도마다 무조건 부호 반전, +0.45 로 시작

차가 어느 쪽에 끼었는지 **모른다**. 시도의 절반이 장애물 쪽으로 조향한다. 후진 1.8 초 + 쿨다운
1.2 초이므로 챔피언의 113 초·119 초 랩은 약 20 회 시도이고 그 절반이 낭비다.

노드는 이미 MPC 의 nominal 명령을 받고 있고, MPC 는 밀려나는 반대쪽으로 조향한다. 따라서
**정지 직전의 유의미한 nominal 조향이 빈 쪽을 가리킨다.** 검출 시점에는 읽을 수 없다 — QP 가
infeasible 이면 조향 명령이 0 에 가깝고 그게 바로 정체 시점이다. 그래서 nominal 제어 중에
latch 한다.

후진 시 조향 부호와 탈출 방향의 관계는 단정하지 않는다(바퀴를 돌린 후진은 뒤를 한쪽으로,
앞을 반대쪽으로 보낸다). `RECOVERY_DIRECTED` = 0 무작위 교대(현행 검증 동작) / 1 = +sign /
2 = -sign, **셋 다 측정한다.** 기본 0.

**같은 파일의 기존 수정 두 개가 기본 OFF 다.** 그중 하나의 주석은 *144 초 정지 동안 nominal
명령이 stuck 게이트를 만족한 표본이 16.6% 뿐이고 복구는 후진을 한 번도 하지 않았다* 는 측정을
담고 있다(`RECOVERY_CREEP_ESCALATION`). 다른 하나는 `RECOVERY_STRAIGHT_ESCAPE`. **검증된
제출본에서는 둘 다 돌지 않는다** — 진단은 트리에 있는데 차는 같은 방식으로 계속 실패한다.

### 두 트랙 동시 강화학습: 지금 형태로는 철회

3 대 중 한 대를 `tiny_lidar_net` 으로 돌렸다(`--methods mpc,mpc,tiny_lidar_net`).

    d1(MPC)  랩 229.8 173.9 / 0 랩 / 0 랩
    d2(MPC)  랩 298.3 / 441.6 / 172.7 175.4
    d3(AI)   0 랩, 매 레이스

**AI 차가 한 랩도 못 돌아 경쟁자가 아니라 굴러가는 장애물이 되고, 공유하려던 SW 측정을 파괴한다.**
한 레이스로 두 트랙을 재려면 AI 차가 먼저 랩을 완주할 수 있어야 한다.

### AI 트랙: 스캔으로 결정될 수 없는 양을 타깃으로 삼고 있었다

배선은 정상이다 — 노드는 `/scan` 을 구독하지만 launch 가 `/sensing/lidar/scan` 으로 리맵한다
(모델을 의심하기 전에 확인함). 문제는 타깃이다. 추출기 자신의 주석:

> *가속도 타깃은 현재 속도 없이는 학습 불가능하다*

추출기는 그래서 측정 속도를 저장하지만 **아무도 쓰지 않는다** — 입력은 750 점 스캔뿐이다. 그리고
노드는 예측된 가속도를 적분해 속도를 만든다(편향 누적).

MPC 의 `control_cmd` 에는 `longitudinal.speed` 가 이미 들어있다. 속도를 직접 회귀하면 입력 차원·
아키텍처·가중치 변환이 모두 그대로다. `TLN_SPEED_TARGET` (데이터) + `TLN_DIRECT_SPEED` (노드),
둘 다 기본은 기존 규약. 규약 불일치는 "이상하게 달리는 차" 로 나타나므로 추론하지 않고 명시한다.
**속도를 입력으로 넣는 쪽은 후속으로 남긴다** — 규칙은 휠 오도메트리를 허용하고 네트워크는 아직
쓰지 않는다.

**전문가 데이터는 매일 눈앞을 지나갔다.** 깨끗한 MPC 솔로 런이 곧 BC 시연이고 하네스에 레코더가
있다. 두 가지가 버리고 있었다: (1) `run_autoware.bash` 가 `rosbag:=` 를 넘기지 않아 모든 런이
`rosbag: false`, (2) 화이트리스트에 **라이다 스캔과 속도 리포트가 없다**. 둘 다 고쳤고 기본은
여전히 off(측정 런에 부하 없음), 셋 다 `aichallenge_submit` 밖이라 제출물에 들어가지 않는다.

### 도구 버그: 이번 세션 내내 명령을 조용히 날려온 원인

`Exit code 144` 는 `pkill -f chain_dual.sh` 가 **자기를 실행하는 셸의 명령줄까지 매치해 부모를
죽이는** 것이었다. 같은 명령 뒤쪽의 heredoc 파일 생성이 통째로 사라진다. 자기 참조를 피하면
정상이다: `pkill -f 'chain_du[a]l'`.

`lib/data.py` 패치도 조용히 죽을 뻔했다 — 삽입이 `except` 안 `raise` 직후 12 칸에 들어가
**도달 불가 코드**가 됐다. `ast.parse` 는 통과한다. 인덴트를 내린 뒤 AST 로 `__init__` 본문
최상위 문장임을 단정해서 확인했다.


## 2026-08-23 — wp 133-134 의 원인을 특정했다. 그리고 그걸 고치려던 A/B 는 하네스가 망가져 결론 없음

### 먼저: 이 머신의 평가 이미지는 SW 제출본이 아니었다

`aichallenge-2025-eval:latest` 는 8/7 17:29 에 **AI 타르볼**로 구워져 있었다(런치 md5
`56701a29`, `control_method=tiny_lidar_net`, `config_mid45.yaml` 존재). 그대로 `make eval` 을
돌렸으면 AI 차를 재고 SW 제출본이라고 보고했을 것이다. SW 타르볼(`e40bd2b7`)로 `--no-cache`
재빌드해 이미지 안의 런치가 `f118481b` = SW 판임을 확인한 뒤에 측정했다.

**소스 트리는 MPC 경로가 타르볼과 바이트 동일하다.** 8/6-8/11 의 AI 커밋들이 건드린 것은
`tiny_lidar_net_*`, `pilot_net` 가중치, `config_mid45.yaml` 뿐이고 MPC 코어·복구 노드·궤적·
`ref_vel`·런치 기본값은 차이 0 이다. 그러므로 소스 트리를 마운트하는 3 대 하네스의 판정은
이 타르볼을 그대로 서술한다.

### 벽 페널티는 사고가 아니라 매 랩 일어나는 편류의 꼬리다

공식 단독 600 s, ROSBAG=1: 6/6, 279.49 s, 그런데 **한 랩만 54.94 s** 이고 벽 페널티 6.5 s
한 건. 세 런 모두 같은 자리다 — 랩 진입 후 **17.0-17.3 s**, 랩만 5/5/4 로 다르다.

bag 자세를 라인에 투영한 여섯 랩 (부호 + = 좌, 라인 기준 m):

| 랩 | wp128 | wp131 | wp133 | wp134 | wp136 | wp139 | 결과 |
|---:|---:|---:|---:|---:|---:|---:|---|
| 1 | +0.29 | +0.80 | +1.07 | +1.15 | +1.20 | +0.97 | 정상 |
| 2 | +0.39 | +1.17 | +1.50 | +1.58 | +1.54 | +1.11 | 정상 |
| 3 | +0.45 | +0.87 | +1.04 | +1.06 | +0.96 | +0.65 | 정상 |
| 4 | +0.45 | +1.39 | +1.98 | **+2.24** | **+2.69** | +2.21 | **벽, 6.5 s** |
| 5 | +0.43 | +0.88 | +1.03 | +1.04 | +0.90 | +0.56 | 정상 |
| 6 | +0.66 | +1.31 | +1.54 | +1.57 | +1.45 | +0.98 | 정상 |

wp 131 진입 속도는 정상 랩과 사고 랩이 구분되지 않는다(7.5-8.2 대 7.9). **속도가 아니라
횡위치다.** 점유격자 실측으로 그쪽 벽은 라인에서 3.7-3.9 m 이고, 충돌 자세에서 가장 가까운
장애물까지 1.10 m — 차폭 반 0.75 m 에 요를 더하면 남지 않는다. 속도는 1 초에 8.10 → 0.01 m/s,
**−8.1 m/s²** 로 제동 한계 1.6 의 다섯 배이므로 감속이 아니라 접촉이다.

즉 고칠 대상은 드문 사고가 아니라 **중앙값이 이미 벽까지 3 분의 2 를 가 있는 분포**다.
`config.yaml:51` 이 지목하고 2026-08-03 이 "다음 표적" 이라고 적어둔 wp 129-134 가 이것이고,
그 해부는 그날 이후 AI 트랙으로 넘어가면서 실제로는 수행된 적이 없다.

**도구 함정 하나**: `Map.data` 는 int8 이고 1 이 주행가능이며 **행 0 이 이미지 위쪽**이다.
네 가지 관례를 라인에 대조해야 갈린다 — `H-1-(y-oy)/res` + `data==1` 만 350/350 을 free 로
읽고 나머지는 0.49 / 0.51 / 0.00 이다. 처음에 이걸 틀려서 "wp 134 의 여유가 0.02 m" 라는
말이 안 되는 표를 얻었다.

### 라인 국소 이동 A/B: 결론 없음, 하네스 결함

이 구역에서 이미 소진된 것과 겹치지 않는 유일한 축이라 시도했다(`max_width=4.0` 무차이,
`s6=18.0` 은 구간 속도라 금지, `steer_rate_max=0.50`·`wp_id_offset=3`·`smoothing_distance=5`
전부 훨씬 나쁨). `tools/shift_line_134.py` 로 wp 124-144 에 raised-cosine 범프를 넣으면
곡률은 **내려가고**(구간 최대 0.167 → 0.150) 길이는 +0.26 m 다.

`tools/shift_ab.py` 로 3 팔 × 2 라운드를 교대로 돌렸는데 **여섯 런 전부 5 랩에서 끊겼다**
(6 랩 완주 0/6). 총시간 비교가 성립하지 않는다. 랩당 최대 이탈 중앙값만 보면 기준선 1.51 →
r06 0.95 로 방향은 맞지만 최댓값은 오히려 나쁘고(2.57 → 2.93), r09 한 런은 444 s 로 무너졌다.

**n=2 로는 아무것도 말할 수 없고, 5 랩 절단은 측정 자체를 무효로 만든다. 이 근거로 제출본을
바꾸지 않는다.** 다음에 할 일은 `shift_ab.py` 의 종료 판정(`latest link updated` 조기 탈출로
의심)을 고치고 팔당 4 런 이상으로 다시 재는 것이다. 판정량은 랩당 최대 이탈(레이스당 6 표본)
이지 사고 발생 여부가 아니다.


# 2026-08-25 — 접는다. 무엇이 남아 있는지만 기록한다

사용자 결정으로 이 대회의 작업을 중단한다. 재개할 사람을 위해 상태만 남긴다.

## 그대로 두고 가는 것

| | |
|---|---|
| 제출물 | `submit/aichallenge_submit.tar.gz`, md5 `e40bd2b704ba10727b7c5de2e2ccab9f` |
| 검증 | 타르볼에서 구운 이미지로 공식 단독 600 s **6/6 완주**, 279.5 s (2026-08-23 재확인) |
| 공식 3 대 형태 | **18/18 슬롯이 6 랩 완주**, 최악 완주자 327.6 s / 600 s (2026-08-06) |
| 예선 마감 | 9/1. **올리는 데 몇 분이면 된다** — 포털 Login → Submit Code |

**즉 제출물은 완성돼 있고 검증돼 있다.** 접는 것은 개선 작업이지 제출물이 아니다. 마감 전에
마음이 바뀌면 올릴 것이 이미 있다.

## 왜 개선을 접는 것이 옳은가

8/2 이후 시험한 모든 개입이 기준선을 이기지 못했다. 페이스 축은 물리로 닫혔고, 구간 속도
인하는 복구를 깨서 금지, `max_width` 확대는 무차이, 라인 국소 이동은 아래대로 무차이다.
남은 손실은 레이스당 약 10 s 인데 **완주 예산은 600 s 에 주행 279 s 로 여유가 320 s** 라
그 10 s 는 순위가 아니라 동점 처리에만 닿는다.

## 라인 국소 이동(wp 124-144): 무차이. 축을 닫는다

bag 기준 재채점, 랩 2-6 합계:

| 배치 | base | r06 | r09 |
|---|---:|---:|---:|
| a (n=2/팔) | 229.19 | 261.59 | 310.61 |
| b (n=3 / n=1, 중단) | 233.61 | 265.18 | — |

최대 이탈 중앙값은 base 1.47-1.51 대 r06 0.95-1.14 로 **겨눈 지표는 내려간다.** 그런데
총시간은 매번 base 가 앞선다. `wp_id_offset=3`·`steer_rate_max=0.50`·`s6=18.0` 과 같은
서명이고, 그 셋은 전부 기각됐다. **`config_shift_r06/r09` 와 `traj_top36_blend45_r06/r09.csv`
는 남겨두되 채택하지 않는다.**

## 확인되지 않은 채로 남는 것

우리 실제 보드 순위. 공개 페이지는 제출 횟수만 준다(**メタモン, 88 회, 7 위**). 머리말의
"레이팅 734, 코스 2 내 22/60" 과 "KSK 6 랩 282.155 s" 는 로그인 없이는 확인 불가이고
**미확인 표시가 붙은 채로 남는다.** SIM 결선이 상위 32 이므로 이 숫자가 재개 여부를
결정하는 유일한 입력이다.

## 이번에 새로 알아낸 것 중 살아남는 것

* 벽 페널티의 원인은 wp 128-142 에서 **여섯 랩 전부** 같은 쪽으로 1.0-1.6 m 밀리는 편류이고,
  한 랩이 2.7 m 까지 가면 닿는다. 사고가 아니라 분포의 꼬리다. (2026-08-23 항목)
* `Map.data` 는 int8, 1 = 주행가능, **행 0 이 이미지 위쪽**이다.
* 이 저장소의 평가 이미지는 마지막으로 구운 타르볼을 담는다. **SW 를 재려면 SW 타르볼로 먼저
  다시 구울 것** — 8/7 에 AI 판이 구워져 있었다.
* `race_outcome` 은 `ROSBAG=1` 일 때 완주를 DNF 로 읽는다(종료 처리 68-79 s 대 창 26-62 s).
  bag 으로 채점하려면 `tools/rescore_from_bag.py`.
