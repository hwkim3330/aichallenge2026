# AIC RacingKart 36초 목표 인수인계

갱신: 2026-07-29 (KST)

## 오늘 확인된 것 — 앞의 인수인계에서 정정해야 하는 부분

### 1. 기준선 설정은 문서보다 `output/20260728-172948/d1/autoware.log`가 정확하다

그 로그에는 실행 당시의 config가 주석까지 그대로 찍혀 있고, 이전 인수인계의 서술과 여러 곳이 다르다. 47.28초를 낸 실제 설정:

| 항목 | 기준선(검증됨) | 오늘 아침 작업 트리 |
|---|---|---|
| `v_max` | **33.0** (실규정 상한 35) | 40.0 |
| `delta_max_deg` | **36.0** (차량 한계 0.64 rad) | 32.0 |
| `max_width` | **3.0** | 6.0 |
| `Q` | **[1e6, 1e8, 1.85e6]** | [3e6, 9e7, 1e5] |
| `QN` | **[1e6, 1e6, 1e4]** | [1e6, 1e3, 1e4] |
| ref_vel 직선 | **30** | 40 |

즉 작업 트리에는 검증 안 된 실험값이 여럿 섞여 있었다. **새 실험 전에 먼저 이 로그에서 기준선을 복원할 것.**

그 로그에 남은 다른 확정 사실들:

- `a_max`: 이 차량의 물리적 가속 능력이 **약 1.37 m/s²**(AWSIM handicap)이고, `a_max=2.7`로 올려도 랩타임이 동일했다. 1.35가 실제 최적이며 **가속 한계는 튜닝으로 못 넘는다.**
- `a_min=-2.7`, `N=30`, `R=10000`, `ay_max=28`, `use_max_kappa_pred=false`: 전부 시도했고 전부 무변화. 다시 하지 말 것.
- `smoothing_distance=5`: 76.59초로 크게 악화.

### 2. `load_ref_path`는 psi/kappa도 읽지만 `create_ref_path`가 버린다

```python
wp_x, wp_y, _, _ = load_ref_path(...)
```

psi/kappa는 재샘플링된 x/y에서 다시 계산된다. 그래서 **경로 CSV의 psi가 공식과 90° 달라도 무해하다** — 앞 인수인계가 출발 실패 원인으로 지목한 것은 원인이 아니다.

### 3. CSV 속도 분기에 단위 버그가 있었다 (수정함)

`mpc_controller.py`의 `use_csv_speed_profile` 분기가 config의 km/h `v_max`를 변환 없이 `update_v_max()`에 넘겨, MPC 속도 상한이 11.1 m/s가 아니라 **40 m/s(144 km/h)**가 되고 있었다. 다른 두 호출부는 `kmh_to_m_per_sec()`로 변환한다. 수정 완료.

### 4. 코너 속도는 공짜가 아니었다 — 실패한 실험

36초 주행 telemetry를 구간별로 재보면 우리 프로파일이 거꾸로 붙어 있다:

| 구간 | 기준선 | 36초 주행 min..mean |
|---|---|---|
| s4 (wp155-190) | 22 | 31.0 .. 33.1 |
| s6 (wp265-290) | 22 | 30.4 .. 31.3 |
| s8 (wp320-335) | 25 | 33.3 .. 33.7 |
| s5 (wp190-265) | 30 | 28.6 .. 30.3 |

그래서 s4/s6를 30, s8을 31로 올려 돌렸더니 **47 m 지점에서 완전 정지**(조향 최대 포화 0.93 rad, 가속 명령 최대). 그 속도들은 36초 주행이 **자기 325 m 라인에서** 낸 값이고, 공식 346 m 라인의 코너 형상에서는 통과되지 않는다.

교훈: telemetry 속도는 그 라인과 묶여 있다. 속도만 옮기는 건 안 된다.

### 5. 그래서 실질 차이는 라인 길이다

| | 길이 | \|k\|p95 | 폐곡선 갭 |
|---|---|---|---|
| 공식 final / ver2 / ver3 / ver4 | 346.1 / 345.5 / 346.0 / 344.6 | 0.185~0.194 | 0.00 |
| **traj_top36_line.csv** | **323.6** | **0.187** | **0.00** |
| traj_top36_telemetry.csv | 325.1 | 0.197 | 0.51 |
| traj_top36_fitted.csv | 328.6 | 0.206 | 0.60 |

공식 경로 4개 버전은 서로 1.5 m 안쪽이라 **공식 저장소에 더 빠른 라인은 없다.** 우승자는 트랙 폭을 써서 mincurv 안쪽을 잘랐고, 그 20 m가 남은 격차의 실체다. `traj_top36_line.csv`는 폐곡선 갭 0.00에 곡률 p95가 공식보다 오히려 낮아 기하학적으로 멀쩡한 후보다.

### 6. eval은 벽 복구가 없다 — 제출물 쪽 복구 노드를 배선했다

`aichallenge/simulator_scripts/eval.sh`는 `--wall-recovery off`로 시뮬레이터를 띄운다. 벽에 닿으면 그대로 끝이고, 오늘 두 번의 실패가 정확히 그 형태였다(멈춘 자리에서 세션 종료까지 정지).

`stuck_recovery_controller` 패키지는 **이미 `aichallenge_submit`에 있었지만 아무 launch도 띄우지 않는 죽은 코드**였다. 배선함:

- `control/mpc.launch.xml`: `output_control_cmd` 인자 + remap 추가
- `reference.launch.xml`: mpc 출력을 `nominal_control_cmd`로 보내고 그 사이에 노드 삽입
- `aichallenge_submit_launch/package.xml`: `exec_depend` 추가

우리 트리의 구현(244행)은 upstream `agent/recovery-supervisor` 브랜치(138행)보다 앞서 있다 — 감지 0.4초, 깊은 후진, 쿨다운, 상호 양보 교착 시 크리프 탈출, 다중차량용 도메인별 복구 조향. upstream에서 되가져올 것 없음.

## 오프라인 랩타임 모델 (`tools/lap_time_model.py`)

컨트롤러의 재샘플링·스무딩·곡률·구간 조회를 그대로 재현해서, 빌드+eval 한 사이클(약 10분) 없이 후보를 점수화한다.

검증:

| 조합 | 예측 | 실측 |
|---|---|---|
| 공식 + 기준선 속도 | 45.31 | **47.28** |
| telemetry 라인 + telemetry 속도 | 36.98 | **36.319** |

telemetry 쪽이 정확한 이유는 그 속도가 실제로 달성된 값이라서다. `ref_vel` 예측은 약 **+2초** 낙관적이고, 그 차이가 MPC 추종 손실이다. **ref_vel 후보를 볼 때는 예측에 2초를 더해 읽을 것.**

주의: 이 모델의 "a_max 손실" 항목은 오해를 부른다. 차량이 어차피 1.37 m/s²에 걸리므로 급변동 프로파일이 추가로 손해를 보는 게 아니다. 비교용으로만 쓸 것.

## 현재 작업 트리 상태

- config: 공식 기준선 값 복원 + `csv_path`를 `traj_top36_line.csv`로 변경, `use_csv_speed_profile: false`
- ref_vel: 기준선 속도(30 / s4 22 / s6 22 / s8 25), **wp_id를 새 라인 인덱스로 재매핑**(좌표 이격 최대 1.73 m, 단조 유지, wp 1~55는 이격 0.00으로 출발 구간 동일)
- 예측 42.21초 → 실측 44초 전후 기대
- 복구 노드 배선 완료
- 실험 파일 다수 untracked이므로 broad clean/reset 금지

## 2026-07-29 오후: 통제된 실측 4건

앵커에서 변수를 하나씩만 바꿔 측정했다. **어느 것도 앵커를 넘지 못했다.**

| 실행 | 변경 | 평균 | 최고 | 복구 |
|---|---|---|---|---|
| 앵커 | – | **47.28** | 46.96 | 0 |
| 조향 | `delta_max_deg` 36 → 22 | 47.75 | 47.07 | 0 |
| 라인 | `traj_top36_fitted` (328.6 m) | 96.08 | **45.43** | 43 |
| 라인+여유 | `traj_top36_widened` (331.6 m) | 52.98 | 46.21 | 4 |

### 조향 gain 보정은 랩타임과 무관하다 (기각)

`create_mpc()`가 `steer_rate_max`는 gain(1.639)으로 나누는데 `delta_max`는 원본을 쓰는 불일치는 **실재한다** — 36°면 MPC가 0.628 rad을 계획하고 gain 후 1.030 rad이 되어 차량 0.64에서 잘린다. 그런데 gain 보정값 22°로 맞춰도 47.75초로 **0.47초 느려졌다.** 기하학적으로 필요한 조향이 `atan(0.236×1.087)=14.4°`뿐이라 상한이 병목이 아니다.

그리고 이것은 해석 하나를 뒤집는다: 실패한 실행들에서 본 조향 포화(0.93 rad)는 **원인이 아니라 라인을 벗어난 뒤의 증상**이다.

### 짧은 라인은 최고랩을 주지만 평균을 잃는다

`traj_top36_fitted`는 깨끗한 랩에서 **45.43초**로 앵커 최고(46.96)보다 1.53초 빠르다. 그러나 최소 여유가 반폭(0.75 m) 대비 **0.76 m = 1 cm**뿐이라 6랩 중 2랩이 갇힘으로 망가지고 평균 96초가 된다. 조향 스윙·요각·추종오차가 그 1 cm를 먹는다.

`tools/widen_line.py`로 여유 부족 지점만 거리장 gradient 방향으로 밀어 331.6 m / 최소 여유 1.12 m를 만들었다. **복구 43 → 4회로 크게 개선**되었지만 3랩이 여전히 57~64초이고, 최고랩은 3 m 대가로 46.21초로 후퇴했다. 평균 52.98로 앵커에 못 미친다.

### 도구

- `tools/lap_time_model.py` — 컨트롤러의 재샘플링·곡률·구간조회를 재현해 후보를 1초에 채점. 검증: telemetry 라인+속도 36.98 예측 vs 실제 36.319. `ref_vel` 예측은 **+2초** 낙관적(MPC 추종 손실).
- `tools/widen_line.py` — occupancy grid를 차 반폭으로 침식한 여유장에서, 부족한 지점만 밀어냄. 길이·곡률·여유를 함께 보고한다.

### 차폭 검사가 라인 하나를 탈락시켰다

`traj_top36_line`(323.6 m)은 **18개 지점에서 차보다 좁은 틈**을 통과하라고 지시한다(최소 여유 0.14 m). 오늘 오전 내내 갇힌 원인이 이것이고, 중심점만 보는 occupancy 검사로는 안 잡힌다. **새 라인은 반드시 반폭 침식 여유로 검증할 것.**

### `use_csv_speed_profile` 경로는 쓸 수 없다

v_ref가 정상 로드되어도(`543 -> 351 points, max=35.5 km/h`) MPC 명령이 **2.55 m/s(9.2 km/h)에 고착**된다. 두 실행에서 동일한 값이 재현됐다. 단위 버그(km/h를 `update_v_max`에 그대로 넘겨 상한이 40 m/s가 되던 것)는 고쳤지만 고착은 남아 있다. 구간 방식(`ref_vel`)만 쓸 것.

### 빌드 경로를 혼동하지 말 것

- **eval**: 제출 tar를 이미지에 굽는다 → `./docker_build.sh eval`
- **dev/online3**: compose가 `./aichallenge:/aichallenge`를 마운트 → `install/`이 **호스트 산출물**이므로 **`make autoware-build` 필수**. `docker_build.sh`만으로는 반영되지 않고, MPC가 없는 config를 찾다 `FileNotFoundError`로 죽는다.

그리고 그 죽음의 원인이 로그에서 가려진다 — `mpc_controller.py`의 `main()`이 생성자 예외 뒤 `node.stop()`을 호출해 `UnboundLocalError`가 원래 예외를 덮는다. 오늘 두 번 이걸로 헤맸다. **아직 안 고쳤다.**

### 끊겨 있던 배선 (이음)

- `MPC_CONFIG_FILE`/`control_method`/`input_source`가 `aichallenge_submit.launch.xml`에서 선언·전달되지 않아 버려지고 있었다 → 이었다. 이제 도메인 1/2/3에 서로 다른 config로 3대를 띄울 수 있다(`online3.sh`, 실측 확인).
- `stuck_recovery_controller`는 `aichallenge_submit`에 있으면서 어떤 launch도 띄우지 않는 죽은 코드였다 → 이었다. `eval.sh`는 `--wall-recovery off`라 이게 없으면 벽 접촉이 곧 랩 손실이다.
- `use_boost_acceleration`이 launch에 `false` 하드코딩이던 것을 인자로 승격(기본 off). 시뮬레이터가 부스트 2회를 주고, MPC 게이트가 44 km/h 이하·조향 12° 이하에서 켜지므로 출발 직선에 정확히 걸린다. 대회 허용 여부는 불명.

### 접지는 병목이 아니다

1등 telemetry의 실측 횡가속도는 p95 15.41 / 최대 20.09 m/s². 그 한계로 계산한 구간별 허용 최저 속도는 어디서나 **29~66 km/h**로 우리 명령값 22~30보다 높다. 즉 속도는 여유가 있다.

## 2026-07-29 저녁: 46.53초 달성 (앵커 −0.75초)

**현재 최선 = `traj_top36_blend45.csv` + 평평한 기준선 속도.** 실측:

```
47.82 / 46.36 / 46.32 / 46.17 / 46.05 / 46.45   평균 46.53  최고 46.05  복구 0회
```

라인 조건이 세 번의 실패로 정리됐다. **짧게만 만들면 안 되고, 여유와 곡률을 동시에 만족해야 한다:**

| 라인 | 길이 | 최소여유 | \|k\|max | 평균 | 복구 |
|---|---|---|---|---|---|
| 공식 | 346.0 | 1.68 | 0.236 | 47.28 | 0 |
| fitted | 328.6 | **0.76** | 0.231 | 96.08 (최고 45.43) | 43 |
| w1.8 (gradient) | 331.6 | 1.12 | **0.245** | 52.98 | 4 |
| **blend0.45** | **336.1** | **1.25** | **0.228** | **46.53** | **0** |

- `fitted`는 차 반폭(0.65 m 실측, config는 마진 포함 0.75) 대비 여유가 1 cm뿐이라 갇힌다.
- `widen_line.py`로 점을 밀면 여유는 얻지만 **곡률이 올라 접지를 넘는다**(0.245 → 요구 ay 17.0 vs 실측 한계 15.41).
- 공식 라인 쪽으로 **섞으면**(`blend`) 둘 다 확보된다. 곡률이 오히려 공식보다 낮아진다.

**새 라인 판정 기준**: 곡률 ≤ 0.231, 회전 스윕 요구량 대비 여유 잔량 ≥ 0.3 m, 스윕 부족 지점 0개. 스윕 요구량은 `max(√((R+0.65)²+1.554²), √((R+0.65)²+0.510²)) − R` (곡률 0.236에서 0.89 m).

### 속도는 올릴 수 없다 — 두 번 실패

| 시도 | 예측 | 실측 |
|---|---|---|
| 10구간 접지기반 (v_max 40) | 38.44 | 코너 2회 충돌 |
| 32구간 램프 (제동거리 검증 통과) | 35.37 | **68.45** (복구 16회) |

**오프라인 모델은 평평한 프로파일에서만 맞다.** 43.99 예측 → 45~46 실측(맞음), 35.37 예측 → 68.45 실측(33초 틀림). 모델은 프로파일을 그대로 추종한다고 가정하는데 MPC의 속도 해가 보수적이다. 구간을 잘게 쪼개면 `update_v_max`가 약 1초마다 바뀌어 솔버를 흔든다. **이론 최적에 가까워질수록 실제로는 나빠진다.**

### 진짜 천장: MPC 가 v_ref 를 못 따라간다

세 현상의 공통 원인이다.

1. `use_csv_speed_profile`: `v_ref`가 정상 로드(max 35.5 km/h)되어도 명령이 **2.55 m/s(9.2 km/h)에 고착**. 두 실행에서 동일 재현.
2. **출발 실가속 0.7 m/s²** — `a_max 1.35`의 절반. rosbag 실측(6~14초 구간 0.57~0.82, p95 1.01). 원인 후보 배제 완료: `KP=100`이라 오차 0.014 m/s에도 `a_max` 포화, 시뮬레이션에서는 `raw_vehicle_cmd_converter`가 안 뜨므로 `accel_map`도 무관.
3. 촘촘한 프로파일이 랩타임을 악화.

`MPC.py`에서 `R`은 억제항이 아니라 **`v_ref`로 끌어당기는 추종항**이다(226행 `q = -diag(R)*ur`, `ur=[v_ref, kappa_ref]`). `Q[2]=1.85e6`이 시간항. 143행 `umax_dyn`은 `ay_max` 기반 동적 상한. **왜 2.55 m/s가 나오는지 이 세 개를 로그로 찍어 확인하는 것이 다음 작업이다.** 웨이포인트 단위 이론값 33.60초와 실측 46.53초 사이 12초가 여기 잠겨 있다.

### 반복 속도

`EVAL_CACHE=1 ./docker_build.sh eval` 로 빌드가 4~5분 → **1분 54초**. 기본은 `--no-cache` 유지(제출 빌드 재현성). 한 사이클 약 7분.

### 대기 작업에 컨테이너 정리를 넣지 말 것

오늘 실험 세 개를 이것으로 날렸다. 결과를 기다리는 백그라운드 작업 끝에 `docker rm -f`를 넣어두면, 그 작업이 자기 폴링 시간이 다 되어 종료할 때 **그 시점에 실행 중인 다른 실험의 컨테이너를 지운다.** v40 실행은 시작 20초 뒤에 이렇게 죽었다. 정리는 명시적으로만.

## 2026-07-29 밤: 44.54초, 그리고 가속 경로가 닫혔다

**현재 최선 = `traj_top36_blend45.csv` + x1.10 속도 + 제동 리드인 3개 (13구간).**

```
46.13 / 44.10 / 44.17 / 44.42 / 44.00 / 44.43   평균 44.54  최고 44.00  복구 0회
```

경로: 47.28(앵커) → 46.53(라인) → 45.35(x1.05) → **44.54**(x1.10+리드인).

### 속도의 천장은 접지가 아니라 구간 전환의 제동 거리다

`ref_vel`은 현재 구간 값을 모든 웨이포인트에 쓰므로 **다음 구간을 미리 감속하지 않는다.** x1.10이 6랩 중 2랩(53.88 / 53.56)을 잃은 지점을 rosbag에서 특정했더니 wp149였고, **거기 곡률은 0.006으로 거의 직선**이다. 원인은 6 웨이포인트 뒤의 s3 33.0 → s4 24.2 km/h 전환으로, `a_min -1.6`에서 12.1 m가 필요한데 6 m만 있다. 제어기가 과제동해 3.09 m/s까지 떨어지며 5초를 잃는다.

x1.00은 같은 전환에 10.0 m 필요로 겨우 통과, x1.05는 11.0 m로 통과, x1.10은 실패.

**해법은 필요한 곳에만 중간 구간을 넣는 것.** 세 전환(s3→s4, s5→s6, s7→s8)에 리드인을 넣어 각 하강을 반으로 쪼갰다:

| 전환 | 필요 | 리드인 위치 | 속도 | 확보 |
|---|---|---|---|---|
| s3 33.0→24.2 | 12.1 m | wp139 | 28.6 | 15.9 m |
| s5 33.0→24.2 | 12.1 m | wp248 | 28.6 | 15.8 m |
| s7 33.0→27.5 | 8.0 m | wp309 | 30.2 | 10.5 m |

32구간으로 균등 분할한 버전은 68.45초였다 — 경계가 10 m마다면 `update_v_max`가 1초마다 바뀌어 솔버가 안정되지 못한다. **계산이 요구하는 곳에만 넣는 것이 핵심.**

### 접지 경계도 확인됐다

x1.15(최대 요구 ay 20.93, 1등 실측 최대 20.09 초과)는 랩 81.11 / 81.17 / 52.40, 복구 10회로 실패. 이건 리드인으로 고칠 수 없는 종류의 실패다. **x1.10이 이 라인에서의 상한.**

### 가속: `a_max` 를 올리는 경로는 닫혔다 (실측)

rosbag에서 명령과 실제를 대조했다. 명령 메시지는 `autoware_auto_control_msgs/AckermannControlCommand`(필드는 `longitudinal.speed`, `.acceleration`).

```
명령가속 1.35 (16초 내내 포화)   명령속도 9.17 m/s (= v_max 33)
실가속   0.30 ~ 0.79
```

속도대별 지속 가속: 0~1 m/s 0.70 / 1~2 0.76 / 2~3 0.80 / 3~4 0.66 / 4~5 0.63 / 5~6 0.61. 출발 구간 직접 계산은 4.8초→15.0초에 0.20→6.74 m/s, **0.64 m/s²로 일정**.

즉 **차량이 `a_max 1.35`를 낼 수 없다.** 기준선 로그의 "a_max=2.7로 올려도 동일"이라는 주장이 처음으로 실측으로 확인됐다. 0→33 km/h에 14초·64 m가 걸리고 그게 첫 랩이 항상 1.5~2초 느린 이유다.

**정정**: 이 세션 중반에 "MPC의 속도 해가 보수적이다"라고 썼는데 틀렸다. 그 근거였던 `u[0]=2.55 m/s`는 고장 난 `use_csv_speed_profile` 실행에서 본 값이다. 정상 설정에서 MPC는 `v_max`와 `a_max`를 온전히 명령한다.

따라서 출발 가속은 튜닝 대상이 아니다. 세 차량이 같은 한계를 가지므로 첫 코너 선점은 부스트 외에는 차별화 수단이 없다.

### 3대 자체 대회 (online3)

차량별 `MPC_REF_VEL_FILE` 배선을 이어서(`run_autoware.bash` → compose → launch 3개) 세 배율을 한 세션에서 붙였다.

| 차량 | 랩 | 평균 |
|---|---|---|
| x1.00 | **83.92** / 46.44 / 46.46 / 46.44 / 46.33 / 46.60 | 52.70 |
| x1.05 | **87.42** / 45.59 / 45.27 / 45.81 / 56.87 | 56.19 |
| x1.10 | **48.13** / 45.05 / 61.94 / 45.77 / 45.40 / 62.20 | 51.42 |

**첫 코너를 깨끗하게 빠져나간 x1.10만 첫 랩 48.13초**이고 나머지 둘은 83~87초로 사실상 끝났다. 사용자 관찰("첫 코너 1등이 곧 레이싱 1등, 뒤에서 받으면 패널티로 정지, 옆접촉은 허용")이 데이터로 확인된다. 우리 복구 노드에는 이미 도메인별 복구 조향(선두 0.15 / 2번 0.45 / 3번 이하 0.60 rad)이 들어 있어 그 규칙에 맞춰 설계돼 있다.

### 출발은 프로파일과 무관하게 불안정하다

x1.05 두 실행 중 하나가 출발부에서 101.47초를 잃었다(복구 `attempt=1..5`가 두 번, 속도 0.006 m/s로 완전 정지). 같은 이미지 재실행은 6/6 깨끗(45.35초). 3대 레이스에서도 세 대 전부 첫 랩이 망가졌다. **표본 2개 중 1개이므로 확률적이고, 첫 코너가 결과를 정하는 형식에서는 랩타임보다 큰 리스크다.**

## 다음에 할 일

1. 위 조합을 eval로 검증한다. 벽에 닿아도 복구 노드가 살려내므로, 결과에서 **어느 구간에서 복구가 발동했는지** 로그로 확인할 것(`stuck detected` 메시지).
2. 복구가 특정 코너에서 반복 발동하면 그 구간만 속도를 내린다. 라인은 유지.
3. 라인이 통하면 그 다음에 코너 속도를 한 구간씩 올린다. 한 번에 한 구간, 한 변수.
4. `dev.sh`는 `--wall-recovery on`이므로 후보 스크리닝은 dev로 먼저 하는 게 싸다. eval은 확정 후보에만 쓸 것.

## 참고

- 리더보드(`https://aichallenge-board.jsae.or.jp/public/live`)는 **로그인이 필요**하다. 헤드리스 브라우저로도 데이터가 안 나온다.
- 36.319초 rosbag은 로컬에 없다. URL은 `https://d3al8lo5i04x19.cloudfront.net/32cc7c53-7277-4c84-8d08-6f9f5d27d331/1/rosbag2_autoware.mcap`. 다만 필요한 telemetry는 이미 `env/final_ver3/traj_top36_telemetry.csv`(544점, 0.6 m 간격)로 추출돼 있다.
- 기준 결과: `output/20260728-172948/d1/{result-summary.json,autoware.log}`
- 진단 대시보드: 이 세션에서 만든 artifact (구간 속도 역전 + 격차 분해 시각화)
