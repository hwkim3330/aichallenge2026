# 제출물

작성: 2026-08-02 (이전 판 2026-07-29 는 아래에 그대로 둠)

## 현재 제출 파일

`submit/aichallenge_submit.tar.gz` — 2026-08-02 재생성
아카이브: `submit/archive/20260802-111000_ticks40_rate015_avoidoff.tar.gz`
직전본: `submit/archive/20260731-001300_prev_ticks20_rate025.tar.gz`

### 왜 재생성했나

7/31 판에는 funnel + deadlock breaker 코드는 들어 있었지만 기본값이 **TICKS 20 / FUNNEL_RATE
0.25** 였다. 8/2 측정에서 그 둘이 **지는 값**임이 확인됐다(`tools/GOAL.md`):

- `MPC_ANTIDEADLOCK_TICKS`: 20 → **40**. 480 초 완주율이 20 에서 7/12, 40·70 에서 9/12.
- `MPC_FUNNEL_RATE`: 0.25 → **0.15**. 0.25 와 0.08 을 모두 이긴다.

funnel + antideadlock 자체의 효과(12 레이스): base 32 랩 / 4-of-12 완주 → **68 랩 / 9-of-12**.

### 확정 설정 (tarball 안에서 직접 읽어 확인)

| 항목 | 값 |
|---|---|
| `MPC_ANTIDEADLOCK_TICKS` | 40 |
| `MPC_FUNNEL_RATE` | 0.15 |
| `use_obstacle_avoidance` | false |
| `use_path_constraints_provider` | false |
| 경로 | `env/final_ver3/traj_top36_blend45.csv` (336.1 m) |
| 속도 | 10 구간 22.0~30.0 km/h |
| `a_max` / `a_min` | 1.35 / −1.6 |
| `delta_max_deg` | 36.0 |

### 검증 — 실제 평가 하네스

`docker_build.sh eval --submit submit/aichallenge_submit.tar.gz` 로 이미지를 굽고 `make eval`:

```
6/6 완주
47.67 / 46.26 / 46.12 / 46.23 / 45.98 / 45.98
평균 46.37  최고 45.98  총 278.23 s
stuck 0  yield 0  process died 0
```

dev 경로 솔로 기준선(46.45 / 46.56)과 일치한다. 결과 원본: `submit/eval_verified_20260802.json`.

**이 검증이 dev 테스트와 다른 점**: `make eval` 은 소스 트리가 아니라 **제출 tarball 에서 구운
이미지**를 돌린다. 과거에 "낮 제출 기록 75-89 초 vs 로컬 46-48 초" 사고가 있었고 실험 중 config
가 제출됐을 가능성이 의심됐다. tarball 내용 직접 확인 → 그 tarball 로 이미지 빌드 → 하네스 실행
세 단계를 모두 거치면 그 경로가 막힌다.

### 회피(추월)는 왜 아직 off 인가

8/2 측정: 회피를 켜면 실행당 1-2 랩이 무너진다. 이벤트당 복구 시간이 기준선 중앙값 9.9 s 대비
provider 경유 48.4 s, 자체 계산 23.5 s 다. 원인은 좁혀졌으나(GOAL.md 참조) 아직 기준선 수준이
아니다. 다만 NPC 는 랩당 149 s 라 **추종만으로는 480 s 에 3.2 랩**이 한계이므로, 6 랩에는
추월이 필수다 — 이 작업은 계속된다.

---

# 제출물

작성: 2026-07-29

## 제출 파일

`submit/aichallenge_submit.tar.gz` (`--no-cache` 빌드)
아카이브 사본: `submit/archive/20260729-161141_SUBMIT_3race_winner_blend45_x100.tar.gz`

## 구성

| 항목 | 값 |
|---|---|
| 경로 | `env/final_ver3/traj_top36_blend45.csv` (336.1 m) |
| 속도 | 10구간, 22.0~30.0 km/h (2026-07-28 기준선 그대로) |
| `v_max` | 40.0 (구간값이 33 이하라 비구속) |
| `a_max` / `a_min` | 1.35 / −1.6 |
| `delta_max_deg` | 36.0 |
| `max_width` | 3.0 |
| 회피 | off |
| 복구 | `stuck_recovery_controller` 배선됨 |

## 선정 근거 — 실제 평가 형식(3대)으로 3회, 배치 회전

`simulator_scripts/online3.sh` (3대·6랩·600초·랭킹 on). 차량 번호는 ROS 도메인으로 고정되므로 **배치를 매 레이스 회전**해 그리드 편향을 제거했다.

| 후보 | 6랩 완주 | 총시간 (초) | 복구 |
|---|---|---|---|
| **A = 이 제출본** (blend45 + ×1.00) | **3 / 3** | 327.6 / 313.5 / **284.4** | 2 / 10 / 3 |
| B (blend45 + ×1.10 + 리드인) | 2 / 3 | 320.8 / 290.7 / **미완주** | 5 / 4 / **16** |
| C (공식 `traj_mincurv` + 기준선) | 1 / 3 | — / — / 329.0 | 12 / 8 / 6 |

2차전까지는 B가 답으로 보였다 — 두 번 다 이겼고 한 번은 뒷줄에서였다. 3회를 돌린 이유가 3차전이다: B가 **앞줄에서 5랩, 복구 16회**로 무너졌다. 랭킹 레이스에서 미완주는 회복 불가이고, 세 번 모두 완주한 것은 A뿐이다. 최고 총시간(284.4초)도 A가 냈다.

C는 탈락. 미완주 2회이고 그중 한 번은 앞줄이었다. 여유폭은 더 크지만(최소 1.68 m vs 1.25 m) 라인이 10 m 길어 트래픽에서 손해가 크다.

## 단독 성능 (참고)

```
6랩:  47.82 / 46.36 / 46.32 / 46.17 / 46.05 / 46.45   평균 46.53  복구 0회
```

시작점(2026-07-28 기준선, 공식 라인)은 47.28초였으므로 단독 −0.75초. 라인만 바꾼 결과다.

참고로 더 빠른 설정은 있었다. ×1.10 + 리드인은 단독 최고 44.54초(복구 0)를 냈고 깨끗한 랩은 모든 실행에서 44.3~44.6초로 일관적이다. 그런데 단독 3회 기대값이 약 49초(44.54 / 47.79 / 55.16)이고, 3대 레이스에서 3회 중 1회 미완주했다. 페이스는 진짜지만 완주 확률을 그만큼 지불한다.

## 재확인 불필요한 경계 (전부 실측)

- `x1.12` → 단독 49.58초, 랩 2개 손실. `x1.15` → 붕괴(81초대, 복구 10회). 최대 요구 횡가속도 20.93이 이 차량이 실제로 낸 최대 18.60을 넘는다.
- **구간별 접지 상한 설정은 세 번 다 실패**했다(리드인 유무·거친 구간·잘게 쪼갠 32구간). 모델의 접지 계산은 완벽 추종을 가정하는데 MPC는 인접 구간 간 큰 속도 변화를 따라가지 못한다. **검증된 모양의 비례 확대만 통한다.**
- `a_max`를 1.35 위로 올리는 것은 무의미하다. 명령 1.35가 출발 16초 내내 포화인데 차량은 0.64 m/s²만 낸다.
- `delta_max_deg`를 gain 보정값 22°로 맞추면 0.47초 느려진다. 기하가 요구하는 조향은 14.4°뿐이다.
- 회피(`use_obstacle_avoidance`)를 켜면 추월이 아니라 감속 후 줄서기가 된다.
- 라인을 더 줄이는 것도 한계다. 네 후보 라인의 곡률 분포가 사실상 동일하고(p95 0.188~0.206), 웨이포인트별 최적화는 336.1 → 333.6 m로 2.5 m만 줄인다. 묶는 제약은 여유가 아니라 곡률이다.

## 2026-07-29 밤: 연속 속도 프로파일 경로 복구 (Fable 서브에이전트)

`use_csv_speed_profile` 고착은 세 겹이었고 전부 실측으로 특정됐다.

1. **2.55 m/s 고착 = 이중 km/h→m/s 변환.** `MPCConfig.v_max`는 `create_mpc()`가 `mpc_controller.py:385`에서 이미 변환해 넣는다. 그날 오전의 "단위 수정"이 그것을 raw `self._cfg.mpc.v_max`(km/h)로 착각해 한 번 더 나눴다: 33/3.6/3.6 = 2.546. 원래 코드가 맞았다. **내가 만든 버그다** — 호출부 세 곳의 단위만 비교하고 `MPCConfig`를 채우는 385행을 안 봤다.
2. **로딩을 고쳐도 `u[0]`가 `v_ref`를 무시.** 시간항 `Q[2]=1.85e6`이 추종항 `R[0]=1e5`을 압도해 `u[0]=min(umax, umax_dyn)`이 된다. **구간 방식이 작동했던 이유가 이것이다** — `update_v_max()`가 구간 속도를 하드 제약으로 만들었기 때문. 지평 단계별로 `umax_dyn = min(umax_dyn, v_ref)`를 적용해 해결.
3. **정지 상태 4.58 m/s 캡 = 미수렴 OSQP 해가 `kappa_pred` 오염.** 측정된 `kpred=0.952`(그 지점 경로 곡률 0.033, 입력 경계 0.668). `get_control()`이 수렴 검사 없이 `dec.x`를 받는다. `kappa_pred`를 `tan(delta_max)/L`로 클립해 해결. 미수렴 해를 **거부하면 교착**된다(라인 이탈 → QP 영구 infeasible → 명령 0 → 복구 노드는 0이 아닌 명령을 조건으로 하므로 발동 못 함).

부수 수정: `except TypeError or ValueError`는 `TypeError`만 잡는다. `main()`의 생성자 예외 은폐(`UnboundLocalError` 마스킹)도 고쳤다.

### 그래서 얻은 것과 못 얻은 것

**연속적으로 변하는 `v_ref`는 속도와 무관하게 횡추종을 불안정하게 만든다.** 검증된 범위 이하 속도의 min-time 프로파일 세 개가 모두 중간 곡률 굽이에서 갇혔다(63~82초, 복구 15~67회). **평탄 구간 + 전환부 램프만**은 안정적이다.

최선 후보 `config_csvmt_ramps.yaml` + `traj_blend45_s110L_ramps.csv`(x1.10 구간값을 연속 경로로 램프와 함께 전달)는 깨끗한 랩 44.28~44.57로 x1.10 최고 페이스를 재현했다. 그러나 **채택하지 않았다**:

| | 단독 평균 | 3대 완주 총시간 평균 |
|---|---|---|
| 제출본 (flat x1.00) | **46.53** | **289.7** (294.2 / 285.1) |
| x1.10+리드인 | 46.17 (3회 평균 49) | 300.9 (288.2 / 313.6) |
| csv-ramps 후보 | 47.68 | 317.8 (307.6 / 327.9) |

깨끗한 랩은 후보가 1.5~2초 빠르지만 **총합에서 가장 나쁘다.** 더러운 랩에서 더 많이 잃는다. 완주율은 세 설정 모두 2/3이고, Fable의 3회 회전에서 **중간 그리드가 설정과 무관하게 3번 다 미완주**했으므로 완주 확률은 그리드가 결정한다 — 즉 설정 간 완주율 차이는 노이즈다.

코드 수정은 제출 후보와 무관하게 유지한다. 플랫 제출 설정으로 회귀 검증됨(단독 46.50 / 46.27 / 45.99 / 46.23, 복구 0회).

## 남은 레버

정체 지점은 곡률 0.073~0.080의 완만한 굽이에 여유 2.2~3.5 m인 곳이고, 차가 라인에서 1~3 m 벗어나 갇힌다. **즉 남은 문제는 형상이나 속도가 아니라 횡방향 추종 정확도**다. `Q[0]`(e_y), `Q[1]`(e_psi), `max_width` 코리도가 손대지 않은 축이다. 기준선 로그에 `Q` 실험 기록이 있으나 그것은 이전 세션의 주석이므로 신뢰하지 말고 다시 측정할 것.
